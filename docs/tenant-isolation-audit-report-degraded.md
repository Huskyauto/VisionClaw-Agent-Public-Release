# Tenant-Isolation Audit Report

_Generated 2026-09-20T13:57:14.669Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 0 file(s) were NOT audited this run (chunk cap in effect). Findings below are PARTIAL — do not read "few findings" as "all clear."

- Coverage: **689/689** file(s) audited · chunks 64/64 OK (0 failed)
- Findings: **222** (CRITICAL 46 · HIGH 128 · MEDIUM 47 · LOW 1)
- Severe dispositions: **174 kept** · 0 precision false-positive · 0 allowlist false-positive · 0 deferred real risk

### CRITICAL (46)

- **server/agentic/wake-scheduler.ts:142** — UPDATE on tenant-scoped table agent_wake_schedules uses an unscoped sub-SELECT (no tenant_id filter). The statement updates rows across all tenants: `WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ... )`.
  - _Fix:_ Limit the selection to a single tenant (e.g. add `AND tenant_id = ${tenantId}`) or iterate per-tenant so the worker only claims rows for one tenant at a time. Alternatively, include tenant_id in the outer WHERE (and in the inner SELECT) to ensure rows are only modified for the intended tenant. Consider rewriting as a per-tenant loop or passing an explicit tenantId to this function.
- **server/commitment-drafter.ts:234** — UPDATE against tenant-scoped table `commitments` with only WHERE id = ${row.id} (no tenant_id filter). This can modify a row belonging to another tenant if IDs collide or are guessed.
  - _Fix:_ Include tenant scoping in the WHERE clause when reverting/unclaiming commitments. Change the statement to: UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id} AND tenant_id = ${row.tenantId} (or AND tenant_id = ${tenantId} where available). Always ensure any UPDATE/DELETE on tenant-scoped tables includes an explicit tenant_id filter.
- **server/embeddings.ts:809** — storeEmbeddingVec falls back to executing UPDATE ${sql.raw(table)} ... WHERE id = ${id} when no tenantId is provided, i.e. it updates tenant-scoped tables (memory_entries/agent_knowledge) by id only with no tenant filter.
  - _Fix:_ Do not allow a blind UPDATE without tenant context. Require and validate a positive tenantId for tenant-scoped tables and include AND tenant_id = ${tenantId} in the WHERE clause; otherwise throw an error. Remove the unconditional raw-table update path.
- **server/embeddings.ts:855** — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} is executed without any tenant_id constraint and will update rows across tenants by id alone.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id || tenantId}) and ensure the backfill routine knows/validates the tenant for each row before updating.
- **server/embeddings.ts:872** — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} performs an update with no tenant_id constraint and may modify agent_knowledge rows for other tenants.
  - _Fix:_ Include AND tenant_id = ${row.tenant_id || tenantId} in the WHERE clause and ensure the routine tracks the tenant for each row before issuing the UPDATE. Avoid unscoped UPDATEs on tenant-scoped tables.
- **server/felix-brain.ts:23** — SELECT from tenant-scoped table `agent_runs` without any tenant_id filter (reads rows across all tenants). This rehydrates in-memory task state for all tenants, violating tenant isolation and risking cross-tenant data leakage.
  - _Fix:_ Restrict the query to a single tenant (e.g. add WHERE tenant_id = ${tenantId}) or run rehydration per-tenant. Alternatively, limit the rehydration to rows intended for the current service scope and avoid reading all tenant data in a global boot-time query.
- **server/google-drive.ts:1664** — INSERT into tenant-scoped table 'project_files' does not set tenant_id (INSERT VALUES omit tenant_id). Tenant_id columns are NOT NULL with no default, and omission leads to cross-tenant data corruption or insertion under an implicit/incorrect tenant.
  - _Fix:_ Include the tenant id explicitly in the INSERT (and bind it from a trusted source), for example: INSERT INTO project_files (project_id, tenant_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params.projectId}, ${resolvedTenantId}, ${params.fileName}, ...). Ensure resolvedTenantId is validated to belong to the caller before inserting.
- **server/lib/scheduled-post-runner.ts:61** — SELECT/UPDATE on tenant-scoped table `scheduled_posts` performed without constraining by tenant_id (CTE selects ids then updates rows across tenants). This allows the system runner to lock and change rows for all tenants in one statement.
  - _Fix:_ Restrict the CTE to a single tenant (WHERE tenant_id = <tenant>) or explicitly document and gate this cross-tenant operation behind an admin-only context. If the runner must operate across tenants, ensure an explicit admin/host-level authorization check before running and log/limit scope. Prefer selecting rows per-tenant (or pass a tenant list) to avoid accidental cross-tenant mutation.
- **server/mpeg-engine.ts:1058** — INSERT into tenant-scoped table `project_files` does not include tenant_id. The code checks ownership via assertProjectInTenant but still inserts a row without an explicit tenant_id column/value, violating the invariant that every INSERT into tenant-scoped tables must set tenant_id explicitly.
  - _Fix:_ Include the tenant_id column/value in the INSERT and validate it first. Example: ensure options.tenantId is a positive integer and change the query to INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${options.tenantId}, ${options.projectId}, ${...}). Keep the assertProjectInTenant check for ownership, but always persist tenant_id in the row to enforce application-level isolation.
- **server/mpeg-engine.ts:1445** — INSERT into tenant-scoped table `project_files` does not include tenant_id. Even though assertProjectInTenant is called, the INSERT omits tenant_id in the column list/values, risking records without tenant context and violating the tenant-isolation invariant.
  - _Fix:_ Add tenant_id to the INSERT column list and values, and validate options.tenantId is a proper positive integer before inserting. For example: INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${options.tenantId}, ${options.projectId}, ${...}). Maintain the ownership check but always write tenant_id with the row.
- **server/oauth-subscriptions.ts:906** — SELECT FROM oauth_subscriptions (SELECT id, provider, tenant_id, expires_at, refresh_token, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE) with no tenant_id constraint — reads subscription rows across all tenants (access tokens / refresh tokens).
  - _Fix:_ Restrict this scan to an explicit tenant (WHERE tenant_id = ...) or iterate a vetted per-tenant list and handle each tenant separately. Avoid querying all tenant rows in a single statement; ensure any background job that legitimately needs to operate across tenants performs explicit per-tenant authorization and limits scope.
- **server/oauth-subscriptions.ts:915** — SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' has no tenant_id constraint — enumerates subscription rows across tenants.
  - _Fix:_ Add an explicit tenant_id filter (WHERE provider = 'google-workspace' AND tenant_id = ${tenantId}) or fetch per-tenant. If this query is intentionally global, keep it in an admin-only path with additional safeguards and audit logging; avoid returning sensitive tokens/metadata for all tenants.
- **server/oauth-subscriptions.ts:1258** — SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE lacks tenant_id — background proactive refresh iterates subscriptions for all tenants and may access sensitive data across tenants.
  - _Fix:_ Change proactiveTokenRefresh to iterate explicit tenant IDs and run per-tenant queries (SELECT ... WHERE tenant_id = ${tenantId}) or otherwise restrict to a configured admin list. Do NOT run an unconstrained SELECT on a tenant-scoped secrets table.
- **server/oauth-subscriptions.ts:1328** — SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE (forceRefreshAllSubscriptions) has no tenant_id constraint — enumerates active subscriptions across tenants.
  - _Fix:_ Require an explicit tenant scope for refreshing (e.g., accept a tenantId parameter or iterate a verified tenant list). If an admin-wide job must run, ensure it runs with elevated audit/logging and implements safe handling so tokens are not leaked to non-admin contexts.
- **server/plan-executor.ts:744** — UPDATE on tenant-scoped table "plans" without tenant_id constraint (UPDATE plans SET status='executing' ... WHERE id = ${planId} AND status = 'approved' RETURNING ...). This can change a plan in any tenant by id alone.
  - _Fix:_ Add AND tenant_id = ${callerTenantId} to the WHERE clause, or otherwise ensure the update is performed only within the authenticated/validated tenant context. Prefer an atomic claim pattern that includes tenant_id in the WHERE or performs a FOR UPDATE with tenant-scoped filter.
- **server/plan-executor.ts:1280** — claimStaleRecoveryChildCore performs an UPDATE on tenant-scoped table "plans" without a tenant_id filter (UPDATE plans SET status='executing' WHERE id=${planId} AND source='stale-plan-recovery' AND status='approved' RETURNING ...). This allows claiming/executing a plan by id across tenants.
  - _Fix:_ Require and include tenant_id in the WHERE clause (AND tenant_id = ${callerTenantId}) when claiming the plan. If this function must operate cross-tenant only for admin flows, make that explicit by passing an admin tenant id or safe admin guard and document it; otherwise, constrain to the plan's tenant.
- **server/project-brain.ts:135** — SELECT FROM projects WHERE id = ${projectId} is executed with no tenant_id filter. The function takes no tenant context and reads tenant-scoped project data.
  - _Fix:_ Require and pass an explicit tenantId to updateProjectBrain and always include AND tenant_id = ${tenantId} in the SELECT. Verify ownership before reading or writing tenant-scoped resources.
- **server/project-brain.ts:332** — Backfill query selects projects JOIN conversations without any tenant scoping (SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ...). This can enumerate projects across tenants.
  - _Fix:_ Restrict the query to a specific tenant (e.g. WHERE p.tenant_id = ${tenantId} AND c.tenant_id = ${tenantId}), or otherwise iterate per-tenant ensuring all queries include tenant_id filters.
- **server/project-brain.ts:349** — Query selecting conversations for a project (SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id}) lacks tenant_id constraint and can return conversations from other tenants.
  - _Fix:_ Include tenant scoping (e.g. AND c.tenant_id = ${tenantId}) and ensure proj was resolved within the same tenant before using its id in further queries.
- **server/project-brain.ts:359** — Selecting messages (SELECT role, content FROM messages WHERE conversation_id = ${conv.id}) has no tenant filter — messages are tenant-scoped and this reads data across tenants.
  - _Fix:_ Add tenant scoping to the messages query (e.g. AND tenant_id = ${tenantId}) or validate the conversation belongs to the expected tenant before reading messages.
- **server/recurring-messages.ts:307** — UPDATE on tenant-scoped table `agent_knowledge` is executed without constraining by tenant_id (WHERE id = ${r.id} only). This can modify rows belonging to other tenants.
  - _Fix:_ Always include tenant scoping on updates. Change the query to include AND tenant_id = ${r.tenant_id} (or pass the tenantId for this operation and use WHERE id = ${r.id} AND tenant_id = ${tenantId}), or use Drizzle's eq(agent_knowledge.tenant_id, tenantId) to ensure the update cannot affect another tenant's rows.
- **server/reference-learner.ts:296** — UPDATE on tenant-scoped table `memory_entries` is executed without constraining by tenant_id (WHERE id = ${storedId} only). This can update memory entries belonging to other tenants.
  - _Fix:_ Include the tenant guard in the UPDATE: WHERE id = ${storedId} AND tenant_id = ${input.tenantId} (or use Drizzle eq(memory_entries.tenant_id, input.tenantId)). Even if the id was looked up earlier with a tenant-scoped SELECT, every subsequent write must re-check/include tenant_id in the WHERE clause.
- **server/research-engine.ts:171** — UPDATE on tenant-scoped table research_sessions performed without any tenant_id constraint (global write). The cleanupZombieSessions function issues: `UPDATE research_sessions ... WHERE status = 'running' AND started_at < ${staleThreshold}` with no tenant scoping — this can modify rows across all tenants.
  - _Fix:_ Restrict the UPDATE to a single tenant context or explicitly iterate tenants. For example, add AND tenant_id = ${tenantId} (passing a validated tenantId), or run the cleanup per-tenant inside a loop that supplies a validated tenantId. Alternatively, record and use the tenant set to exclude activeSessions per tenant and parameterize via sql.join to avoid interpolation. Ensure every UPDATE/DELETE/SELECT against research_sessions includes a tenant_id filter unless the operation is intentionally global and guarded by an explicit admin-only check.
- **server/routes.ts:188** — Unscoped SELECT on tenant-scoped table `file_storage` during startup restore: db.select(...).from(fileStorage).where(like(fileStorage.mimeType, 'image/%') ... ) does not constrain by tenant_id.
  - _Fix:_ Restrict restore to a single tenant at a time or iterate tenants and include a tenant filter (e.g. where(eq(fileStorage.tenantId, tenantId), ...)). Do not perform a cross-tenant SELECT that can write files to the shared uploads directory; instead run per-tenant queries and restore into tenant-isolated locations or otherwise enforce per-tenant authorization before writing disk files.
- **server/sculptor.ts:106** — UPDATE sculptor_sessions executed without scoping to tenant (missing AND tenant_id = ...) — write to a tenant-scoped table by id only
  - _Fix:_ Include the tenant filter in the UPDATE (e.g. AND tenant_id = ${params.tenantId} or the appropriate tenant variable) so the write cannot modify rows in other tenants. Use parameterized value, not string interpolation.
- **server/sculptor.ts:113** — UPDATE sculptor_sessions SET conversation_id ... WHERE id = ${session.id} has no tenant_id constraint — tenant-scoped write without tenant filter
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId} or ${tenantId} passed into monitor/update) so only the caller's tenant row can be modified.
- **server/sculptor.ts:174** — UPDATE sculptor_sessions SET ... WHERE id = ${sessionId} (monitorSession) lacks tenant_id constraint — unscoped write to tenant-scoped table
  - _Fix:_ Constrain this UPDATE by tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) — ensure the monitorSession code uses the tenantId parameter in the query.
- **server/sculptor.ts:359** — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} has no tenant_id constraint — write to tenant-scoped table without tenant filter
  - _Fix:_ Include AND tenant_id = ${tenantId} (or the appropriate tenant variable) in the WHERE clause so only rows belonging to the caller's tenant are updated.
- **server/second-opinion.ts:504** — UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} performs a write against a tenant-scoped ledger row identified only by id (no tenant_id filter) — possible cross-tenant modification if ids collide
  - _Fix:_ Require and pass tenant_id into settleFusionReservation and include it in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}). Validate the reservation row belongs to the expected tenant before updating.
- **server/seed-cleanup.ts:8** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation' lacks tenant scoping — reading tenant-scoped table globally
  - _Fix:_ Scope the query to a tenant (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND enabled = true AND type = 'delegation') or otherwise operate only on explicitly global tables. Avoid performing cross-tenant reads without explicit intention.
- **server/seed-cleanup.ts:16** — UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' AND enabled = true updates heartbeat_tasks across all tenants (no tenant_id filter)
  - _Fix:_ Add a tenant filter (e.g. AND tenant_id = ${ADMIN_TENANT_ID} or the specific tenant) to prevent disabling tasks in other tenants. If the intent is to target the admin tenant, make that explicit in the WHERE clause.
- **server/seed-cleanup.ts:45** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND name ILIKE '%session%logger%' lacks tenant scoping — unscoped read across tenants
  - _Fix:_ Restrict the query to a tenant (e.g. WHERE tenant_id = ${ADMIN_TENANT_ID} AND enabled = true AND name ILIKE ...) or otherwise ensure the table is truly global before running tenant-agnostic operations.
- **server/seed-cleanup.ts:52** — UPDATE heartbeat_tasks SET enabled = false WHERE name ILIKE '%session%logger%' AND enabled = true lacks tenant_id constraint — disables matching tasks across all tenants
  - _Fix:_ Add AND tenant_id = ${ADMIN_TENANT_ID} (or the appropriate tenant) to the WHERE clause, or redesign to operate only on intended tenant rows.
- **server/seed-cleanup.ts:59** — SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND (name ILIKE ... ) is missing tenant scoping — unscoped read against tenant-scoped table
  - _Fix:_ Scope the query by tenant (e.g. tenant_id = ${ADMIN_TENANT_ID}) or confirm heartbeat_tasks is a global table if global behavior is intended; otherwise add tenant filter.
- **server/seed-cleanup.ts:76** — UPDATE heartbeat_tasks SET enabled = false WHERE (name ILIKE ... ) AND enabled = true updates rows across all tenants (no tenant_id in WHERE)
  - _Fix:_ Include tenant scoping (e.g. AND tenant_id = ${ADMIN_TENANT_ID}) to avoid modifying other tenants' rows. If global changes are intended, document and ensure heartbeat_tasks is a global table.
- **server/seed.ts:2172** — UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 AND primary_conversation_id IS NULL is executed without a tenant_id constraint. projects is a tenant-scoped resource — this UPDATE can modify a project in any tenant.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tenantId} AND primary_conversation_id IS NULL) or use Drizzle's .where(eq(projects.tenantId, tenantId)). Never perform unscoped UPDATEs against tenant-scoped tables.
- **server/seed.ts:2240** — SELECT DISTINCT c.id, c.project_id FROM conversations c JOIN projects p ... is executed without restricting by tenant_id (the JOIN verifies p.tenant_id = c.tenant_id but there is no WHERE tenant_id = <caller>). The script then inserts project↔conversation links based on that result. This effectively reads across all tenants and can create cross-tenant associations.
  - _Fix:_ Restrict the SELECT to the intended tenant (e.g. add WHERE c.tenant_id = ${tenantId} AND p.tenant_id = ${tenantId}) or otherwise explicitly scope the operation to a safe system/owner tenant. Also consider adding tenant_id to project_conversations (or include tenant_id in the insert) so link rows remain tenant-scoped and cannot be created across tenants.
- **server/tool-learning.ts:316** — DELETE of custom_tools in revertToolRow uses only id in WHERE: db.delete(customTools).where(eq(customTools.id, tool.id)). A delete without tenant scoping can remove rows belonging to other tenants if an id is guessed or duplicated across tenants.
  - _Fix:_ Require and include tenant_id in the DELETE predicate, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tool.tenantId))). Additionally, ensure revertToolRow receives/validates the tool.tenantId before performing the delete.
- **server/tools.ts:2931** — INSERT into tenant-scoped table `project_files` does not set tenant_id. The INSERT statement (VALUES ...) omits tenant scope, so rows may be created without explicit tenant ownership.
  - _Fix:_ Include tenant_id explicitly in the INSERT into project_files (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})). If you rely on the project_id ownership check, still persist tenant_id on the row to enforce application-level isolation and make downstream reads simple and unambiguous.
- **server/tools.ts:2945** — INSERT into tenant-scoped table `project_notes` does not set tenant_id. The INSERT INTO project_notes (...) VALUES (...) omits tenant scope.
  - _Fix:_ Include tenant_id explicitly in the INSERT into project_notes (e.g. INSERT INTO project_notes (..., tenant_id) VALUES (..., ${tenantId})). Do not rely solely on a prior project ownership query; every tenant-scoped row must record tenant_id at write-time.
- **server/tools.ts:2969** — INSERT into tenant-scoped table `project_conversations` does not set tenant_id. The code inserts (project_id, conversation_id) without persisting tenant_id on the new row.
  - _Fix:_ Add tenant_id to the INSERT into project_conversations (e.g. INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (${params.id}, ${params.conversationId}, ${tenantId})). Persisting tenant_id prevents cross-tenant contamination and makes later queries safe when a tenant filter is expected.
- **server/tools.ts:7006** — INSERT into tenant-scoped table 'project_files' does not include tenant_id in the column list/VALUES. The code inserts (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) but omits tenant_id.
  - _Fix:_ Include the tenant id explicitly in the INSERT. Either use Drizzle's insert API with a tenantId field or add tenant_id to the column list and VALUES (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${params.project_id}, ...)). Ensure params._tenantId is validated/derived from authenticated context (and keep the existing assertProjectInTenant check).
- **server/tools/domains/browser/handlers.ts:165** — UPDATE on tenant-scoped table 'browser_workflows' lacks a tenant_id constraint (UPDATE browser_workflows SET last_replayed = NOW() WHERE id = ${workflow.id}). This can allow modifying another tenant's row by id.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Example: add AND tenant_id = ${tenantId} to the WHERE clause (or use Drizzle .where(eq(table.tenantId, tenantId))). Ensure all UPDATE/DELETE statements against tenant-scoped tables include the tenant filter and that all writes set tenant_id explicitly when inserting.
- **server/tools/domains/strategic-memory/handlers.ts:103** — UPDATE memory_entries SET ... WHERE id = ${id} is executed without tenant scoping. Although id was earlier selected under a tenant filter, performing an UPDATE by id alone can modify rows from other tenants if an id is mixed or reused.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${tid}) to ensure the write is constrained to the caller's tenant. Always pair writes with an explicit tenant_id clause even when ids were previously selected under tenant scope.
- **server/tools/domains/strategic-memory/handlers.ts:164** — UPDATE memory_entries SET ... WHERE id = ANY(${ids}) is executed without tenant scoping. This bulk update can affect rows belonging to other tenants if ids are manipulated or a race occurs.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ANY(${ids}) AND tenant_id = ${tid}) or perform the update via a join that enforces tenant_id = ${tid}. Never perform writes to tenant-scoped tables using id-only predicates.
- **server/tools/domains/strategic-memory/handlers.ts:263** — UPDATE memory_entries SET ... WHERE id = ${id} is executed without tenant scoping in recordStrategicWinHandler. As above, this allows cross-tenant modification if ids are not strictly bound to the caller's tenant.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${id} AND tenant_id = ${tid}) before performing the write. Audit all memory_entries writes to ensure tenant_id is always part of the WHERE clause.

### HIGH (128)

- **server/agentic-engines.ts:463** — Incorrect tenant filter in UPDATE: the WHERE clause uses tenant_id = ${insight.insightId} instead of tenant_id = ${insight.tenantId}. This will either fail to update or update the wrong tenant's rows.
  - _Fix:_ Use the correct tenant id in the WHERE clause: WHERE id = ${insight.insightId} AND tenant_id = ${insight.tenantId}. Add tests to catch tenant-variable mixups and consider centralizing tenant param passing to avoid copy/paste errors.
- **server/agentic/approvals.ts:135** — expireStaleApprovals performs a bulk UPDATE on agentApprovals to set status='expired' WHERE status='pending' AND expiresAt < now() with no tenant_id filter. This will expire approvals across all tenants in one statement and then act on their linked runs.
  - _Fix:_ Either scope the bulk expiry to a tenant or implement an explicit per-tenant sweep (select tenant ids then run tenant-scoped UPDATEs). Avoid performing cross-tenant UPDATEs without explicit per-tenant scoping and add safeguards/tests to prevent accidental multi-tenant writes.
- **server/agentic/resume-worker.ts:65** — Raw SELECT on tenant-scoped tables (agent_runs / agent_approvals) without a tenant_id constraint — the query returns rows across all tenants.
  - _Fix:_ Restrict the query to a tenant context before selecting (e.g. add WHERE r.tenant_id = ${tenantId} or otherwise iterate per-tenant). If the sweep intentionally needs to operate across tenants, ensure it's running with an auditable privileged path and that any returned data is handled under strict access controls. Prefer explicit per-tenant queries or an administrative-only audit path rather than an unbounded SELECT that exposes multi-tenant rows.
- **server/agentic/task-forces.ts:41** — createTaskForce inserts a provided projectId into task_forces without first verifying that the referenced project row belongs to the caller's tenant. This allows linking a task force to a project that may belong to another tenant.
  - _Fix:_ Validate foreign IDs before persisting: SELECT the referenced project with `WHERE id = ${projectId} AND tenant_id = ${p.tenantId}` and reject if not found/owned. Enforce this check for any foreign id fields (projectId, conversationId, etc.).
- **server/agentic/wake-scheduler.ts:66** — scheduleWake INSERT writes foreign ids (persona_id, conversation_id, project_id) provided by the caller into agent_wake_schedules without verifying that those referenced rows belong to the caller's tenant.
  - _Fix:_ Before inserting, validate any referenced IDs (persona_id, conversation_id, project_id) belong to the tenant by querying each table with `WHERE id = <id> AND tenant_id = ${p.tenantId}` and reject or nullify them if not owned. Alternatively, perform these checks in a transaction and fail-closed if ownership cannot be proven.
- **server/auto-asset-capture.ts:184** — SELECT (correlated subquery) against tenant-scoped table `messages` omits tenant_id constraint in WHERE clause. The query: SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0 ...
  - _Fix:_ Constrain the subquery to the conversation's tenant (e.g. AND tenant_id = c.tenant_id) or join via an already-scoped messages query. Always include tenant_id in WHERE for tenant-scoped tables when scanning across tenants.
- **server/auto-asset-capture.ts:207** — SELECT from tenant-scoped table `messages` omits tenant_id constraint: SELECT content FROM messages WHERE conversation_id = ${c.id} AND role = 'assistant' ...
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. AND tenant_id = ${c.tenant_id}) or otherwise verify/derive the tenant from a prior, trusted row before reading messages.
- **server/auto-project.ts:135** — SELECT from tenant-scoped table `messages` omits tenant_id constraint: SELECT role, LEFT(content, 300) as content FROM messages WHERE conversation_id = ${conversationId} ...
  - _Fix:_ Include AND tenant_id = ${tenantId} (or join to conversations with tenant_id) so the messages read are constrained to the caller's tenant. Do not rely solely on conversation_id without tenant scoping.
- **server/auto-transcript.ts:46** — SELECT from tenant-scoped table `messages` omits tenant_id constraint: SELECT role, content, created_at FROM messages WHERE conversation_id = ${conversationId} ...
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. AND tenant_id = ${tenantId}) or otherwise verify ownership before reading messages to prevent cross-tenant access.
- **server/auto-transcript.ts:128** — SELECT (correlated subquery) against tenant-scoped table `messages` omits tenant_id constraint in WHERE clause: (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0
  - _Fix:_ Constrain the subquery to the conversation's tenant (e.g. AND tenant_id = c.tenant_id) or rewrite as a join that enforces tenant scoping so the existence/count check can't touch other tenants' rows.
- **server/auto-transcript.ts:153** — SELECT (correlated subquery) against tenant-scoped table `messages` omits tenant_id constraint in WHERE clause in the `alsoLinked` query: (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0
  - _Fix:_ Add tenant scoping to the subquery (e.g. AND tenant_id = c.tenant_id) or otherwise ensure the message-count check is limited to the same tenant as the conversation to prevent cross-tenant reads.
- **server/build-video-from-brief.ts:427** — Unverified caller-supplied Drive file ID (userImageDriveFileId) is downloaded server-side without verifying it belongs to the caller's tenant. This lets a caller supply an arbitrary Drive ID and cause the server to read potentially cross-tenant/private files.
  - _Fix:_ Before downloading a caller-supplied Drive file ID, validate ownership/permission: e.g. call a storage/drive helper that fetches the file metadata and asserts the file is owned by (or shared with) the tenantId, or provide a server-side mapping of tenant-scoped uploaded assets and only allow IDs from that mapping. Fail closed if ownership cannot be proven.
- **server/browser-tool.ts:1687** — Caller-controlled _projectDriveFolderId (passed via params) is forwarded into uploadScreenshotToDrive/uploadAndShare as a parentFolderId without verifying the folder belongs to the caller's tenant. This can enable cross-tenant writes or placing artifacts into folders the caller does not own.
  - _Fix:_ Do not trust a caller-supplied folder ID. Resolve and validate folder ownership before use (e.g. map project → drive folder on the server, or call a helper that checks folder metadata and confirms tenant ownership/permission). If validation fails, refuse to use the provided folderId and either store in a tenant-owned folder or return an error.
- **server/business-tools.ts:96** — INSERT into invoice_items omits tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERT (columns are NOT NULL with no default).
  - _Fix:_ Include tenant_id in the invoice_items INSERT (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${newId}, ...)). Ensure invoice_items has tenant_id NOT NULL and that it is set from the caller's authenticated tenant. Also validate invariants (or assert the invoice belongs to the tenant) before inserting line items.
- **server/ceo-orchestrator.ts:660** — SELECT from personas table with no tenant filter: `const cachedPersonas = await db.select().from(personasTable);` reads persona rows across all tenants (no WHERE tenant_id = ...) and can leak cross-tenant data.
  - _Fix:_ Constrain persona reads to the plan owner: include a tenant filter when querying personas, e.g. using Drizzle: `await db.select().from(personasTable).where(eq(personasTable.tenantId, plan.tenantId))`. Or, if a full cache is required, partition the cache by tenant and only expose entries for the current tenant before use.
- **server/ceo-orchestrator.ts:1026** — Using persona IDs from a tenant-unfiltered cache when creating conversations: targetPersona is chosen from cachedPersonas (global result) and then passed as personaId into storage.createConversation with tenantId = plan.tenantId. There is no verification that the chosen persona actually belongs to the plan's tenant.
  - _Fix:_ Validate persona ownership before using persona.id for another tenant. Either (1) only populate cachedPersonas with rows scoped to the current tenant (see previous finding), or (2) check the persona row's tenant_id equals plan.tenantId before using its id. If channel/global personas are intentional, add explicit cross-tenant authorization checks and document the design.
- **server/channel-routing.ts:30** — SELECT JOIN reads personas without tenant constraint: the query in listChannelRoutes JOINs `personas p ON p.id = cr.persona_id` but does not restrict p.tenant_id. This can expose persona names/rows from other tenants.
  - _Fix:_ Make the query tenant-aware. Either add tenant_id to channel_routes and filter by it, or join with a tenant predicate: `JOIN personas p ON p.id = cr.persona_id AND p.tenant_id = ${tenantId}` and require the function to accept the caller's tenantId. Prefer making channel_routes tenant-scoped and ensuring all queries include WHERE tenant_id = callerTenantId.
- **server/channel-routing.ts:48** — setChannelRoute inserts/updates a channel_routes row referencing a persona_id provided by the caller without validating that the persona belongs to the caller's tenant (and channel_routes has no tenant_id column). This can allow a caller to point global routing to a persona from another tenant.
  - _Fix:_ Enforce tenant ownership before writing: either (a) add tenant_id to channel_routes and include it in INSERT/UPSERT so routing is per-tenant, or (b) verify the supplied personaId belongs to the caller's tenant by querying personas with a tenant_id filter before inserting. Never accept a foreign persona_id from an untrusted caller without ownership validation.
- **server/channel-routing.ts:61** — getPersonaForChannel reads channel_routes (and ultimately returns persona_id) without any tenant scoping. If channel_routes should be tenant-scoped, this function lacks a WHERE tenant_id = <caller tenant>. If channel_routes is global, callers may use returned persona_id without verifying that persona belongs to their tenant.
  - _Fix:_ Make the function tenant-aware: require caller tenantId and query `SELECT persona_id FROM channel_routes WHERE channel = ${channel} AND enabled = true AND tenant_id = ${tenantId}` (or otherwise validate the returned persona_id belongs to the calling tenant before use). If channel_routes is intentionally global, add explicit ownership checks wherever the persona_id is consumed.
- **server/cmmc-assessments.ts:145** — getCmmcAccessByToken performs a SELECT joining cmmc_assessment_invitations and cmmc_assessments but does not include a tenant_id constraint in the WHERE clause. The query can read tenant-scoped rows without an explicit tenant ownership filter (no eq(...tenantId, ..) on the invitation/assessment tables).
  - _Fix:_ Bind the lookup to a tenant before reading tenant-scoped rows. Options: (1) Accept a tenantId parameter and add eq(cmmcAssessmentInvitations.tenantId, tenantId) (and/or eq(cmmcAssessments.tenantId, tenantId)) to the WHERE clause; or (2) if the token is intended to fully authorize access, ensure the token lookup also asserts the tenant (e.g. store/derive tenant in the token record and include eq(...) in the WHERE). Never perform cross-tenant SELECTs on tenant-scoped tables without validating tenant ownership.
- **server/data-protection.ts:149** — Query selects rows from tenant-scoped table `conversations` without constraining by tenant_id. The statement `SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}` reads across all tenants and then performs tenant-scoped deletes based on the returned tenant_id values — this violates the invariant that tenant-scoped reads must include an application-level tenant predicate.
  - _Fix:_ Restrict the initial SELECT to an explicit tenant context or otherwise make the cross-tenant intent explicit and guarded. Options: (a) run this purge per-tenant (iterate known tenant IDs) and add `AND tenant_id = ${tenantId}` to the WHERE clause; (b) require an explicit admin-authorized flag and run the work inside a guarded transaction using withTenantTx for each tenant to set app.current_tenant; or (c) if the job must operate globally, document and gate it behind a platform-admin-only path and use RLS/SET ROLE or other strong authorization. Avoid unbounded selects across tenant-scoped tables.
- **server/doc-collections.ts:294** — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} updates a tenant-scoped table using only id without verifying tenant_id. This is a write against a tenant-scoped row with no tenant constraint in the WHERE clause.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) to ensure the write cannot affect rows belonging to other tenants. Derive tenantId from the same context used to SELECT the rows earlier (or include tenant_id in the loop context) and always include it in WHERE clauses for updates/deletes.
- **server/email.ts:370** — SELECT from inbox_messages uses only message_id with no tenant_id filter. This lookup can match rows from other tenants and cause incorrect deduping/behavior across tenants.
  - _Fix:_ Include tenant scoping in the query (e.g. WHERE message_id = ${msgId} AND tenant_id = ${tenantId}) or otherwise determine the tenant first and restrict the SELECT to that tenant.
- **server/email.ts:415** — Backfill SELECT from inbox_messages (SELECT id, message_id FROM inbox_messages ...) has no tenant_id constraint. Reading inbox_messages across all tenants can leak tenant data to the backfill process.
  - _Fix:_ Perform backfill per-tenant (accept a tenantId) or add an explicit tenant_id = ${tenantId} WHERE clause so only rows for the intended tenant are read.
- **server/email.ts:428** — UPDATE inbox_messages SET body_text = ..., body_html = ... WHERE id = ${row.id} does not include tenant_id in the WHERE clause. This could modify inbox_messages rows belonging to other tenants.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}), or derive and pass the tenantId alongside the row id before updating.
- **server/embeddings.ts:845** — Global backfill SELECT from memory_entries (SELECT id, embedding FROM memory_entries WHERE ... LIMIT 200) has no tenant_id constraint. This reads memory entries across all tenants.
  - _Fix:_ Perform backfill per tenant (accept a tenantId) or add tenant_id = ${tenantId} to the WHERE clause so reads are restricted to a single tenant at a time.
- **server/embeddings.ts:861** — Global backfill SELECT from agent_knowledge (SELECT id, embedding FROM agent_knowledge WHERE ... LIMIT 200) lacks a tenant_id filter and will read rows across tenants.
  - _Fix:_ Run backfill per-tenant or add tenant_id = ${tenantId} to the WHERE clause so only rows for the intended tenant are read and processed.
- **server/embeddings.ts:814** — backfillMissingKnowledgeEmbeddings SELECT (SELECT id, tenant_id, title, content FROM agent_knowledge WHERE embedding IS NULL ...) reads agent_knowledge across tenants (no tenant scoping).
  - _Fix:_ Either accept a tenantId and filter on tenant_id = ${tenantId}, or enumerate tenants and run the backfill per-tenant. Never run tenant-scoped reads across all tenants without explicit intent and safeguards.
- **server/felix-loop.ts:676** — UPDATE on tenant-scoped table `felix_loop_runs` uses WHERE id = ${runId} but does not include tenant_id. This can modify another tenant's row if runId is not validated against tenant ownership.
  - _Fix:_ Include tenant scoping in the WHERE clause for updates (e.g. WHERE id = ${runId} AND tenant_id = ${tenantId}). Always constrain UPDATE/DELETE statements on tenant-scoped tables to the caller's tenant_id.
- **server/felix-loop.ts:690** — Error-path UPDATE on tenant-scoped table `felix_loop_runs` updates by id only (WHERE id = ${runId}) without tenant_id, allowing accidental cross-tenant modification on recovery/error logging.
  - _Fix:_ Add tenant scoping to the error-update statement (e.g. WHERE id = ${runId} AND tenant_id = ${tenantId}), and validate runId ownership before performing recovery/error writes.
- **server/firecrawl.ts:231** — UPDATE on tenant-scoped table 'scraped_pages' omits tenant_id in WHERE clause (uses only id). Without tenant constraint this can update another tenant's row if ids collide.
  - _Fix:_ Include the tenant constraint in the UPDATE WHERE (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) or otherwise ensure the update is executed only for rows owned by the caller's tenant.
- **server/firecrawl.ts:363** — UPDATE on tenant-scoped table 'scraped_pages' omits tenant_id in WHERE clause (uses only id). This occurs in the crawl loop when updating existing pages.
  - _Fix:_ Add tenant scoping to the update, e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Always constrain updates with tenantId to enforce isolation.
- **server/google-drive.ts:879** — UPDATE on tenant-scoped table 'projects' sets driveFolderId/driveFolderUrl with WHERE eq(projects.id, projectId) but omits tenant_id constraint. Although ownership was checked earlier, the UPDATE itself lacks an explicit tenant filter, violating the policy that all writes must include tenant scoping.
  - _Fix:_ Constrain the UPDATE by tenant as well, e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). Failing to do so can permit cross-tenant modifications if checks are bypassed.
- **server/google-workspace.ts:2287** — INSERT into presenter_slide_images does not include tenant scoping or ownership verification. The code inserts image rows keyed only by session_id (INSERT INTO presenter_slide_images (session_id, slide_index, ...)) without setting or checking tenant_id. If presenter_slide_images is a tenant-scoped table (or should be constrained by tenant ownership), this allows writes that are not constrained to the caller's tenant.
  - _Fix:_ Either include tenant_id explicitly in the INSERT values (e.g. INSERT ... (tenant_id, session_id, ...) VALUES (${tenantId}, ${sessionId}, ...)) or verify the session_id belongs to the expected tenant before inserting. Prefer a single parameterized statement that enforces ownership (or SELECT presenter_sessions WHERE id=${sessionId} AND tenant_id=${tenantId} and fail if not found) so every write to tenant-scoped data is constrained to the caller's tenant.
- **server/google-workspace.ts:2307** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} lacks a tenant_id constraint / ownership check. The code updates a tenant-scoped resource by session id only, without ensuring the session belongs to the caller's tenant.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or first SELECT the presenter_sessions row and verify its tenant_id matches the caller's tenant before updating. Never trust an external or derived session id without verifying tenant ownership; always include tenant_id in WHERE clauses for tenant-scoped tables.
- **server/heartbeat-schedules.ts:60** — SELECT * FROM research_schedules executes against a tenant-scoped table with no tenant_id constraint. This reads rows across tenants without tenant scoping.
  - _Fix:_ Restrict the query by tenant_id (e.g. WHERE tenant_id = ${tenantId}) or, if this must be a cross-tenant scheduler operation, require an explicit admin/system context (pass and assert ADMIN_TENANT_ID or a dedicated privileged flag) and document the trusted privilege. Avoid unscoped SELECT * on tenant-scoped tables from non-privileged contexts.
- **server/heartbeat-startup.ts:81** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} runs without constraining by tenant_id. This can modify another tenant's row if ids collide or attacker influences ids.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or otherwise assert an admin/system context (and restrict to ADMIN_TENANT_ID) before performing cross-tenant updates. Always include tenant_id in writes to tenant-scoped tables.
- **server/heartbeat-startup.ts:85** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} runs without tenant_id in the WHERE clause (same issue as above).
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or ensure this mutation runs only under an explicit, audited admin/system tenant context.
- **server/heartbeat-task-scheduler-storage.ts:11** — getDueHeartbeatTasksAcrossTenants() issues SELECT * FROM heartbeat_tasks ... with no tenant_id filter. This is an unscoped read from a tenant-scoped table.
  - _Fix:_ If this function is intentionally a privileged cross-tenant scheduler, require and assert an explicit admin/system context (pass ADMIN_TENANT_ID or similar) and document that it is privileged. Otherwise add tenant_id = ${tenantId} to the WHERE clause or provide a separate, explicitly-named admin-only API that is clearly audited and protected.
- **server/heartbeat-task-scheduler-storage.ts:43** — UPDATE heartbeat_tasks ... WHERE id = ${id} ... is executed without a tenant_id constraint in claimDueHeartbeatTasksAcrossTenants(). This updates heartbeat_tasks without ensuring tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${id} AND tenant_id = ${expectedTenantId}) or ensure the caller provides/validates an explicit admin/system tenant context. Even when UPDATE targets a PK, enforce tenant_id checks to maintain the invariant that application-level filters enforce isolation.
- **server/heartbeat.ts:261** — SELECT * FROM event_log WHERE status = 'pending' ... (no tenant_id filter). This reads event_log rows across tenants without tenant scoping.
  - _Fix:_ Either add tenant scoping (WHERE tenant_id = ${tenantId}) and iterate per-tenant, or require that this code runs in a trusted admin scheduler context and explicitly assert that context. Avoid unscoped SELECTs on tenant-scoped tables unless the call-site is explicitly privileged and audited.
- **server/heartbeat.ts:2682** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} executes without tenant_id constraint inside processDelegations. Writes to tenant-scoped table lacking tenant filter.
  - _Fix:_ Constrain the UPDATE to the creating tenant (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or perform the update via the storage layer that asserts/uses the task's tenant. Ensure every UPDATE to tenant-scoped tables includes tenant_id in WHERE.
- **server/heartbeat.ts:3155** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} (delegateTaskFromChat) executes without tenant_id in the WHERE clause, modifying tenant-scoped data without tenant scoping.
  - _Fix:_ Add tenant filtering to the UPDATE (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or call a storage API that enforces tenant ownership. All mutations to tenant-scoped tables must validate/limit by tenant_id.
- **server/job-queue.ts:154** — claimDueJobs performs an UPDATE on agent_jobs (setting status='running', attempts++, lease_until, etc.) without scoping to a tenant_id — the UPDATE/subselect operates across all tenants.
  - _Fix:_ Either scope the claim to a specific tenant (pass a tenantId into claimDueJobs and add AND tenant_id = ${tenantId} in the WHERE/subselect), or ensure this code is only runnable by a platform-scoped worker that is explicitly authorized to modify rows for all tenants. Prefer per-tenant claiming or an explicit, well-audited admin path to avoid accidental cross-tenant mutations.
- **server/job-queue.ts:192** — reclaimExpiredLeases issues two UPDATE statements that move/terminalize agent_jobs whose leases expired, but neither UPDATE includes tenant_id filtering — these writes affect jobs across all tenants.
  - _Fix:_ Scope lease-reclaim operations to an explicit tenant when invoked from tenant-scoped flows (or loop tenants and run per-tenant). If a global reclaim is required, ensure it runs under a platform-admin context with strict audit/authorization and consider adding a partitioning or guard so tenant-facing callers cannot trigger an unscoped reclaim.
- **server/job-queue.ts:334** — cancelJob updates agent_jobs (SET status='cancelled') by id without requiring or checking tenant_id. A caller could cancel a job belonging to another tenant by id.
  - _Fix:_ Require and check tenant ownership when cancelling: add a tenantId parameter and include AND tenant_id = ${tenantId} in the WHERE clause, or resolve/authorize the caller as a platform admin before allowing an unscoped cancel. Enforce this at the DB boundary to preserve tenant isolation.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor runs a cross-tenant aggregation: SELECT tenant_id, persona_id, COUNT(*) FROM agent_knowledge ... GROUP BY tenant_id, persona_id HAVING COUNT(*) >= ${MIN_ENTRIES} — this reads tenant-scoped data for all tenants in one unscoped query.
  - _Fix:_ Restrict this scan to admin/operator contexts only, or iterate tenants and run a per-tenant query (SELECT ... WHERE tenant_id = ${tenantId}) so each DB access is explicitly tenant-scoped. If the scan must be global, ensure it's executed under a platform-admin process that is audited and not callable from tenant-scoped code paths.
- **server/lib/action-ledger.ts:266** — Cross-tenant SELECT from tenant-scoped table `action_attempts` (listUnknownAttemptsAllTenants) with no tenant_id predicate. This reads tenant-scoped rows for all tenants in a single query.
  - _Fix:_ If this cross-tenant sweep is intentional for a reconciler, make the risk explicit and protect the callsite (run-only-by-admin service, audit/logging, minimal returned columns). Otherwise restrict to a single tenant by adding a tenant_id WHERE clause or change API to iterate per-tenant (pass tenantId into the function) so every read is explicitly tenant-scoped.
- **server/lib/action-ledger.ts:300** — UPDATE that modifies `action_attempts` across tenants (sweepStaleAttempts) uses no tenant_id constraint. The UPDATE uses a subquery selecting ids from action_attempts with no tenant filter, causing writes to affect rows for multiple tenants.
  - _Fix:_ Avoid cross-tenant writes. Either require an explicit tenantId parameter and include AND tenant_id = ${tenantId} in the UPDATE predicate, or confine this reconciler to an admin-only maintenance job with strong auditability. At minimum, document and gate the cross-tenant intent; prefer per-tenant transactional processing to preserve tenant isolation.
- **server/lib/agent-trace.ts:115** — UPDATE agent_trace_spans in persistSpanClose performs a tenant-scoped table write using only span_id in the WHERE clause (no tenant_id predicate). This allows closing/updating spans without enforcing the tenant ownership constraint.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE span_id = ${spanId} AND tenant_id = ${tenantId}). Pass the tenantId into persistSpanClose (or record tenantId when opening the span and store it with the open promise/map) so the close operation can assert and enforce tenant ownership before updating the row.
- **server/lib/ideabrowser-backfill.ts:49** — Query aggregates across the tenant-scoped projects table without constraining by tenant_id: `SELECT tenant_id, count(*)::int AS c FROM projects WHERE tags && ${IDEA_TAG_LITERAL}::text[] GROUP BY tenant_id`. This reads rows for all tenants in a multi-tenant DB, violating the invariant that every read against tenant-scoped tables must be constrained to the caller's tenant.
  - _Fix:_ Avoid scanning across all tenants. Either perform the short-circuit per-target-tenant (iterate wantedByTenant and query each tenant with `WHERE tenant_id = <tenant>`), or restrict the aggregation to a single tenant by adding `WHERE tenant_id = ${tenantId}`. If you need a cross-tenant migration/backfill tool, run it as an administrative, out-of-band job with an explicit admin-only path and careful access control; never leak cross-tenant reads in regular application code.
- **server/lib/inbox-ingest.ts:536** — SELECT joins tenant-scoped table `inbox_messages` (alias m) but does not constrain m.tenant_id to the caller tenant. The query filters c.tenant_id but joins m without a tenant_id predicate, which can allow leaking rows from another tenant if IDs collide.
  - _Fix:_ Add an explicit tenant constraint on the joined table (e.g. AND m.tenant_id = ${tenantId} in the WHERE clause or include m.tenant_id = c.tenant_id) so every read from inbox_messages is scoped to the caller's tenant.
- **server/lib/mention-injection.ts:120** — Query selects from `personas` via LEFT JOIN (p.name) but does not constrain personas by tenant_id. The SELECT filters cm.tenant_id, but the join to personas lacks a tenant predicate and may expose persona rows from other tenants.
  - _Fix:_ Constrain the join to the personas table by tenant (e.g. LEFT JOIN personas p ON p.id = cm.from_persona_id AND p.tenant_id = ${params.tenantId}) or otherwise filter p.tenant_id in the WHERE clause so all reads from personas include the caller's tenant_id.
- **server/lib/outcome-promotion.ts:158** — SELECT FROM tenant-scoped table deliverable_grade_events is executed without a tenant_id WHERE filter — performs cross-tenant read
  - _Fix:_ Restrict the query to a single tenant (add WHERE tenant_id = ${tenantId}) or iterate per-tenant and run the aggregation inside a per-tenant context. If this is intended as an admin/global job, require and validate an explicit admin privilege parameter and document the cross-tenant nature; otherwise avoid scanning tenant-scoped tables across all tenants.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from tenant-scoped table `agent_runs` executed with no tenant_id filter (scans all rows where status='running'). This reads potentially sensitive tenant-scoped rows across all tenants.
  - _Fix:_ Add an explicit tenant_id predicate to the WHERE clause or make this function an auditable admin-only operation (and require admin credentials) so cross-tenant reads are intentional and logged. If the intent is a platform-wide admin audit, enforce an explicit admin guard and record justification in telemetry.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from tenant-scoped table `mind_tickets` executed with no tenant_id filter (scans all rows where status='in_progress'). This reads potentially sensitive tenant-scoped rows across all tenants.
  - _Fix:_ Add an explicit tenant_id predicate to the WHERE clause or make this function an auditable admin-only operation (and require admin credentials) so cross-tenant reads are intentional and logged. If the audit must be platform-wide, require an explicit admin context and record the operation in audit logs.
- **server/martech-bundle.ts:314** — UPDATE against tenant-scoped table tenant_voice_profiles uses only the row id in the WHERE clause (eq(tenantVoiceProfiles.id, existing.id)) with no tenant_id constraint. This allows a row to be modified without explicitly constraining to the caller's tenant.
  - _Fix:_ Include the tenant_id in the UPDATE WHERE clause (or otherwise verify ownership) — e.g. .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId))). Never rely solely on the id for tenant-scoped writes; always assert tenant ownership server-side.
- **server/memory-graph.ts:130** — UPDATE against tenant-scoped table 'memory_categories' missing tenant_id predicate (UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}).
  - _Fix:_ Constrain the UPDATE to the caller's tenant. Add AND tenant_id = ${tenantId} (or use a parameterized Drizzle .where(eq(...)) ): e.g. UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId} AND tenant_id = ${tenantId}.
- **server/memory-graph.ts:709** — UPDATE against tenant-scoped table 'memory_entries' missing tenant_id predicate (UPDATE memory_entries SET category_id = ${categoryId} WHERE id = ${mem.id}).
  - _Fix:_ Include the tenant filter on the UPDATE to avoid cross-tenant writes. Use WHERE id = ${mem.id} AND tenant_id = ${tenantId} (or equivalent Drizzle .where(eq(...))).
- **server/memory-intelligence.ts:413** — UPDATE against tenant-scoped table 'knowledge_triples' missing tenant_id predicate (UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${c.id}).
  - _Fix:_ Restrict the UPDATE to the tenant in question to prevent modifying another tenant's row. Use WHERE id = ${c.id} AND tenant_id = ${tenantId} (or equivalent Drizzle .where(eq(...))).
- **server/memory/forgetting-store.ts:1103** — SELECT from tenant-scoped table 'memory_erasure_requests' without a tenant_id WHERE clause (listing stale erasure requests across tenants).
  - _Fix:_ If cross-tenant enumeration is not intended, add a tenant_id predicate. If the scheduler legitimately needs cross-tenant visibility, ensure this code path is strictly admin-only and that the caller is authenticated/authorized; otherwise scope queries by tenant (WHERE tenant_id = ${tenantId}) or provide a separate admin API with explicit authorization checks and audit logging.
- **server/orchestrator-ledger.ts:451** — Use of sql.raw(lockKey) inside tx.execute(sql`SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`) — raw SQL interpolation of a value derived from ledgerKey/tenantId. sql.raw with caller-provided input can lead to SQL injection or logic corruption of the advisory lock expression.
  - _Fix:_ Avoid sql.raw with unescaped/concatenated values. Use a properly typed parameter binding (e.g., pass the bigint value as a normal interpolated parameter: sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)` or compute the numeric BigInt in JS and pass it as a bound parameter). Validate/normalize ledgerKey before deriving the lock key and do not accept arbitrary user-supplied SQL fragments.
- **server/persona-export.ts:33** — SELECT * FROM personas WHERE id = ${personaId} has no tenant_id constraint — reading a persona row without verifying it belongs to the caller's tenant.
  - _Fix:_ Constrain persona queries to the caller's tenant_id: SELECT * FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}. If persona lookup must be cross-tenant, restrict to admin-only flows and add explicit ownership/authorization checks before returning data.
- **server/plan-executor.ts:741** — SELECT from tenant-scoped table "plans" without constraining tenant_id (SELECT source FROM plans WHERE id=${planId} LIMIT 1). This reads a plan row across tenants using only the plan id.
  - _Fix:_ Include an explicit tenant_id constraint or resolve the tenant in a single parameterized statement. For example fetch the row using WHERE id = ${planId} AND tenant_id = ${callerTenantId}, or combine the probe with the claiming UPDATE/RETURNING that also includes tenant_id to avoid an unscoped cross-tenant read.
- **server/plan-executor.ts:1404** — Unscoped SELECT scanning "plans" for stale executing rows (SELECT id, tenant_id, status, plan_json, execution_log, version FROM plans WHERE status = 'executing' ...). This query reads potentially many tenants' rows without tenant_id restriction.
  - _Fix:_ If the intent is an admin/global maintenance task, require this code path to run under an explicit admin identity and document it; otherwise restrict the query by tenant_id = ${ADMIN_TENANT_ID} or iterate per-tenant. At minimum, avoid unscoped cross-tenant reads or gate them behind an explicit admin guard and audit log.
- **server/plan-executor.ts:1540** — SELECT from tenant-scoped table "plans" (transientFailures query) without tenant_id constraint (SELECT ... FROM plans WHERE status = 'failed' AND ceo_decision = 'approved' ...). This reads failed plans across all tenants.
  - _Fix:_ Scope this query by tenant_id or require an explicit tenant argument / admin identity. If the intended behavior is to scan across tenants for recovery, make the admin context explicit and ensure only privileged, audited code paths run it; otherwise add WHERE tenant_id = ${tenantId}.
- **server/plan-executor.ts:1600** — SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 (no tenant_id filter). This fetches approved plans across tenants for execution kick-off.
  - _Fix:_ Restrict to the appropriate tenant_id (or iterate per-tenant) or require an explicit admin tenant id. Ensure any queuing/kick logic only pulls rows for the tenant the worker is authorized to operate on, or run it under an explicit and audited admin process that intentionally scopes work per-tenant before acting.
- **server/process-governor.ts:1235** — UPDATE governance_rules executed without scoping by tenant_id — WHERE id = ${rule.id} has no tenant constraint.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}) so a rule update cannot affect another tenant's rows.
- **server/process-governor.ts:898** — UPDATE agent_desks sets status for a desk row using only id (WHERE id = ${desk.id}) with no tenant_id constraint.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${desk.id} AND tenant_id = ${tenantId}) or otherwise verify the desk belongs to the tenant before mutating it to prevent cross-tenant modification.
- **server/recurring-messages.ts:265** — SELECT from tenant-scoped table `agent_knowledge` is executed without a tenant_id filter (global read).
  - _Fix:_ If a global scan is intentional (cron runner), call the approved helper that documents/flags global access (e.g. listScheduledMessages with _allowGlobalSystemUseOnly) and ensure downstream per-row writes always include tenant scoping. If this should be tenant-scoped, add an explicit WHERE tenant_id = ${tenantId} (or equivalent Drizzle eq) to the SELECT so rows are limited to a single tenant.
- **server/routes.ts:979** — SELECT from tenant-scoped table `file_storage` by filename with NO tenant_id WHERE clause (const owners = await db.select(...).from(fileStorage).where(eq(fileStorage.filename, baseName))). This returns rows across tenants based on a request-derived filename.
  - _Fix:_ Avoid querying all tenants' rows for a request-supplied filename. Constrain the query to the authenticated/signed tenant (eq(fileStorage.tenantId, authedTenantId)) or, if you must detect collisions, perform a safe, explicit per-tenant check (e.g. query only for the authedTenantId and separately decide how to handle basename collisions). Ensure any logic that relies on cross-tenant results is executed only with explicit admin privilege and clearly documented.
- **server/routes.ts:1927** — SELECT from tenant-scoped table `presenter_sessions` by token with NO tenant_id constraint (SELECT ... FROM presenter_sessions WHERE token = ${token}). The token is request-derived and the row's tenant_id is not validated against caller context before use.
  - _Fix:_ Treat the token as a capability that must be validated server-side, or scope the query by tenant when possible (WHERE token = ${token} AND tenant_id = ${expectedTenantId}). If using a signed token as the sole auth, ensure the token cryptographically binds the tenant and validate that binding before returning tenant-scoped data. Prefer returning only fields necessary and avoid further actions that assume caller's tenant without explicit verification.
- **server/routes.ts:1955** — SELECT from tenant-scoped table `presenter_sessions` by token with NO tenant filter (SELECT id, tenant_id FROM presenter_sessions WHERE token = ${token}). The handler then uses the returned tenant_id, but the initial lookup was not constrained to caller context.
  - _Fix:_ Either include a tenant constraint derived from a validated capability or session, or verify that the caller is authorized for the tenant_id returned (e.g. compare to an authenticated tenant or validate a cryptographic token that binds token→tenant). Avoid trusting a row returned from an unconstrained lookup as proof of caller ownership without explicit verification.
- **server/routes.ts:4653** — INSERT into tenant-scoped table (project_notes) omits tenant_id. The code selects a project_id scoped to conv.tenantId but then inserts a note without setting tenant_id, which can create rows not bound to the caller's tenant and risk cross-tenant data exposure.
  - _Fix:_ Explicitly include and set tenant_id on the INSERT and/or use a storage layer call that enforces tenant scoping. Example: INSERT INTO project_notes (project_id, tenant_id, note, author) VALUES (${projId}, ${tenantId}, ${snapshot.slice(0,500)}, ${'system:context-guard'}) after verifying ownership. Alternatively call a tenant-aware API (e.g. storage.createProjectNote(..., tenantId)).
- **server/routes/admin.ts:910** — SELECT from heartbeat_tasks omits tenant_id — querying a tenant-scoped table without constraining to the caller's tenant.
  - _Fix:_ Add an explicit tenant constraint (e.g. WHERE tenant_id = ${tenantId}) or otherwise ensure this admin route intentionally performs cross-tenant reads only after an explicit and auditable admin approval. Prefer using Drizzle query builders (db.select().from(...).where(eq(table.tenantId, tenantId))) rather than raw/interpolated SQL.
- **server/routes/admin.ts:899** — SELECT from ai_insights (stuckInsights) omits tenant_id — reading tenant-scoped insights without restricting to tenant.
  - _Fix:_ Constrain the query by tenant_id (WHERE tenant_id = ${tenantId}) or explicitly document and gate the cross-tenant intent behind a requirePlatformAdmin check. Use Drizzle builders to attach the tenant filter rather than interpolating strings.
- **server/routes/admin.ts:921** — SELECT from ai_insights (stuckPending) omits tenant_id — tenant-scoped rows are read without a tenant filter.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (or otherwise require and log an explicit platform-admin decision for cross-tenant reads). Prefer Drizzle's parameterized where(eq(...)) forms to avoid implicit interpolation issues.
- **server/routes/conversations.ts:119** — UPDATE on tenant-scoped table 'conversations' without constraining tenant_id. The query 'UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}' lacks 'AND tenant_id = ...'
  - _Fix:_ Include tenant scoping in the UPDATE WHERE clause (e.g. WHERE id = ${conv.id} AND tenant_id = ${tenantId} or use conv.tenantId). Alternatively, call a storage-layer function that enforces tenant ownership before mutating the row.
- **server/routes/conversations.ts:120** — SELECT from tenant-scoped table 'project_conversations' without tenant constraint: 'SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}' has no tenant_id filter.
  - _Fix:_ Filter by tenant_id in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or otherwise derive and validate the tenant ownership of both project and conversation before reading this table.
- **server/routes/conversations.ts:123** — INSERT into tenant-scoped table 'project_conversations' does not set tenant_id: 'INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)'.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id})) after validating ownership of project and conversation for that tenant.
- **server/routes/conversations.ts:125** — UPDATE on tenant-scoped table 'projects' without tenant constraint: 'UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}' lacks tenant_id check.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) so only the tenant's project row is modified, or use a storage API that enforces tenant ownership.
- **server/routes/conversations.ts:256** — INSERT into tenant-scoped table 'project_notes' does not set tenant_id: 'INSERT INTO project_notes (project_id, note, author) VALUES (...)' lacks tenant_id.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note}, ${author})) and ensure the project belongs to that tenant before inserting.
- **server/routes/inbox-notifications.ts:101** — UPDATE executed on tenant-scoped table `inbox_messages` without constraining by tenant_id (missing AND tenant_id = ${tenantId}). This allows changing another tenant's row if an attacker obtains or guesses the message id.
  - _Fix:_ Include the tenant_id predicate on the UPDATE. For example: await db.execute(sql`UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}`); Alternatively, use the storage helper that accepts an explicit tenant scope to perform the update so tenant isolation is enforced consistently.
- **server/routes/projects.ts:401** — INSERT into tenant-scoped table `project_conversations` omits tenant_id. (INSERT INTO project_conversations (project_id, conversation_id) VALUES ... )
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${targetProjectId}, ${conversationId})). Ensure all inserts into tenant-scoped tables explicitly set tenant_id and validate it matches the authenticated tenant.
- **server/routes/projects.ts:569** — INSERT into tenant-scoped table `project_notes` omits tenant_id. (INSERT INTO project_notes (project_id, note, author) VALUES ... )
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${id}, ${req.body.note}, ${author})). Always persist tenant_id for tenant-scoped records and reject or validate derived tenant values.
- **server/routes/projects.ts:609** — INSERT into tenant-scoped table `project_files` omits tenant_id in /api/projects/:id/files-base64 handler. (INSERT INTO project_files (project_id, file_name, file_path, ...) VALUES ... )
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, file_path, ...) VALUES (${tenantId}, ${projectId}, ...)). Ensure all file upload paths set tenant_id and are executed under the authenticated tenant.
- **server/routes/projects.ts:616** — UPDATE on tenant-scoped table `projects` lacks tenant constraint. (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}) — no tenant_id filter.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}). Every UPDATE/DELETE/SELECT against tenant-scoped tables must include tenant_id = <caller tenant> or equivalent.
- **server/routes/projects.ts:677** — INSERT into tenant-scoped table `project_files` omits tenant_id in multipart upload handler. (INSERT INTO project_files (project_id, file_name, ...) VALUES ... )
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${tenantId}, ${projectId}, ...)). Ensure both upload handlers behave identically and always record tenant_id.
- **server/routes/projects.ts:684** — UPDATE on tenant-scoped table `projects` lacks tenant constraint. (UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}) — no tenant_id filter.
  - _Fix:_ Add tenant constraint to the UPDATE (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}). Do not rely solely on prior ownership checks — the query itself must include tenant scoping.
- **server/routes/projects.ts:703** — SELECT from tenant-scoped table `project_files` omits tenant constraint. (SELECT * FROM project_files WHERE project_id = ${projectId} ...)
  - _Fix:_ Constrain the SELECT to the caller tenant (e.g. SELECT * FROM project_files WHERE project_id = ${projectId} AND tenant_id = ${tenantId} ORDER BY ...). Every read against tenant-scoped tables must include tenant_id filtering in the SQL statement (or an equivalent JOIN/WHERE that enforces tenant_id).
- **server/routes/projects.ts:726** — SELECT from tenant-scoped table `project_files` omits tenant constraint in file download handler. (SELECT file_name, file_type, file_path FROM project_files WHERE project_id = ${projectId} AND file_path = ...)
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId} AND file_path = ...). This prevents cross-tenant file lookup if orphaned rows or malformed FKs exist.
- **server/routes/projects.ts:755** — DELETE from tenant-scoped table `project_files` omits tenant constraint. (DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId})
  - _Fix:_ Include tenant_id in the DELETE (e.g. DELETE FROM project_files WHERE id = ${fileId} AND project_id = ${projectId} AND tenant_id = ${tenantId}). Always require tenant filtering on destructive operations.
- **server/routes/projects.ts:166** — Subqueries counting dependent tenant-scoped tables (project_files, project_notes, project_conversations) do not include tenant_id filtering. (SELECT COUNT(*) FROM project_files WHERE project_id = p.id ... )
  - _Fix:_ Constrain the subqueries by tenant (e.g. SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}) or JOIN through projects where the tenant condition is explicit in the same statement. Every read against tenant-scoped tables must include tenant_id in the query itself.
- **server/routes/watchlist.ts:38** — addWatchlistItem is invoked with an object literal that spreads untrusted req.body after tenantId ({ tenantId, ...req.body }). Because later object properties override earlier ones in JS, a caller can supply tenantId in the body to override the server-resolved tenant, enabling cross-tenant writes.
  - _Fix:_ Do not allow the client to override tenantId. Either spread req.body first and then append tenantId ({ ...req.body, tenantId }) so the server value wins, or (preferable) change the API to pass tenantId as a separate explicit parameter and have addWatchlistItem ignore any tenantId in the body. Also validate/normalize inputs server-side and ensure the DB INSERT always uses the server-provided tenantId (and rejects or ignores any client-supplied tenant_id).
- **server/seed.ts:42** — SELECT COUNT(*) FROM research_sessions is executed without constraining by tenant_id. research_sessions is tenant-scoped but this read scans all tenants.
  - _Fix:_ Add an explicit tenant filter to the query (for example: SELECT COUNT(*) FROM research_sessions WHERE tenant_id = ${tenantId}) or use Drizzle .where(eq(research_sessions.tenantId, tenantId)). If this is intended to be an owner/system-only operation, make that explicit and scope it to the admin/owner tenant in the WHERE clause.
- **server/seed.ts:3639** — DELETE FROM heartbeat_tasks is executed without constraining by tenant_id. heartbeat_tasks is a tenant-scoped table (idx_heartbeat_tasks_tenant_id exists), so this DELETE will remove matching rows across all tenants.
  - _Fix:_ Restrict the DELETE to a single tenant (e.g. WHERE tenant_id = ${tenantId} AND ...), or enumerate tenants and run a per-tenant cleanup. If the intent is to only affect the platform/system tenant, add AND tenant_id = ADMIN_TENANT_ID. Avoid global deletes against tenant-scoped tables.
- **server/seed.ts:3667** — UPDATE memory_entries (the memory normalizer) runs without any tenant_id filter. memory_entries is tenant-scoped (idx_memory_entries_tenant_id exists), so this will update rows for all tenants.
  - _Fix:_ Constrain the UPDATE to the intended tenant(s) (e.g. AND tenant_id = ${tenantId}), or perform the normalization in a per-tenant loop. If the goal is to run only for the platform demo tenant, explicitly use that tenant_id.
- **server/seed.ts:3693** — UPDATE conversations SET model = 'gpt-5' WHERE model = 'gpt-4.1' has no tenant_id restriction. conversations is tenant-scoped (idx_conversations_tenant_id exists), so this mutates data for all tenants.
  - _Fix:_ Apply the model backfill per-tenant (WHERE tenant_id = ${tenantId} AND model = 'gpt-4.1') or iterate tenants and run the update scoped to each tenant. If intended for a specific tenant, add an explicit tenant_id condition.
- **server/self-improvement.ts:434** — UPDATE against tenant-scoped table `experiments` uses only experiments.id in WHERE and lacks a tenant_id constraint, so it could update a row belonging to another tenant.
  - _Fix:_ Constrain the UPDATE to the caller's tenant. E.g. include the tenant condition: .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, config.tenantId!))). Ensure the tenantId used is the authenticated caller's tenant and not user-controlled.
- **server/self-reflection.ts:383** — UPDATE on tenant-scoped table `memory_entries` uses WHERE id = ... without also restricting by tenant_id, allowing cross-tenant updates if ids collide or are manipulated.
  - _Fix:_ Add a tenant check to the UPDATE WHERE clause (e.g. WHERE id = ${id} AND tenant_id = ${tenantId}). Even if you previously selected the row with tenant_id, enforce the tenant restriction on the UPDATE to prevent race/TOCTOU or malicious id usage.
- **server/source-repair-handoff.ts:272** — Unscoped SELECT from tenant-scoped 'plans' table — the query SELECT id, tenant_id, source_ref, plan_json, execution_log FROM plans WHERE status = 'handoff_pending' ... has no tenant_id filter. This reads rows across tenants and relies on later per-row updates for enforcement, violating the invariant that reads must be constrained.
  - _Fix:_ Constrain the query to a specific tenant (e.g. WHERE tenant_id = ${tenantId} AND status = 'handoff_pending') or iterate per-tenant so background reconciliation only selects plans for one tenant at a time. If the intent is to scan across tenants, make the operation explicit and perform tenant-scoped selects/updates for each tenant so every DB read/write includes tenant_id in the WHERE clause.
- **server/stability-watchdog.ts:211** — DELETE FROM heartbeat_tasks is executed without any tenant_id constraint. heartbeat_tasks is tenant-scoped; this DELETE will remove matching rows across all tenants.
  - _Fix:_ Scope the DELETE to a specific tenant (e.g. add AND tenant_id = ${tenantId}) or iterate per-tenant in an admin-only context. If this is intended as a system-wide cleanup, make the admin intent explicit (require an ALL_TENANTS sentinel or an explicit admin check) and document/guard it to avoid accidental cross-tenant deletes.
- **server/storage.ts:591** — getMemoryEntry performs a SELECT from memory_entries potentially without a tenant predicate when tenantId is undefined (it emits a warning but proceeds). This allows unscoped reads on a tenant-scoped table.
  - _Fix:_ Make tenantId required for this API (fail-closed) or require callers to pass an explicit ALL_TENANTS sentinel for administrative use. Always include a tenant predicate (e.g. requiredTenantScope(memoryEntries.tenantId, tenantId) or eq(memoryEntries.tenantId, tenantId)) when reading tenant-scoped rows.
- **server/tenant-fork.ts:228** — UPDATE against a tenant-scoped config table omits tenant_id in the WHERE clause (tx.update(entry.table).where(eq(entry.table.id, p.newId))). This can modify rows across tenants if IDs collide or are guessed.
  - _Fix:_ Include the tenant filter on updates to tenant-scoped tables. For example: .where(and(eq(entry.table.id, p.newId), eq(entry.table.tenantId, newTenantId))). Use the tx-scoped newTenantId value (or otherwise bind the new tenant id) so the UPDATE is always constrained to the intended tenant.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table custom_tools omits tenant filter: db.select().from(customTools).where(eq(customTools.name, parsed.name)). This violates tenant isolation — reads must be scoped to the caller tenant.
  - _Fix:_ Add the tenant_id constraint to the WHERE clause, e.g. .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))). Ensure all reads on custom_tools are filtered by the provided tenantId.
- **server/tool-learning.ts:155** — SELECT by id against custom_tools omits tenant filter: db.select().from(customTools).where(eq(customTools.id, inserted.id)). Reads against tenant-scoped tables must include tenant_id to enforce isolation.
  - _Fix:_ Constrain the SELECT by tenant as well, e.g. .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))). If 'inserted' contains tenantId, use that value to scope the query.
- **server/tool-learning.ts:291** — UPDATE of custom_tools (auto-fix path) uses only the primary id in WHERE: db.update(customTools).set({ implementation: fixedImpl }).where(eq(customTools.id, tool.id)). Missing tenant_id constraint risks cross-tenant mutation.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tool.tenantId))). Always pair PK filters with tenant_id when modifying tenant-scoped tables.
- **server/tool-learning.ts:365** — UPDATE to increment usageCount on custom_tools uses only id in WHERE: db.update(customTools).set({...}).where(eq(customTools.id, tool.id)). Missing tenant filter allows cross-tenant modification of usage counts.
  - _Fix:_ Scope the UPDATE by tenant too: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tool.tenantId))). Consistently apply tenant_id checks on all UPDATE/DELETE/SELECT operations against tenant-scoped tables.
- **server/tools.ts:2926** — SELECT queries against `project_files` (duplication check) do not include tenant_id in WHERE. These SELECTs use project_id and file_name/file_url but lack an explicit tenant_id constraint.
  - _Fix:_ Constrain SELECTs on project_files by tenant (e.g. AND tenant_id = ${tenantId}), or rewrite them to JOIN projects and check p.tenant_id = ${tenantId}. Every read of a tenant-scoped table must include an explicit tenant constraint.
- **server/tools.ts:2966** — SELECT id FROM project_conversations WHERE project_id = ... AND conversation_id = ... has no tenant_id constraint. Reading project_conversations without tenant scope risks cross-tenant enumeration or matches.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} AND tenant_id = ${tenantId}) or JOIN to a tenant-scoped parent (projects) and assert its tenant_id; prefer explicitly including tenant_id in the table queried.
- **server/tools.ts:2844** — Correlated subqueries in the projects LIST query count rows in tenant-scoped tables (project_files, project_notes, project_conversations) but the subqueries lack tenant_id filters. They rely only on project_id = p.id without an explicit tenant constraint.
  - _Fix:_ Include tenant constraints within the subqueries (e.g. (SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = ${tenantId}) ...) or perform JOINs that enforce p.tenant_id = ${tenantId}. Ensure every read from tenant-scoped tables is explicitly constrained by tenant_id.
- **server/tools.ts:4546** — INSERT into project_notes omits tenant_id. This writes to a project-scoped table without explicitly setting tenant ownership in the INSERT (tenant_id column not provided).
  - _Fix:_ Include the tenant_id column/value in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, ...) VALUES (${params._tenantId}, ${projectId}, ...)) or otherwise ensure the tenant id is passed as a parameter in the SQL. Prefer explicit application-level tenant filters on every write; consider adding DB constraints or RLS as defense-in-depth.
- **server/tools.ts:4550** — INSERT into project_files omits tenant_id. The code inserts a project_files row using project_id but does not pass tenant_id in the INSERT values.
  - _Fix:_ Add tenant_id to the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${params._tenantId}, ${projectId}, ...)). Ensure every tenant-scoped table write includes the caller's tenant id and that ownership was validated before write.
- **server/tools.ts:6465** — INSERT into project_files omits tenant_id. The handler inserts into project_files (project_id, file_name, ...) without explicitly setting tenant_id even though project_files appears tenant-scoped.
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${params._tenantId}, ${params.project_id}, ${filename + '.' + ext}, ...)). Ensure assertProjectInTenant is used (it is) and still pass tenant_id explicitly on every INSERT to enforce isolation.
- **server/tools.ts:8977** — UPDATE on tenant-scoped table 'proposed_skills' uses only WHERE eq(proposedSkills.id, row.id) without constraining by tenant_id. This can allow cross-tenant updates if an id from another tenant is supplied or guessed.
  - _Fix:_ Constrain UPDATEs by tenant as well. Add a tenant predicate to the WHERE clause, e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) (or the equivalent Drizzle expression). Use the local tenantId (tid) that's in scope to ensure the row belongs to the caller's tenant.
- **server/tools.ts:9007** — UPDATE on tenant-scoped table 'proposed_skills' uses only WHERE eq(proposedSkills.id, row.id) without constraining by tenant_id (security-scan rejection path).
  - _Fix:_ Add a tenant constraint to the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Ensure tid is the calling tenant's id and is validated before updating.
- **server/tools.ts:9021** — UPDATE on tenant-scoped table 'proposed_skills' sets status='accepted' but restricts by id only (WHERE eq(proposedSkills.id, row.id)), missing tenant_id guard.
  - _Fix:_ Include tenant_id in the update WHERE clause to enforce tenant isolation (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Verify tid is the caller's tenant id.
- **server/tools.ts:9038** — UPDATE on tenant-scoped table 'proposed_skills' sets status='rejected' but uses WHERE eq(proposedSkills.id, row.id) only, lacking a tenant_id constraint.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Ensure the operation verifies the row belongs to the caller's tenant before mutating it.
- **server/tools/domains/knowledge/handlers.ts:91** — UPDATE on tenant-scoped table `knowledge_triples` executed without constraining by tenant_id (WHERE clause lacks tenant filter). This UPDATE uses a row.id derived from a prior SELECT but does not include tenant_id in the WHERE, violating the invariant that all writes/updates on tenant-scoped tables must be tenant-constrained.
  - _Fix:_ Restrict the UPDATE to the caller's tenant. e.g. UPDATE knowledge_triples SET valid_until = ${validFrom}, updated_at = NOW() WHERE id = ${row.id} AND tenant_id = ${tenantId} (or use a parameterized Drizzle eq(knowledgeTriples.tenantId, tenantId)). This ensures a row id alone cannot be used to affect another tenant's rows.
- **server/tools/domains/files/handlers.ts:941** — UPDATE on tenant-scoped table `projects` executed without a tenant_id constraint (UPDATE projects SET updated_at = ... WHERE id = ${projId}). Even though projId was obtained earlier via a tenant-checked query, the UPDATE itself lacks an explicit tenant filter in its WHERE clause.
  - _Fix:_ Add an explicit tenant constraint to the UPDATE to enforce isolation and defend against race/TOCTOU issues. For example: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projId} AND tenant_id = ${tenantId}. Prefer using parameterized Drizzle calls or include eq(projects.tenantId, tenantId) in the WHERE.
- **server/tools/domains/security/handlers.ts:163** — Query against provider_keys omits tenant scoping — SELECT provider, LENGTH(api_key) ... FROM provider_keys has no WHERE tenant_id = ${tenantId}. This reads across tenants (provider key inventory) for non-scoped callers.
  - _Fix:_ Constrain queries on provider_keys to the caller's tenant (e.g. WHERE tenant_id = ${tenantId}) or perform an explicit admin-authorisation check before running cross-tenant queries. Use the parameterized tenantId binding (scanSql`... AND tenant_id = ${tenantId}`) to avoid accidental cross-tenant reads.
- **server/tools/domains/security/handlers.ts:260** — Query counts distinct providers in provider_keys without tenant scoping — SELECT COUNT(DISTINCT provider) ... FROM provider_keys has no tenant filter. This enumerates provider configuration across tenants.
  - _Fix:_ Add tenant scoping (WHERE tenant_id = ${tenantId}) or require an explicit admin-only path/authorization before running cross-tenant aggregation. Prefer parameterized drizzle/sql interpolation with the tenantId variable to ensure safe binding.
- **server/twilio.ts:102** — SELECT from tenant-scoped table `agent_knowledge` without constraining by tenant_id. The query retrieves pairing rows across all tenants: `SELECT id, content::jsonb AS info FROM agent_knowledge WHERE category = 'messaging_pairing' AND title = ${`${channel}:${phone}`} ...`.
  - _Fix:_ Restrict the query to the caller/owner tenant (e.g. add `AND tenant_id = ${tenantId}`) or otherwise avoid scanning all tenants. If you intentionally need to detect contested pairings across tenants, perform that via a trusted/operator-side mechanism (explicit admin query or a separate global pairing registry) and ensure any multi-tenant read is guarded, audited, and cannot leak or let an attacker influence routing. Prefer selecting only rows you can prove belong to the tenant and avoid blind cross-tenant reads of tenant-scoped tables.
- **server/twilio.ts:116** — SELECT from tenant-scoped table `conversations` without an explicit tenant_id constraint: `SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} LIMIT 1`. This reads conversation rows across tenants by id without scoping to the claimed tenant.
  - _Fix:_ Constrain the lookup to the claimed tenant and conversation id in one statement (e.g. `SELECT 1 FROM conversations WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId} LIMIT 1`) or otherwise include `AND tenant_id = ${...}` in the WHERE to ensure you only read rows belonging to the expected tenant. This avoids cross-tenant reads and enforces the tenant ownership check at the DB query level.
- **server/venture-discovery/budget.ts:156** — UPDATE against tenant-scoped table `agent_cost_ledger` without any tenant_id constraint (UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId}). This allows modifying ledger rows belonging to other tenants if an attacker supplies or guesses a reservation id.
  - _Fix:_ Include tenant scoping in the UPDATE (verify ownership) — e.g. require and pass tenantId into settleVentureReservation and use WHERE id = ${reservationId} AND tenant_id = ${tenantId} (or the Drizzle equivalent: .where(and(eq(table.id, reservationId), eq(table.tenantId, tenantId)))). Alternatively fetch the row first and validate its tenant_id before updating, and perform the change inside a transaction.
- **server/venture-discovery/budget.ts:170** — UPDATE against tenant-scoped table `agent_cost_ledger` without any tenant_id constraint (UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId}) in releaseVentureReservation. Same cross-tenant modification risk as above.
  - _Fix:_ As above, require/accept the tenantId for the reservation and include tenant_id in the WHERE clause (AND tenant_id = ${tenantId}), or validate the reservation belongs to the caller's tenant before performing the UPDATE. Use parameterized Drizzle APIs to enforce the tenant condition atomically.
- **server/workflow-templates.ts:71** — Tenant identifier is passed under a nonstandard key (_tenantId) to executeTool. If the downstream tool expects tenant_id or tenantId for DB INSERTs, this may result in inserts that do not set the required tenant column, breaking the invariant that every tenant-scoped INSERT must include tenant_id.
  - _Fix:_ Pass the tenant identifier using the exact parameter/key/name the tool and DB layer expect (e.g. tenant_id or tenantId) and make this a required field for any tool that performs DB writes. Additionally, audit produce_video (and all tools) to guarantee they set tenant_id on every INSERT and apply WHERE tenant_id = <caller> for all reads/updates.
- **server/workflow-templates.ts:73** — project_id is conditionally set to the admin project (14) only for the admin tenant; otherwise project_id is omitted. The code comment indicates that omitting project_id causes writes to fall back to a tenant-less project_files FK path, which can result in cross-tenant writes or inserts without tenant_id.
  - _Fix:_ Do not rely on implicit tenant-less FK fallbacks. Either always supply an explicit tenant-scoped project_id that belongs to the caller (after verifying ownership), or create a tenant-scoped project for the caller prior to writing. Validate ownership of any project_id before using it, and ensure produce_video (and downstream DB code) enforces tenant_id on every INSERT and applies tenant-scoped WHERE clauses on reads/updates.

### MEDIUM (47)

- **server/ab-optimizer.ts:110** — Global SELECT against tenant-scoped table `experiments` without a tenant_id filter (SELECT id, tenant_id, ... FROM experiments WHERE category = 'ab_test' AND status = 'running').
  - _Fix:_ Scope the query by tenant (e.g. add WHERE tenant_id = ${tenantId}) or explicitly iterate tenants and run the query per-tenant. Never run unscoped reads against tenant-scoped tables unless intentionally and safely implemented as an explicit multi-tenant sweep with careful controls and auditing.
- **server/agent-activity.ts:60** — getPersonality queries `personas` by personaId without constraining by tenant (SELECT ... FROM personas WHERE id = personaId). This reads a potentially tenant-scoped row without verifying tenant ownership.
  - _Fix:_ Add tenant scoping / ownership verification (e.g. WHERE id = ${personaId} AND tenant_id = ${tenantId}) or otherwise validate the persona belongs to the caller's tenant before returning persona data.
- **server/agent-desk.ts:295** — Unscoped SELECT retrieving personas (SELECT id, name FROM personas) in getDesksOverview — no tenant_id filter. This returns persona rows across tenants.
  - _Fix:_ Restrict the personas query to the current tenant (e.g. WHERE tenant_id = ${tenantId}) or otherwise map persona names from a tenant-scoped persona table. If personas are global by design, document that explicitly; otherwise enforce per-tenant filtering.
- **server/agentic-engines.ts:193** — SELECT from `plans` / `ai_insights` in suppressPendingRejectedPlanDuplicates is executed with an optional tenantScope; when tenantId is undefined the query has no tenant_id constraint and will scan all tenants.
  - _Fix:_ Either require an explicit tenantId for this operation or, if performing a global sweep, implement an explicit per-tenant loop (select tenants then run per-tenant queries) so each database read/update is explicitly scoped to a tenant (WHERE tenant_id = ...). Logging/auditing should make cross-tenant sweeps explicit.
- **server/agentic-engines.ts:500** — retryPendingMinervaRouting queries ai_insights for rows stuck in pending with no tenant_id constraint (SELECT ... FROM ai_insights WHERE status = 'applied' AND priority = 'high' AND action_taken = PENDING_PLAN_MARKER LIMIT 50).
  - _Fix:_ Scope the query to a tenant (e.g. iterate tenant list and run per-tenant WHERE tenant_id = ${tid}) or add an explicit tenant filter parameter. Avoid unscoped reads/updates on tenant-scoped tables.
- **server/agentic-engines.ts:688** — autoApplyEligibleInsights executes an UPDATE on ai_insights where tenant scope is optional; when tenantId is not provided the UPDATE runs across all tenants (UPDATE ai_insights ... WHERE status = 'new' AND category IN (...) ${tenantScope}).
  - _Fix:_ Require a tenantId or perform an explicit per-tenant sweep. Always include AND tenant_id = ${tenantId} on updates to tenant-scoped tables to prevent accidental cross-tenant writes.
- **server/agentic-engines.ts:815** — runDecisionEngine queries personas with no tenant_id constraint (SELECT name, role FROM personas WHERE is_active = true LIMIT 14) and then uses those persona names in tenant-scoped processing — possible cross-tenant leakage.
  - _Fix:_ Restrict persona lookup to the tenant (e.g. WHERE tenant_id = ${tenantId} AND is_active = true) or otherwise verify persona ownership before using names in tenant-context prompts.
- **server/agentic-features.ts:861** — enrollInSequence checks for an existing active enrollment with SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' but omits tenant_id — row ownership is not verified before refusing/allowing an enrollment.
  - _Fix:_ Include tenant scoping in the check (AND tenant_id = ${params.tenantId}) so the existence test only considers enrollments belonging to the caller's tenant. Always check tenant_id when using foreign ids from callers.
- **server/agentic/department-budgets.ts:143** — Query scans a tenant-scoped table (department_budgets) without a tenant_id filter: `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'`. This violates the project's invariant that reads/writes against tenant-scoped tables must be constrained to a caller's tenant.
  - _Fix:_ If this is meant to be an administrative sweep, make the admin intent explicit and protect it (e.g. require an explicit admin-only path or environment guard). Otherwise restrict the query to a single tenant by adding a WHERE tenant_id = ${tenantId} (or equivalent Drizzle eq) and iterate per-tenant from a controlled caller. Example fixes: (a) add an explicit tenantId parameter and use WHERE tenant_id = ${tenantId}; or (b) if scanning across tenants is required, ensure the routine runs only with an audited/privileged identity and document that it is an admin-only operation (or use a dedicated admin tenant constant like ADMIN_TENANT_ID in the WHERE clause).
- **server/agentic/task-forces.ts:44** — personaIdArrayLiteral produces an array literal string which is inlined into SQL via `sql`${literal}::int[]`` using request-derived personaIds. This is effectively string-interpolated SQL for user input and risks injection / misuse and bypassing parameterization.
  - _Fix:_ Avoid building SQL array literals by hand. Use proper parameterized binding for arrays (pass personaIds as a parameter) and validate each persona id (Number.isInteger) and ownership (SELECT persona WHERE id IN (...) AND tenant_id = ...) before storing. If the DB driver requires a cast, bind the array as a parameter and cast safely rather than injecting a literal string.
- **server/commitment-drafter.ts:146** — SELECT from tenant-scoped table `commitments` without scoping to a specific tenant (SELECT DISTINCT tenant_id ... FROM commitments WHERE ...). This enumerates tenant rows from a tenant-scoped table without an explicit caller tenant filter.
  - _Fix:_ If this is intended only to enumerate tenants with candidates, prefer querying the global `tenants` table or otherwise ensure this background job is explicitly authorized. If you must scan `commitments`, make the behavior explicit and auditable (admin-only background job) or limit the query to an explicit tenant context. Consider replacing the free scan with a safe listing from `tenants` (e.g. SELECT id FROM tenants WHERE <criteria>) or add additional guardrails and logging to reduce risk of accidental cross-tenant exposure.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` (scanAndEscalate) without a tenant_id filter — the scanner fans-in across all tenants and selects rows from `commitments` without scoping to a caller tenant.
  - _Fix:_ If intentional for an admin-level escalator, document and ensure the routine is run only with proper privileges and that outputs are redacted (as comments indicate). Otherwise restrict the SELECT to a specific tenant or iterate tenants and run tenant-scoped queries per-tenant. Prefer selecting tenant IDs from the `tenants` table or explicitly add tenant_id = ${tenantId} when called in a per-tenant context.
- **server/dfy-intake.ts:52** — Querying dfy_intake_forms by token without constraining tenant_id. This SELECT reads a tenant-scoped table (dfy_intake_forms) but does not include tenant_id in the WHERE clause.
  - _Fix:_ Include tenant scoping in the lookup (e.g. accept/derive tenantId and add AND dfy_intake_forms.tenant_id = ${tenantId}), or otherwise validate the token maps to the expected tenant before returning the row. If the token is intentionally global, document that clearly and consider storing a namespaced token or an explicit tenant_id in the token payload to avoid cross-tenant access.
- **server/doc-collections.ts:166** — DELETE FROM doc_chunks is executed without a tenant_id constraint (DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}).
  - _Fix:_ Add tenant scoping to the DELETE, e.g. WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}. Even when collection ownership was previously checked, always include an explicit tenant_id clause on mutations against tenant-scoped tables.
- **server/doc-collections.ts:136** — listCollections uses subqueries against doc_chunks that do not include tenant_id (e.g. SELECT COUNT(*) FROM doc_chunks WHERE collection_id = c.id). These reads touch a tenant-scoped table without an explicit tenant_id filter.
  - _Fix:_ Constrain the subqueries by tenant_id (e.g. WHERE collection_id = c.id AND tenant_id = ${tenantId}) or rewrite the query to JOIN on doc_chunks with an explicit dc.tenant_id = ${tenantId} condition to enforce tenant isolation.
- **server/doc-collections.ts:457** — getCollectionStatus runs subqueries over doc_chunks without tenant_id (e.g. (SELECT COUNT(*) FROM doc_chunks WHERE collection_id = c.id)). These inner queries read a tenant-scoped table without an explicit tenant constraint.
  - _Fix:_ Add tenant scoping to the subqueries (e.g. WHERE collection_id = c.id AND tenant_id = ${tenantId}) or JOIN doc_chunks with an explicit tenant filter to ensure all counts/aggregates are limited to the caller's tenant.
- **server/glasses-gateway.ts:173** — UPDATE statement against tenant-scoped table 'api_keys' updates by id only (.where(eq(apiKeys.id, key.id))) without also constraining tenant_id. Relying solely on id (even after a prior select) risks missing the explicit tenant-bound WHERE required by policy.
  - _Fix:_ Include the tenant constraint in the WHERE clause when updating tenant-scoped rows, e.g. .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId))). This enforces the invariant that all writes are constrained to the caller's tenant.
- **server/heartbeat-schedules.ts:26** — Cross-tenant SELECT from tenant-scoped table `messages` without a tenant_id constraint (SELECT DISTINCT tenant_id FROM messages ...). Reads tenant-scoped rows with no tenant scoping.
  - _Fix:_ If this is intended to enumerate tenants, make the intent explicit and restrict to a trusted/admin context (e.g. require an ADMIN_TENANT_ID or dedicated scheduler role). Otherwise add an application-level tenant filter (WHERE tenant_id = ${tenantId}) or use a dedicated metadata table for tenant enumeration to avoid unscoped reads of tenant-scoped tables.
- **server/job-queue.ts:375** — getJobTenantForAdmin SELECTs tenant_id FROM agent_jobs WHERE id = ${id} without a tenant filter — it's a raw lookup returning tenant scope for a given job id and may be used by callers without authorization checks.
  - _Fix:_ Ensure this API is restricted to platform-admin usage only (documented and enforced in call-sites). Alternatively, accept an authenticated admin token in the caller chain or require a caller-provided tenantId and validate it against the stored tenant to avoid exposing cross-tenant data to tenant-scoped callers.
- **server/job-queue.ts:406** — listJobs issues a general SELECT FROM agent_jobs and can run without tenant_id in the WHERE (tenantId optional). When no tenantId is provided it returns jobs across all tenants.
  - _Fix:_ Require an explicit tenantId for tenant-scoped callers, or require an explicit ALL_TENANTS admin scope for cross-tenant listing. Enforce authorization at the route layer and, where possible, add DB-level WHERE tenant_id = ${tenantId} for tenant-facing consumers.
- **server/lib/restraint-trust.ts:88** — Dynamic SQL identifier via sql.raw(column) is used where the column name originates from a caller-supplied 'outcome' value (even though the code validates outcome against a map). Using sql.raw() with any runtime-derived value risks SQL injection and weakens the tenant-isolation/authorization invariants.
  - _Fix:_ Avoid embedding runtime-derived identifiers into raw SQL. Replace the sql.raw(column) pattern with an explicit, parameterized update for each allowed outcome. For example, switch on outcome and run one of a small set of fixed UPDATE statements that reference the column name literally (or use the query builder's typed .set(...) form). This keeps SQL identifiers static and fully parameterizes values (delta, tenantId, personaId, category). Example: if (outcome === 'action_ok') await db.execute(sql`UPDATE trust_scores SET action_alpha = action_alpha + ${delta}, updated_at = NOW() WHERE tenant_id = ${tenantId} AND persona_id = ${personaId} AND category = ${category}`); — implement similar branches for the other allowed outcomes.
- **server/lib/system-probes.ts:163** — SELECT count(*) FROM messages (messagesTable) is executed with no WHERE tenant_id = ... filter. This reads across tenant-scoped data without an explicit tenant constraint.
  - _Fix:_ If this probe is intended to be per-tenant, add a WHERE clause restricting tenant_id = ${tenantId} (or use the storage layer that accepts ALL_TENANTS explicitly). If the aggregate across all tenants is intentional, make the admin-only intent explicit (guard the call to admin-only context or document and centralize the all-tenant aggregation) so the lack of tenant filter is deliberate and controlled.
- **server/pace-control.ts:85** — SELECT COUNT(*) FROM heartbeat_logs WHERE created_at > ... AND ${laneFilter} does not include any tenant_id filter — reads activity across all tenants (heartbeat_logs appears to be tenant-scoped).
  - _Fix:_ Add tenant scoping to the query (e.g., WHERE tenant_id = ${tenantId} AND created_at > ...) or change the API to accept an explicit tenantId. If the intent is cross-tenant global metrics, ensure this runs only in a trusted admin context and avoid exposing results to unprivileged callers.
- **server/pace-control.ts:140** — getPaceSnapshot queries heartbeat_logs (SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ...) with no tenant_id constraint — returns global activity instead of per-tenant data.
  - _Fix:_ Scope snapshot queries by tenant_id when called in tenant context. If dashboard intends global view, separate admin-only endpoints must be guarded. Prefer explicit tenant parameter to ensure application-level isolation.
- **server/persona-routing-outcomes.ts:44** — Query joins action_outcomes (filtered by ao.tenant_id = ${tenantId}) to personas p without constraining p.tenant_id (FROM action_outcomes ao JOIN personas p ON p.id = ao.persona_id ...). This reads from the personas table without an explicit tenant_id filter.
  - _Fix:_ Ensure joins to tenant-scoped tables include explicit tenant filters on each table (e.g., JOIN personas p ON p.id = ao.persona_id AND p.tenant_id = ${tenantId}) or otherwise verify ownership before selecting persona fields. Avoid relying solely on ao.tenant_id to implicitly guarantee persona ownership.
- **server/plan-executor.ts:1619** — backfillHistoricalRecoveryBlockedPlansCore SELECT includes a conditional that allows tenantId to be null and thus selects rows across all tenants (AND (${tenantId ?? null}::integer IS NULL OR tenant_id=${tenantId ?? null})). This results in unscoped reads when tenantId is omitted.
  - _Fix:_ Avoid the "IS NULL OR" pattern that implicitly allows full-table cross-tenant scans. Require callers to pass an explicit tenantId, or if global scans are needed, run them under a clearly authenticated admin context and explicitly pass ADMIN_TENANT_ID. Prefer explicit per-tenant iteration instead of implicit unscoped selection.
- **server/routes.ts:486** — Unscoped DELETE on tenant-scoped table `order_lookup_codes`: DELETE FROM order_lookup_codes WHERE expires_at < NOW() executes across all tenants.
  - _Fix:_ Limit the prune to a specific tenant or iterate tenants and prune each tenant's expired codes separately (DELETE ... WHERE tenant_id = <tenant> AND expires_at < NOW()). If this is an intentional global admin maintenance task, ensure it runs under a clearly-authorized admin context and document the reason for cross-tenant mutation.
- **server/routes.ts:627** — SELECT over tenant-scoped table `projects` (JOIN tenants) without tenant scoping: SELECT p.id, p.name, p.tenant_id, t.name as tenant_name FROM projects p JOIN tenants t ON t.id = p.tenant_id WHERE p.drive_folder_id IS NULL OR p.drive_folder_id = '' — this iterates across all tenants.
  - _Fix:_ If this backfill must act across tenants, run it under an explicit, documented admin context and/or iterate tenants and perform tenant-scoped queries (WHERE p.tenant_id = <tenant>). Prefer per-tenant operations to avoid accidental cross-tenant actions; include authorization checks for any code that mutates or creates tenant resources.
- **server/routes/admin.ts:973** — SQL text interpolates a constructed `where` sql fragment that contains request-derived values (req.query.source / req.query.status). Using interpolated sql with request-derived data risks injection and makes tenant-isolation harder to audit.
  - _Fix:_ Build the WHERE clause with Drizzle primitives (and/eq/sql fragments via the library) and pass parameters safely, or at minimum validate+canonicalize request inputs and avoid embedding a pre-built sql fragment into another template. Ensure tenant_id is enforced explicitly by those primitives.
- **server/routes/admin-extra.ts:125** — Raw/interpolated sql`...` uses request-derived values (tenantId and limit from the request) inside a template literal (decline-events). This pattern mixes request data into raw SQL text.
  - _Fix:_ Use Drizzle's query builder (db.select().from(...).where(eq(table.tenantId, tenantId)).limit(limit)) or otherwise pass parameters via the query builder instead of embedding request-derived values into sql`` templates.
- **server/routes/admin-extra.ts:153** — Raw/interpolated sql`...` uses request-derived values (tenantId, status, limit) when listing self_initiatives, embedding request-controlled values in template SQL.
  - _Fix:_ Use the query builder with explicit where conditions (eq(self_initiatives.tenantId, tenantId), eq(self_initiatives.status, status)) and .limit(limit) to avoid string/interpolated SQL with request inputs.
- **server/routes/admin-extra.ts:184** — UPDATE via sql`` interpolates request-derived id and tenantId. While the WHERE includes tenant_id, embedding params via raw/interpolated SQL increases risk and makes auditing isolation harder.
  - _Fix:_ Use Drizzle's update API (db.update(self_initiatives).set({...}).where(and(eq(self_initiatives.id, id), eq(self_initiatives.tenantId, tenantId))).returning(...)) or otherwise parameterize via the query builder to avoid raw/interpolated SQL with request values.
- **server/routes/admin-extra.ts:216** — Multiple database COUNT queries in account deletion-summary embed tenantId (and subqueries) using sql`` with a request-derived tenantId. This uses string-interpolated SQL with request data.
  - _Fix:_ Use the Drizzle query builder or parameterized queries (db.select(...).from(...).where(eq(table.tenantId, tenantId))) for each count and subquery to keep request-derived values out of raw SQL templates and to make tenant scoping explicit.
- **server/routes/admin-hvac-missed-call.ts:30** — Raw sql uses tenantId (from request/session) in an interpolated sql`` template when selecting workspaces. Although tenant_id is included, this is string-interpolated SQL with request-derived data.
  - _Fix:_ Prefer Drizzle query builder (db.select().from(hvac_missed_call_workspaces).where(eq(table.tenantId, tenantId)).orderBy(...).limit(...)) to parameterize values and make tenant scoping explicit; avoid embedding request-derived values directly in sql``.
- **server/routes/admin-hvac-missed-call.ts:43** — INSERT INTO hvac_missed_call_workspaces is performed via an interpolated sql`` template using multiple request-derived values (idempotencyKey, prospectName, companyName, JSON bodies). Even though tenant_id is provided, embedding request data directly in raw SQL increases injection/isolation risk.
  - _Fix:_ Use Drizzle's insert API (db.insert(table).values({...})) or parameterized queries to insert rows and to pass JSON as parameters rather than interpolating stringified JSON into a sql`` template.
- **server/routes/admin-zombie-detector.ts:36** — Transaction block interpolates windowDays (derived from req.query) directly into sql`` fragments used by several queries. Embedding request-controlled numeric values into raw SQL risks injection and can complicate tenant-isolation auditing.
  - _Fix:_ Pass windowDays as a parameter in the query builder or use eq/literal constructors provided by Drizzle (or validate/parse and use parameter placeholders). Wrap SET LOCAL usage as currently done, but build the inner queries with parameterized values (or Drizzle builders) instead of inline interpolation.
- **server/routes/agent-insights.ts:58** — SQL interpolates windowDays (from req.query) into an interval expression via string-interpolated sql`` (e.g. AND started_at > NOW() - (${windowDays} || ' days')::interval). This uses a request-derived value directly in raw SQL.
  - _Fix:_ Use parameterized queries or the query builder to pass windowDays as a parameter (or compute a timestamp in JS and pass that as a parameter) rather than interpolating into SQL text. This avoids injection problems and makes tenant scoping clearer.
- **server/routes/conversations.ts:237** — SELECT from tenant-scoped table 'conversations' (alias c) without an explicit tenant filter: the query selects c.project_id WHERE c.id = ${id} but does not include c.tenant_id = ${tenantId} (it only constrains p.tenant_id).
  - _Fix:_ Add an explicit tenant filter for the conversations row (e.g. WHERE c.id = ${id} AND c.tenant_id = ${conv.tenantId ?? tenantId} ...) to ensure the SELECT cannot return rows from other tenants, or fetch the conversation via a storage API that enforces tenant ownership.
- **server/routes/goal-ledger.ts:92** — SELECT from tenant-scoped table 'agent_jobs' without tenant filtering: the query returns active jobs across tenants (no WHERE j.tenant_id = ...).
  - _Fix:_ If cross-tenant visibility is intended, make that explicit and ensure the route is tightly admin-gated and audited. Otherwise constrain the query by tenant (e.g. WHERE j.tenant_id = ${tenantId}) or accept a tenant parameter and validate admin privileges before returning cross-tenant data.
- **server/seed.ts:3889** — INSERT INTO project_notes is executed without setting tenant_id. Many note tables are tenant-scoped; inserting without tenant_id can violate the tenancy invariant (and may fail if tenant_id is NOT NULL with no default).
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (..., ${tenantId})), or query the project's tenant and set it explicitly. If project_notes is intentionally global, document that explicitly; otherwise ensure tenant_id is always provided.
- **server/skillify.ts:221** — Reading personas table without tenant scoping — the code queries personas by id (WHERE personas.id = conv[0].personaId) with no tenant_id constraint. If 'personas' is tenant-scoped, this allows reading another tenant's persona record.
  - _Fix:_ Add a tenant constraint when querying personas (e.g. .where(and(eq(personas.id, conv[0].personaId), eq(personas.tenantId, conv[0].tenantId)))) or otherwise verify that the persona row belongs to the same tenant as the conversation before returning persona data.
- **server/storage.ts:289** — attachCitationMetadata queries messages for citations using WHERE id IN (...) but does not include a tenant_id constraint. messages is tenant-scoped; the query relies on ids supplied by caller rather than explicitly constraining by tenant_id.
  - _Fix:_ Add an explicit tenant predicate to the query (e.g. AND tenant_id = ${tenantId}) or fetch citations using a join that includes the tenant filter. Ensure the function receives/validates the tenantId of the messages it is operating on before running the SELECT.
- **server/storage.ts:979** — archiveExpiredMemories runs an UPDATE on memory_entries to archive rows without any tenant filter. memory_entries is tenant-scoped, so this mutates rows across all tenants without explicit intent-checking.
  - _Fix:_ If this operation must run per-tenant, iterate per-tenant and include eq(memory_entries.tenant_id, tenantId). If it must be global/admin, require an explicit admin mode/ALL_TENANTS sentinel and document the privileged nature of this call. Otherwise add a tenant constraint to the UPDATE.
- **server/storage.ts:995** — archiveStaleMemories updates tenant-scoped memory_entries (sets status = 'archived') without including a tenant_id constraint, causing cross-tenant updates.
  - _Fix:_ Run this per-tenant with an explicit tenant parameter and include eq(memory_entries.tenant_id, tenantId), or require an explicit admin/ALL_TENANTS sentinel to make global intent explicit and gated.
- **server/storage.ts:959** — getMemoriesWithoutEmbeddings selects from memory_entries (tenant-scoped) without any tenant_id constraint — returning rows across tenants.
  - _Fix:_ If this is a global background job, require/validate an explicit admin/ALL_TENANTS sentinel. If it should be tenant-scoped, add a tenant parameter and include requiredTenantScope(memory_entries.tenant_id, tenantId) (or eq) to the WHERE clause.
- **server/storage.ts:969** — getKnowledgeWithoutEmbeddings selects agent_knowledge rows without a tenant predicate. agent_knowledge is tenant-scoped, so this returns rows across tenants.
  - _Fix:_ Either require an explicit ALL_TENANTS sentinel for admin/global processing or add a tenant parameter and scope the query via requiredTenantScope(agent_knowledge.tenant_id, tenantId) (or eq(agent_knowledge.tenantId, tenantId)).
- **server/video-job-runner.ts:275** — SELECT from tenant-scoped table `videoJobs` (listStaleVideoJobs) has no tenant_id constraint. The function lists stale job rows across all tenants (SELECT ... FROM videoJobs WHERE <conditions> ...), which reads tenant data without scoping to a caller tenant.
  - _Fix:_ If this cross-tenant scan is intentional (background recovery service), document and protect it (require an operator/admin context). Otherwise scope the query to a specific tenant: accept a tenantId parameter and add eq(videoJobs.tenantId, tenantId) to the WHERE clause so only the caller's tenant rows are read. If the background runner must operate across tenants, ensure it runs with a tightly controlled service account and that access is auditable.

### LOW (1)

- **server/persona-export.ts:39** — SELECT name, description, enabled FROM skills WHERE persona_id = ${personaId} OR persona_id IS NULL is not filtered by tenant_id. If skills is a tenant-scoped table, this will return rows across tenants.
  - _Fix:_ If skills is tenant-scoped, add tenant_id filtering (e.g., AND (tenant_id = ${tenantId} OR tenant_id IS NULL) or include tenant_id in the schema lookup). Otherwise document that skills is intentionally global and safe to read without tenant scoping.
