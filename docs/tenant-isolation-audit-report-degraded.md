# Tenant-Isolation Audit Report

_Generated 2026-09-16T20:47:10.816Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 490 file(s) were NOT audited this run (chunk cap in effect). Findings below are PARTIAL — do not read "few findings" as "all clear."

- Coverage: **191/681** file(s) audited · chunks 20/63 OK (0 failed, 43 pending)
- Findings: **90** (CRITICAL 21 · HIGH 44 · MEDIUM 25 · LOW 0)
- Severe dispositions: **65 kept** · 0 precision false-positive · 0 allowlist false-positive · 0 deferred real risk

### CRITICAL (21)

- **server/agentic/resume-worker.ts:65** — Cross-tenant SELECT on tenant-scoped agent_runs/agent_approvals without constraining to a specific tenant. The candidate query (db.execute(sql`SELECT ... FROM agent_runs r LEFT JOIN agent_approvals a ...`) returns rows across all tenants because there is no WHERE r.tenant_id = <tenant> filter.
  - _Fix:_ Restrict the query to a single tenant (or iterate per-tenant explicitly). Add an application-level tenant filter, e.g. WHERE r.tenant_id = ${tenantId} (or otherwise supply the caller's tenantId) so every read from agent_runs / agent_approvals is limited to that tenant. If this worker intentionally scans all tenants, make that explicit and ensure it runs under a service/admin context with audited access; otherwise narrow the query to the caller tenant.
- **server/auth.ts:1327** — Broad DELETE from auth_sessions without tenant filter in clearAllSessionsAsPlatformAdmin: `DELETE FROM auth_sessions`. This mutates every tenant's sessions in a single statement.
  - _Fix:_ This is an intentionally cross-tenant admin action — ensure the function is only callable after a strict platform-admin check (the code already requires an authority object). Make the platform-admin guard explicit in code/comments, log/analyze the action, and consider replacing the blanket DELETE with an explicit per-tenant loop or an audited multi-tenant operation to make the blast-radius and authorization obvious. If cross-tenant mutation is not intended, add `WHERE tenant_id = ...`.
- **server/chat-engine/project-context.ts:25** — Query reads from `project_conversations` (pc) without constraining pc.tenant_id to the caller's tenant. The JOIN to projects (p) checks p.tenant_id but the project_conversations row itself is not tenant-scoped in this WHERE, potentially allowing cross-tenant linkage or IDOR-style access.
  - _Fix:_ Add an explicit tenant filter for project_conversations (e.g. AND pc.tenant_id = ${tenantId}) or use Drizzle where(eq(project_conversations.tenantId, tenantId)). Verify ownership of referenced rows before trusting them.
- **server/chat-engine/project-context.ts:45** — EXISTS subquery on `project_conversations` (pc) uses pc.conversation_id and pc.project_id but does not include a tenant_id constraint, allowing a row from another tenant to satisfy the condition.
  - _Fix:_ Include tenant filtering inside the EXISTS subquery (e.g. WHERE pc.conversation_id = ${conversationId} AND pc.project_id = p.id AND pc.tenant_id = ${tenantId}) or use Drizzle helpers to enforce eq(project_conversations.tenantId, tenantId). Always scope subqueries to the caller tenant.
- **server/chat-engine/project-context.ts:181** — Query joins `project_conversations` (pc) to `conversations` but filters only on conversations.tenant_id = tenantId and pc.project_id = ${projectId}; there is no explicit tenant constraint on pc, so project_conversations rows from other tenants could be used to access conversation ids.
  - _Fix:_ Add tenant_id checks for project_conversations (e.g. AND pc.tenant_id = ${tenantId}) or use where(eq(project_conversations.tenantId, tenantId)). Ensure every table referenced that is tenant-scoped is explicitly constrained by the caller's tenant_id.
- **server/coinbase-commerce.ts:73** — SELECT from tenants returns tenant-scoped webhook secrets without any tenant_id constraint (SELECT coinbase_commerce_webhook_secret FROM tenants ...). This enumerates other tenants' webhook secrets in a request handler.
  - _Fix:_ Do NOT scan the tenants table unscoped in a request path. Restrict access to tenant secrets: store webhook-secret → tenant mapping in a dedicated admin-only table or only query secrets for a specific tenant, or perform this enumeration only in a privileged offline/admin job. At minimum, ensure the code path is admin-authorized or move secrets into a table whose rows are guarded and queried with tenant_id or an explicit server-admin check. Avoid returning or decrypting other tenants' secrets in normal request handlers.
- **server/embeddings.ts:845** — backfillEmbeddingVecs queries and updates tenant-scoped tables without constraining by tenant_id (SELECT from memory_entries at line ~845, UPDATE memory_entries at line ~855). This reads and writes memory rows across all tenants in a single operation.
  - _Fix:_ Limit backfill to a single tenant per run or include tenant_id in WHERE clauses. For example, iterate tenants and run: SELECT ... FROM memory_entries WHERE tenant_id = ${tenantId} AND ...; UPDATE memory_entries SET ... WHERE id = ${row.id} AND tenant_id = ${tenantId}. Avoid global scans/updates that touch other tenants' rows.
- **server/embeddings.ts:861** — backfillEmbeddingVecs/backfillMissingKnowledgeEmbeddings operate on agent_knowledge without tenant scoping (SELECT agent_knowledge at line ~861 and UPDATE agent_knowledge at line ~872). This allows reading/updating knowledge for all tenants.
  - _Fix:_ Add tenant scoping to the queries or process per-tenant. Example: SELECT id, embedding FROM agent_knowledge WHERE tenant_id = ${tenantId} AND embedding IS NOT NULL ...; UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} AND tenant_id = ${tenantId}.
- **server/embeddings.ts:809** — storeEmbeddingVec has a fallback branch that performs an UPDATE without any tenant_id constraint (UPDATE ${sql.raw(table)} ... WHERE id = ${id}). When called without an explicit tenantId this will update a tenant-scoped table row globally by id.
  - _Fix:_ Require and validate tenantId for all writes to tenant-scoped tables. Never perform an UPDATE without AND tenant_id = ${tenantId}. If a tenantId is intentionally optional, document and restrict it to admin-only paths with explicit safeguards.
- **server/felix-brain.ts:23** — SELECT from tenant-scoped table `agent_runs` is executed without any tenant_id constraint. The rehydrateTaskStateOnBoot query (SELECT id, tenant_id, ... FROM agent_runs WHERE status IN ('running','pending') AND created_at > NOW() - INTERVAL '7 days') pulls rows for all tenants and writes them into a process-global in-memory store, causing cross-tenant data exposure/isolation failure.
  - _Fix:_ Restrict the query to a single tenant or otherwise enforce tenant scoping. Options: require a tenantId parameter and add WHERE tenant_id = ${tenantId}; or iterate per-tenant in a controlled admin-only loop; or limit the query to the tenant(s) this process is serving. Ensure only rows with tenant_id matching the caller's tenant are loaded into taskStateStore and never read or loaded for other tenants.
- **server/google-drive.ts:1664** — INSERT into project_files is executed without providing tenant_id. Per the invariant, tenant_id must be explicitly set on inserts to tenant-scoped tables (columns are NOT NULL with no default).
  - _Fix:_ Include tenant_id in the INSERT and bind it from the validated tenant context (e.g. INSERT INTO project_files (tenant_id, project_id, ...) VALUES (${resolvedTenantId}, ${params.projectId}, ...)). Ensure ownership was verified (assertProjectInTenant) before inserting and use parameterized Drizzle insert APIs rather than raw SQL string interpolation.
- **server/heartbeat-task-scheduler-storage.ts:44** — UPDATE on tenant-scoped table `heartbeat_tasks` without tenant constraint: `UPDATE heartbeat_tasks SET last_run_at = ${now}, next_run_at = ${nextRun} WHERE id = ${id} ...` can modify any tenant's task row because tenant_id is not checked.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. AND tenant_id = ${expectedTenantId}) or require the caller to pass a validated tenantId and verify it before performing the UPDATE. Alternatively, return tenant_id from a prior SELECT and verify ownership before updating. Never perform UPDATEs on tenant-scoped tables by id alone in cross-tenant contexts.
- **server/heartbeat.ts:2676** — UPDATE on tenant-scoped table `heartbeat_tasks` without tenant constraint: `UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id}` modifies approval_status by id only, with no tenant_id check.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or use a tenant-aware storage API to change approval_status so the update cannot affect rows owned by other tenants. Validate the task's tenant before performing the UPDATE.
- **server/heartbeat.ts:3146** — UPDATE on tenant-scoped table `heartbeat_tasks` without tenant constraint: `UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id}` (in delegateTaskFromChat) lacks tenant scoping and can affect other tenants' tasks.
  - _Fix:_ Constrain the UPDATE by tenant (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use the storage layer that enforces tenant ownership. Always verify tenant ownership before mutating tenant-scoped rows.
- **server/job-queue.ts:160** — claimDueJobs performs an UPDATE on agent_jobs without any tenant_id constraint (claims/runs jobs across all tenants).
  - _Fix:_ Scope claims to an explicit tenant (e.g. pass a tenantId or run per-tenant workers) or include AND tenant_id = ${tenantId} in the WHERE/subselect. Ensure worker tenancy is enforced before mutating agent_jobs.
- **server/job-queue.ts:195** — reclaimExpiredLeases Step 1 updates agent_jobs to failed_terminal for expired leases without a tenant_id restriction, mutating rows across tenants.
  - _Fix:_ Apply tenant scoping to the reclamation update (or run the reclaim logic per-tenant under an admin-approved bounded loop). Require an explicit tenant parameter or run per-tenant transactions to avoid cross-tenant writes.
- **server/job-queue.ts:209** — reclaimExpiredLeases Step 2 re-queues expired running jobs (UPDATE → pending) without any tenant_id filter, affecting jobs from all tenants.
  - _Fix:_ Restrict the UPDATE to a tenant scope or iterate tenants and run the reclaim per-tenant. Never run unscoped mutations on tenant-scoped tables.
- **server/job-queue.ts:238** — completeJob updates agent_jobs to 'succeeded' by id/attempts without verifying tenant_id, allowing completion of jobs across tenants if called with an id.
  - _Fix:_ Require and validate tenant_id when completing jobs (e.g. UPDATE ... WHERE id = ${id} AND tenant_id = ${tenantId} AND status = 'running' AND attempts = ${attempts}). Enforce lease-fencing plus explicit tenant scoping.
- **server/job-queue.ts:298** — failJob may UPDATE agent_jobs to 'failed_terminal' without any tenant_id constraint, mutating other tenants' job rows by id.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (AND tenant_id = ${tenantId}) or otherwise verify the job belongs to the tenant derived from a trusted source before writing terminal failure state.
- **server/job-queue.ts:318** — failJob's retry path updates agent_jobs (setting next_run_at / clearing lease) without tenant_id restriction, modifying rows across tenants.
  - _Fix:_ Require tenant scoping for this UPDATE (AND tenant_id = ${tenantId}) and validate the job's tenant before re-queuing. Consider passing tenantId through the worker call flow to enforce it.
- **server/job-queue.ts:335** — cancelJob updates agent_jobs status to 'cancelled' by id without any tenant_id constraint, permitting cross-tenant cancellations.
  - _Fix:_ Require tenant_id for cancel operations (UPDATE ... WHERE id = ${id} AND tenant_id = ${tenantId} ...) or restrict this API to admin flows that first resolve the job's tenant and perform an authorized, audited action.

### HIGH (44)

- **server/ab-optimizer.ts:110** — SELECT across experiments omits tenant scoping — the heartbeat selects running A/B experiments for ALL tenants (no WHERE tenant_id = ...), exposing metadata and allowing cross-tenant processing.
  - _Fix:_ Restrict the query to the caller's tenant (or iterate tenants explicitly). Add a tenant filter, e.g. WHERE category = 'ab_test' AND status = 'running' AND tenant_id = ${tenantId}, or run the sweep per-tenant after enumerating tenants safely.
- **server/agent-activity.ts:122** — UPDATE agent_activity uses only the activity id in WHERE clause (eq(agentActivity.id, activityId)) with no tenant constraint — this can update rows across tenants if an id is guessed or re-used.
  - _Fix:_ Include tenant scoping in the WHERE clause for updates (e.g. .where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId)))) or otherwise validate the activity belongs to the caller's tenant before applying updates.
- **server/agentic-features.ts:861** — SELECT on outreach_enrollments used to check for an existing enrollment omits tenant scoping (WHERE sequence_id = ... AND contact_email = ...). This can detect or act on enrollments belonging to other tenants.
  - _Fix:_ Add tenant constraint to the query (e.g. WHERE tenant_id = ${params.tenantId} AND sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active') to ensure enrollments are checked only within the caller's tenant.
- **server/agentic-features.ts:1020** — UPDATE outreach_enrollments sets reply_classification etc. but the WHERE clause uses only id = ${row.id} (no tenant_id). That permits updating an enrollment row by id without confirming tenant ownership.
  - _Fix:_ Constrain the UPDATE by tenant as well (e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}) or re-derive the enrollment using a tenant-scoped SELECT and then use a tenant-aware WHERE for the update to prevent cross-tenant modifications.
- **server/agentic/wake-scheduler.ts:142** — Global UPDATE on tenant-scoped table agent_wake_schedules lacks a tenant_id constraint. The statement updates rows across all tenants: `UPDATE agent_wake_schedules SET ... WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ... )`
  - _Fix:_ Restrict the UPDATE to a specific tenant (e.g. pass tenantId into the function and include `AND tenant_id = ${tenantId}`), or run the claim loop per-tenant. If global sweep is intended, ensure this code is only runnable under a platform-admin/maintenance context and add an explicit, auditable guard (and comments) so the cross-tenant mutation is deliberate and reviewed.
- **server/auth.ts:1131** — DELETE from tenant-scoped table password_reset_tokens omits tenant_id in WHERE: `DELETE FROM password_reset_tokens WHERE token = ${tokenHash}`. This allows deleting rows without an explicit tenant_id constraint.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. `WHERE token = ${tokenHash} AND tenant_id = ${tenantId}`) or otherwise derive and validate tenantId from the token before performing the DELETE. Prefer using the same embedded/validated tenant id used by getResetToken.
- **server/auto-asset-capture.ts:184** — SELECT over conversations enumerates rows across all tenants without tenant scoping: `SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND role = 'assistant') > 0`. This reads tenant-scoped data globally.
  - _Fix:_ Restrict the backfill to a single tenant at a time (accept tenantId parameter and add `WHERE c.tenant_id = ${tenantId}`) or run the backfill as a controlled maintenance job with explicit platform-admin authorization and auditing. Ensure per-tenant ownership is validated before any write operations.
- **server/auto-transcript.ts:165** — UPDATE against tenant-scoped table `conversations` is missing a tenant constraint: `UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL`. Without `tenant_id` in the WHERE clause this can modify rows outside the caller's tenant context.
  - _Fix:_ Add an explicit tenant constraint to the UPDATE, for example: `UPDATE conversations SET project_id = ${c.project_id} WHERE id = ${c.conversation_id} AND project_id IS NULL AND tenant_id = ${c.tenant_id}` (or otherwise ensure the update is executed in a transaction that enforces the owning tenant).
- **server/channels/kernel.ts:175** — dispatchInbound calls the adapter-supplied resolveConversation(turn) to get a conversationId and then calls processMessage(conversationId, ...) without verifying the returned conversation actually belongs to turn.tenantId. This allows an adapter to supply a conversationId for a different tenant and have the kernel process messages under the caller's tenant context.
  - _Fix:_ Verify conversation ownership before use. Either require resolveConversation to return an object that includes the conversation's tenant_id and validate it equals turn.tenantId, or perform a DB lookup here to fetch the conversation row and assert tenant_id = turn.tenantId (or reject). Ensure every downstream DB read/write that uses conversationId is guarded by a tenant_id check (or include tenant_id in channel-scoped rows and use it in WHERE clauses).
- **server/channel-routing.ts:31** — listChannelRoutes SELECT joins the personas table (p) but does not constrain the personas rows by tenant. If the personas table is tenant-scoped, this query reads persona rows without a tenant_id filter, violating tenant isolation.
  - _Fix:_ If personas are tenant-scoped, include a tenant constraint in the query (e.g. WHERE p.tenant_id = <currentTenant>) or denormalize persona metadata into a tenant-aware channel_routes table (add tenant_id column on channel_routes and use it in joins). Make tenant ownership explicit and always parameterize the tenant_id in the SQL WHERE clause.
- **server/channel-routing.ts:53** — setChannelRoute INSERT/UPSERT writes persona_id into channel_routes without verifying the supplied personaId belongs to the caller's tenant (and channel_routes has no tenant_id). This lets a caller set routes referencing personas from other tenants.
  - _Fix:_ Validate ownership of personaId before inserting/updating channel_routes. Prefer adding tenant_id to channel_routes and enforce it in the INSERT/UPSERT (and in subsequent queries). At minimum, query the personas row and assert personas.tenant_id === callerTenantId before persisting persona_id into channel_routes.
- **server/chat-engine.ts:1621** — buildMemorySection declares tenantId with a default of 1 (tenantId: number = 1). If this helper is ever invoked without an explicit caller tenant, it will operate under tenant 1 and may cause cross-tenant data access (reads/behaviour that rely on tenant context), violating the tenant-isolation invariant.
  - _Fix:_ Remove the implicit default tenant (do not default to tenant 1). Make tenantId a required parameter (or accept null and fail-closed). Ensure every call site passes the authenticated caller's tenantId. Audit other helper functions for similar implicit-admin defaults and enforce fail-closed behaviour so no tenant-scoped read/write can run without an explicit validated tenant context.
- **server/chat-engine.ts:3800** — capability-review.markReviewReused is invoked with conversationId (a foreign id) but no tenant context. The called function may read/write telemetry or DB rows keyed by conversationId without verifying the caller's tenant, risking cross-tenant writes/reads.
  - _Fix:_ Pass the tenantId into markReviewReused (or have the function accept/require tenantId). Inside the function ensure any SELECT/UPDATE/DELETE uses WHERE tenant_id = <tenantId> (or Drizzle eq(table.tenantId, tenantId)), and/or explicitly verify the conversation belongs to the tenant before any reads/writes.
- **server/chat-engine.ts:3919** — stuck-diagnostics.recordErrorForRetryStormDetection is called with conversationId and error text but without tenant information. If this function persists or reads rows keyed by conversationId, it may operate across tenants without verifying ownership.
  - _Fix:_ Change calls to pass tenantId and update recordErrorForRetryStormDetection to enforce tenant scoping on all DB operations (WHERE tenant_id = <tenantId>). Alternatively, have the function first validate that conversationId belongs to the provided tenant before performing any reads/writes.
- **server/chat-engine.ts:4142** — stuck-diagnostics.recordToolCallForStuckDetection is invoked with conversationId, toolName, parsedArgs and round but without tenant context. This likely records per-conversation patterns and could read/write tenant-scoped tables without tenant filters.
  - _Fix:_ Include tenantId when calling recordToolCallForStuckDetection, and update the implementation to require tenantId and apply tenant-scoped WHERE filters on any DB access. If the function must accept untrusted conversation ids, it should first verify ownership (e.g., SELECT conversation WHERE id = conversationId AND tenant_id = tenantId) before proceeding.
- **server/chat-engine/project-context.ts:159** — SELECT from tenant-scoped table `project_files` lacks an explicit tenant_id constraint. The code relies on prior project ownership checks instead of enforcing tenant_id in this query.
  - _Fix:_ Add an explicit tenant filter to the query (e.g. AND tenant_id = ${tenantId}) or use Drizzle where(eq(project_files.tenantId, tenantId)). Do not rely solely on a previously-validated projectId; every tenant-scoped table access must include the caller's tenant_id in SQL.
- **server/chat-engine/project-context.ts:171** — SELECT from tenant-scoped table `project_notes` omits tenant_id in the WHERE clause (reads by project_id only).
  - _Fix:_ Constrain the query by tenant (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or use Drizzle where(eq(project_notes.tenantId, tenantId)). Ensure every read/write against tenant-scoped tables includes tenant_id.
- **server/commitment-drafter.ts:234** — UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id} executes without constraining tenant_id. This can affect the same commitment id across other tenants (cross-tenant mutation).
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. ... WHERE id = ${row.id} AND tenant_id = ${tenantId}) so the revert/unclaim only affects the intended tenant. All mutations on tenant-scoped tables must carry the caller's tenant_id filter.
- **server/data-protection.ts:149** — SELECT from conversations to enumerate expired soft-deleted rows does not restrict to a tenant (no tenant_id predicate). This reads rows across all tenants.
  - _Fix:_ If this is intended to be a tenant-scoped purge, add a tenant filter (e.g. WHERE deleted_at < ${cutoff} AND tenant_id = ${tenantId}) or iterate per-tenant. If it must scan all tenants as an admin job, run it under a well-audited admin context and minimize exposure (log intent explicitly) — but prefer per-tenant scoped queries to satisfy the app-level isolation invariant.
- **server/data-protection.ts:359** — SELECT from project_notes filters only by project_id (WHERE project_id = ${proj.id}) but does not include tenant_id. project_notes is tenant-scoped and must be constrained to the caller's tenant.
  - _Fix:_ Add an explicit tenant predicate (e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}) or join through projects with p.tenant_id = ${tenantId} to ensure the notes returned are only for the intended tenant.
- **server/data-protection.ts:370** — SELECT from project_files filters only by project_id (WHERE project_id = ${proj.id}) and lacks tenant_id constraint on a tenant-scoped table.
  - _Fix:_ Constrain the query by tenant (e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}), or join via the projects table and require p.tenant_id = ${tenantId} to enforce tenant isolation at query time.
- **server/dfy-intake.ts:50** — SELECT from tenant-scoped table dfy_intake_forms without tenant_id constraint (getDfyIntakeByToken). This returns a tenant row by token alone and does not enforce tenant scoping.
  - _Fix:_ Include tenant scoping in the lookup (e.g. accept/require a tenantId and add WHERE tenant_id = ${tenantId}) or otherwise ensure the token lookup is validated against an explicit tenant before returning the row. If the token is intentionally global, document the security model and ensure tokens are collision-resistant and access-checked.
- **server/doc-collections.ts:165** — DELETE FROM doc_chunks omits tenant_id in WHERE clause (addDocument). This DELETE targets doc_chunks by doc_path and collection_id but does not include AND tenant_id = ${tenantId}.
  - _Fix:_ Constrain the DELETE with tenant_id (AND tenant_id = ${tenantId}) to guarantee tenant isolation. Alternatively bind the delete to the validated collection row (using collection_id AND tenant_id) before removing chunks.
- **server/doc-collections.ts:294** — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} lacks tenant constraint (generateCollectionEmbeddings). Updating by id alone can affect rows across tenants if misused.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or perform the update using the same tenant-scoped selector used to fetch the rows to ensure the row belongs to the expected tenant.
- **server/email.ts:370** — startInboxPolling checks for existing inbox_messages by doing SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 with no tenant filter. inbox_messages is tenant-scoped elsewhere; this read can cross tenant boundaries and may cause incorrect dedup decisions or leaks.
  - _Fix:_ Include an appropriate tenant_id constraint in the existence check (e.g. WHERE message_id = ${msgId} AND tenant_id = ${tenantId}) or otherwise resolve tenant context before querying. If tenant is unknown at that point, explicitly derive and validate it first, and fail closed if unresolved.
- **server/email.ts:415** — backfillEmptyBodies runs SELECT id, message_id FROM inbox_messages ... without restricting by tenant_id. Reading tenant-scoped inbox messages without a tenant constraint can leak or process other tenants' messages.
  - _Fix:_ Run backfill per-tenant or include tenant_id = ${tenantId} in the WHERE clause. Ensure any maintenance/backfill job that touches tenant-scoped tables either operates under a specific tenant context or is explicitly audited and secured.
- **server/firecrawl.ts:239** — UPDATE on tenant-scoped table 'scraped_pages' uses only the row id in WHERE (eq(scrapedPages.id, existing[0].id)) and omits an explicit tenant_id constraint.
  - _Fix:_ Include the caller's tenant in the UPDATE WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) so the write is always constrained to the caller's tenant.
- **server/firecrawl.ts:372** — UPDATE on tenant-scoped table 'scraped_pages' uses only the row id in WHERE (eq(scrapedPages.id, existing[0].id)) and omits an explicit tenant_id constraint.
  - _Fix:_ Include the caller's tenant in the UPDATE WHERE clause (e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId)))) so the write is always constrained to the caller's tenant.
- **server/glasses-gateway.ts:157** — SELECT from apiKeys table is performed without constraining by tenant (where clause checks keyHash and isRevoked only). If apiKeys is tenant-scoped this allows cross-tenant reads.
  - _Fix:_ Either treat apiKeys as a documented global table or add tenant scoping to the lookup. Prefer adding an explicit tenant filter when possible (e.g. .where(and(eq(apiKeys.keyHash, keyHash), eq(apiKeys.isRevoked, false), eq(apiKeys.tenantId, expectedTenant)))) or otherwise ensure the design intentionally keeps apiKeys non-tenant-scoped and is reviewed.
- **server/glasses-gateway.ts:174** — UPDATE on apiKeys (updating lastUsedAt) uses only the key id in WHERE (eq(apiKeys.id, key.id)) and omits an explicit tenant_id constraint.
  - _Fix:_ Constrain the UPDATE to the owning tenant as well (e.g. .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, key.tenantId)))) to prevent accidental cross-tenant updates.
- **server/google-drive.ts:879** — UPDATE on projects sets driveFolderId/driveFolderUrl but the WHERE clause uses only projects.id (eq(projects.id, projectId)) and does not include a tenant_id constraint, allowing a possible cross-tenant mutation.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId)))) or otherwise ensure the update is executed only after verifying and including the caller's tenant id in the WHERE clause.
- **server/health-audit.ts:314** — SELECT from tenant-scoped table 'code_proposals' with no tenant_id filter (reads across all tenants).
  - _Fix:_ Scope this query to a specific tenant or require an explicit admin/system flag. Add WHERE tenant_id = ${tenantId} (or equivalent Drizzle filter) or otherwise enforce that only an admin-scoped audit intentionally queries across tenants. Avoid global selects on tenant-scoped tables.
- **server/health-audit.ts:333** — UPDATE on tenant-scoped table 'code_proposals' (marking stale) uses id array but lacks tenant_id constraint — can modify rows across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or derive ids only for the intended tenant. Alternatively, perform the archival only per-tenant (pass tenantId into archiveStaleProposals) or require an explicit admin-only code path with documented review.
- **server/health-audit.ts:346** — SELECT from 'heartbeat_tasks' has no tenant constraint — reads tasks potentially across tenants.
  - _Fix:_ If heartbeat_tasks are tenant-scoped, scope the query (WHERE tenant_id = ${tenantId}) or require an explicit admin/system intent. Ensure any subsequent actions (disabling tasks) are limited to the tenant's tasks.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(...) executes without tenant_id filtering — may disable tasks across tenants.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. AND tenant_id = ${tenantId}) or resolve and verify task ownership before mutating. Consider accepting a tenantId param or enforcing admin-only operation semantics with explicit checks.
- **server/google-workspace.ts:2287** — INSERT INTO presenter_slide_images does not set tenant_id (INSERT lacks tenantId). If presenter_slide_images is tenant-scoped, this violates the invariant and may write without tenant context.
  - _Fix:_ Make the tenant explicit in the INSERT values (e.g. include tenant_id / tenantId column), or ensure presenter_slide_images is not tenant-scoped. If the operation is intended to be tenant-specific, pass the tenantId into the INSERT and validate ownership of session_id before writing.
- **server/google-workspace.ts:2307** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} updates a tenant-scoped table without constraining by tenant_id.
  - _Fix:_ Constrain the UPDATE to the session's tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or validate session ownership before updating. Ensure all UPDATE/DELETE paths on tenant-scoped tables include tenant_id in the WHERE clause.
- **server/heartbeat-schedules.ts:60** — SELECT from tenant-scoped table `research_schedules` without tenant constraint: `SELECT * FROM research_schedules WHERE is_enabled = true AND next_run_at ...` returns rows across all tenants.
  - _Fix:_ Either restrict the query to a specific tenant (WHERE tenant_id = ${tenantId}) or, if this is intended as a privileged cross-tenant scheduler, ensure the cross-tenant access is intentionally isolated and gated by an admin-only codepath. Prefer explicit ADMIN_TENANT_ID usage or a scheduler-specific API that returns tenant-aware results to avoid accidental cross-tenant work.
- **server/heartbeat-startup.ts:81** — UPDATE on tenant-scoped table `heartbeat_tasks` without tenant constraint: `UPDATE heartbeat_tasks SET model = 'gpt-5-mini' WHERE id = ${t.id}` does not constrain by tenant_id.
  - _Fix:_ Include the tenant filter in the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or use the storage layer helper that accepts tenantId (e.g. storage.updateHeartbeatTask) so mutations are always scoped to the owning tenant.
- **server/heartbeat-startup.ts:85** — UPDATE on tenant-scoped table `heartbeat_tasks` without tenant constraint: `UPDATE heartbeat_tasks SET model = 'gemini-2.5-flash' WHERE id = ${t.id}` lacks tenant scoping.
  - _Fix:_ Add AND tenant_id = ${t.tenantId} to the WHERE clause or call the tenant-aware storage API to perform the update, ensuring writes cannot affect another tenant's row.
- **server/heartbeat-task-scheduler-storage.ts:11** — Scheduler SELECT across heartbeat_tasks with no tenant filter: `SELECT * FROM heartbeat_tasks WHERE enabled = true AND next_run_at <= NOW() ...` returns rows for all tenants.
  - _Fix:_ If this function is intentionally cross-tenant (scheduler-level), document and centralize it as a privileged boundary. Otherwise restrict to tenant-specific queries (WHERE tenant_id = ${tenantId}) or return tenant_id explicitly and ensure callers perform tenant-aware assertions before further processing. Avoid using this API from unprivileged request paths.
- **server/heartbeat.ts:256** — SELECT from tenant-scoped table `event_log` without tenant filter: `SELECT * FROM event_log WHERE status = 'pending' ORDER BY created_at ASC LIMIT 10` reads pending events from all tenants.
  - _Fix:_ Process events per-tenant or add a tenant_id filter when selecting rows (e.g. iterate tenant IDs and select per-tenant), or ensure this is a documented privileged path that explicitly intends cross-tenant processing and enforces strict access controls. Prefer selecting tenant_id in the query and verifying caller's remit before acting.
- **server/heartbeat.ts:2632** — Cross-tenant use of heartbeat_tasks when enforcing per-tenant policies: `existingTasks = await storage.getHeartbeatTasks(undefined, ALL_TENANTS)` then using that list to enforce delegation caps and duplicate detection allows tasks from other tenants to affect decisions for the current tenant.
  - _Fix:_ When enforcing per-tenant limits or duplicate detection, restrict the dataset to the current tenant (call storage.getHeartbeatTasks with the tenantId rather than ALL_TENANTS) or filter existingTasks by tenantId before applying caps/duplicate checks. Ensure policies are enforced against tenant-scoped subsets only.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor runs a GROUP BY query on agent_knowledge across all tenant_id/persona_id pairs (SELECT tenant_id, persona_id, COUNT(*) ... GROUP BY tenant_id, persona_id) with no tenant restriction, enumerating tenant-scoped data across tenants.
  - _Fix:_ Avoid unscoped scans of tenant-scoped tables. Either run the monitor per-tenant (iterate tenants under an authorized admin job) or require this operator job to run under a clearly auditable admin context and document the justification. Prefer per-tenant queries that always include WHERE tenant_id = ${tenantId}.

### MEDIUM (25)

- **server/agent-activity.ts:60** — Lookup of a persona by id does not include tenant scoping — SELECT from personas WHERE id = ... has no tenant filter and may return a persona belonging to another tenant.
  - _Fix:_ Ensure persona lookups are tenant-scoped or document that personas are global. Add tenant context to the query (e.g. .where(and(eq(personas.id, personaId), eq(personas.tenantId, tenantId))) ), or validate ownership before using the persona row.
- **server/agent-desk.ts:297** — Query selects personas without tenant constraint (SELECT id, name FROM personas). This can leak persona names across tenants when used to build an overview.
  - _Fix:_ Filter personas by tenant (WHERE tenant_id = ${tenantId}) when the intent is tenant-scoped. If personas are global/intentional, document and validate that global access is acceptable; otherwise make the query tenant-aware.
- **server/agent-channels.ts:126** — JOIN against personas (LEFT JOIN personas p ON p.id = cm.from_persona_id) does not enforce tenant ownership — the join can surface persona names/metadata from other tenants when persona rows are tenant-scoped.
  - _Fix:_ Ensure the join is tenant-scoped (e.g. JOIN personas p ON p.id = cm.from_persona_id AND p.tenant_id = cm.tenant_id) or otherwise validate persona ownership before returning persona details to the caller.
- **server/agentic/self-initiative.ts:100** — SELECT from heartbeat_logs lacks an explicit tenant_id constraint. The query filters via task_id IN (SELECT id FROM heartbeat_tasks WHERE tenant_id = ${tenantId}) but does not include an explicit WHERE heartbeat_logs.tenant_id = ${tenantId}. If heartbeat_logs is tenant-scoped this can read cross-tenant rows or rely on implicit chaining that may be fragile.
  - _Fix:_ Add an explicit tenant constraint to the heartbeat_logs read (e.g. AND heartbeat_logs.tenant_id = ${tenantId}) or JOIN heartbeat_tasks and require heartbeat_logs.task_id = heartbeat_tasks.id AND heartbeat_tasks.tenant_id = ${tenantId}. Ensure every read against a tenant-scoped table includes an app-level tenant_id filter to enforce isolation.
- **server/auth.ts:1136** — DELETE from password_reset_tokens purges expired tokens across all tenants: `DELETE FROM password_reset_tokens WHERE expires_at < ${Date.now()}` (no tenant_id filter).
  - _Fix:_ Run expiry cleanup per-tenant (iterate tenants) or add an explicit maintenance-only guard and audit log. If a global cleanup is required, protect the function so only a platform-admin/maintenance job can run it and document the intent.
- **server/auth.ts:1242** — Global DELETE from auth_sessions without tenant scoping in clearExpiredSessions: `DELETE FROM auth_sessions WHERE expires_at < ${now}`.
  - _Fix:_ Scope expired-session cleanup by tenant or require a controlled maintenance context. Alternatively, include tenant_id in the WHERE when invoked for a specific tenant, or gate the global purge behind an explicit platform-admin capability and add auditing.
- **server/auth.ts:1335** — Global DELETE from auth_sessions in loadSessionsFromDb: `DELETE FROM auth_sessions WHERE expires_at < ${now}` (no tenant_id constraint).
  - _Fix:_ Either scope cleanup to a tenant, or run this maintenance only in a controlled admin context with explicit audit logging. Prefer per-tenant deletes when invoked from tenant-scoped flows.
- **server/auth.ts:1339** — DELETE from auth_sessions lacking tenant constraint: `DELETE FROM auth_sessions WHERE token NOT LIKE 'v2:%'`. This deletes rows across all tenants (intended for migration but no tenant scoping).
  - _Fix:_ If this is a one-time migration, guard it so it cannot run inadvertently in production; otherwise, make the operation explicit and auditable (e.g. require platform-admin and log the action) or scope by tenant if executed in tenant contexts.
- **server/auto-transcript.ts:128** — Query scans conversations and uses a subselect on messages without scoping by tenant_id: `(SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0`. This reads a tenant-scoped table (messages) with no tenant filter.
  - _Fix:_ Constrain the subquery by the conversation's tenant to avoid cross-tenant reads, e.g. `(SELECT COUNT(*) FROM messages WHERE conversation_id = c.id AND tenant_id = c.tenant_id) > 0` or join messages with an explicit `AND messages.tenant_id = c.tenant_id` clause.
- **server/chat-daily-log.ts:12** — updateDailyLog calls storage.upsertDailyNote(..., tenantId) where tenantId is an optional parameter and may be undefined/null. If the underlying daily_notes table is tenant-scoped and requires tenant_id NOT NULL, this may result in inserts without a tenant_id or accidental cross-tenant writes if storage treats undefined specially.
  - _Fix:_ Require and validate tenantId before calling storage functions. Fail fast if tenantId is missing (throw or return). Ensure storage.upsertDailyNote enforces and requires tenant_id (and that callers always pass an explicit tenant id). If upsertDailyNote is used for both global and tenant notes, use explicit APIs for global notes to avoid ambiguity.
- **server/commitments.ts:135** — SELECT from commitments in scanAndEscalate queries rows across all tenants (SELECT id, tenant_id, description, ... FROM commitments WHERE status='active' ... ) with no tenant_id constraint. This code intentionally fans-in across tenants for an owner digest, but it still reads tenant-scoped table rows without tenant scoping.
  - _Fix:_ If this is an admin-only platform process, ensure it runs under an explicit, well-audited admin context and minimize the columns returned (avoid including tenant-sensitive free-text fields). Otherwise, run per-tenant or add an explicit tenant_id filter. If cross-tenant aggregation is required, move to a dedicated admin-only pathway and document/authorize it, or implement Postgres-side safeguards (RLS) to limit accidental cross-tenant reads.
- **server/data-protection.ts:166** — COUNT(*) query against messages uses only conversation_id (SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id}) and omits tenant_id in the WHERE clause.
  - _Fix:_ Include tenant_id in the WHERE (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}) so reads against tenant-scoped tables are always explicitly constrained to the caller's tenant.
- **server/doc-collections.ts:136** — Subqueries in listCollections COUNT doc_chunks WHERE collection_id = c.id do not include tenant_id. These SELECTs read doc_chunks without an explicit tenant filter.
  - _Fix:_ Add tenant_id filtering to the subselects (e.g. WHERE collection_id = c.id AND tenant_id = ${tenantId}) so counts reflect only the requested tenant and avoid cross-tenant information leakage.
- **server/doc-collections.ts:457** — Subqueries in getCollectionStatus reference doc_chunks (COUNT, SUM) but omit tenant_id in the subselects (e.g. WHERE collection_id = c.id). These read tenant-scoped doc_chunks without an explicit tenant constraint.
  - _Fix:_ Include tenant_id = ${tenantId} in the subselect WHERE clauses (e.g. WHERE collection_id = c.id AND tenant_id = ${tenantId}) to ensure returned metrics are scoped to the tenant.
- **server/dormant-deprecation.ts:169** — SELECT COUNT(*) FROM messages WHERE ... lacks tenant_id filtering (getTrafficSnapshot). messages appears to be a tenant-scoped table elsewhere but is queried here without tenant constraint.
  - _Fix:_ If the intent is per-tenant metrics, add tenant_id = ${tenantId} to the WHERE clause. If this is a global/system-level aggregation, document that fact and ensure only appropriately privileged system code runs this query; otherwise scope queries to tenants to enforce isolation.
- **server/dormant-deprecation.ts:179** — SELECT COUNT(DISTINCT tool_name) FROM tool_performance WHERE last_success_at > ... is missing tenant_id filtering (getTrafficSnapshot). tool_performance is used with tenant scoping elsewhere but here is queried globally.
  - _Fix:_ Decide whether this job should operate globally or per-tenant. For per-tenant operation add AND tenant_id = ${tenantId}; for global operation, ensure it runs only in a trusted context and document the intent. Avoid accidental cross-tenant reads in tenant-scoped contexts.
- **server/dormant-deprecation.ts:204** — SELECT DISTINCT tool_name FROM tool_performance executed without tenant_id (getEverInvokedTools). This reads a tenant-scoped table without tenant scoping.
  - _Fix:_ If per-tenant data is required add WHERE tenant_id = ${tenantId}. If the query is intentionally global, ensure it's executed with appropriate privileges and document that it's aggregating across tenants.
- **server/dormant-deprecation.ts:214** — SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... has no tenant_id filter (getActiveTools14d). This touches a tenant-scoped table without scoping.
  - _Fix:_ Either constrain by tenant_id for tenant-scoped operation, or, if global aggregation is intended, clearly document and restrict the operation to trusted system-level code. Avoid ambiguous unscoped reads against tenant-scoped tables.
- **server/embeddings.ts:256** — vectorSearchMemory uses sql.raw() to inject a vector literal into SQL built from a runtime embedding derived from caller data (vec derived from generateEmbedding(query)) — e.g. 1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}) and ORDER BY embedding_vec <=> ${sql.raw(`'${vec}'::vector`}). Using sql.raw() with runtime/request-derived values increases injection risk even if some validation exists.
  - _Fix:_ Avoid building raw SQL fragments with string interpolation. Use parameterized queries or Drizzle helpers that pass the vector as a bound parameter and cast safely, or ensure a robust numeric-only validation + explicit casting function. Example: use a parameterized representation and a safe cast function, or store the vector in a temporary table/parameter and reference it without string interpolation.
- **server/embeddings.ts:583** — vectorSearchKnowledge similarly uses sql.raw() to inject a runtime vector literal (from generateEmbedding(query)) into SQL (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}). This is using sql.raw() with request-derived data.
  - _Fix:_ Same as above: avoid string-interpolating the vector via sql.raw(). Use parameterized binds or another approach to pass the vector safely to Postgres (or validate thoroughly and centralize safe casting), and ensure tenant scoping is preserved in the query.
- **server/episode-playbooks.ts:133** — String-interpolated array literal used inside SQL for id ANY(...) : WHERE tenant_id = ${tenantId} AND id = ANY(${`{${ids.join(",")}}`}::int[]). Building the array fragment via string interpolation instead of a parameterized array may lead to injection risks if ids are not trusted/sanitized.
  - _Fix:_ Use parameterized expressions or sql.join to pass an array safely (e.g. WHERE id = ANY(${sql`$1::int[]`}, [ids]) or WHERE id = ANY(${sql.join(ids.map(i => sql`${i}`), sql`,`)}) ), and validate ids are integers. Even if ids are DB-derived, prefer parameterized forms to avoid future injection vectors.
- **server/heartbeat-schedules.ts:26** — Cross-tenant SELECT on tenant-scoped table without tenant filter: `SELECT DISTINCT tenant_id FROM messages ...` has no WHERE tenant_id = <tenant> constraint.
  - _Fix:_ If this is an explicit, privileged cross-tenant enumeration, make that intent explicit (call site must be strictly admin-only) and document it. Otherwise add a tenant filter (e.g. WHERE tenant_id = ${tenantId}) or use a dedicated metadata/global table for enumerating tenants. Limit the scope and ensure only privileged background code can run cross-tenant queries.
- **server/job-queue.ts:281** — failJob first SELECT reads max_attempts FROM agent_jobs WHERE id = ${id} with no tenant_id constraint.
  - _Fix:_ Require caller to pass tenant context or tenantId and include AND tenant_id = ${tenantId} in the SELECT to verify the row belongs to the expected tenant before making retry/terminal decisions.
- **server/job-queue.ts:377** — getJobTenantForAdmin selects tenant_id FROM agent_jobs WHERE id = ${id} without tenant-scoping. While intended as an admin helper, it's an unscoped read against a tenant-scoped table.
  - _Fix:_ Document and gate this as an admin-only helper and ensure caller authorization/auditing. If possible, require an admin-scoped code path or explicit admin token; otherwise prefer an API that returns tenant only after verifying caller privileges.
- **server/job-queue.ts:406** — listJobs builds and runs a SELECT over agent_jobs that can omit tenant_id (when filter.tenantId not provided), returning rows across all tenants.
  - _Fix:_ Require callers to pass an explicit tenantId (or an ALL_TENANTS sentinel only for trusted admin contexts). For tenant-facing routes, default to the caller's tenant and forbid unscoped listing.
