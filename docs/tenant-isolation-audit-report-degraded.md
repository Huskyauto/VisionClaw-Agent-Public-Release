# Tenant-Isolation Audit Report

_Generated 2026-09-19T14:02:43.758Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 279 file(s) were NOT audited this run (chunk cap in effect). Findings below are PARTIAL — do not read "few findings" as "all clear."

- Coverage: **409/688** file(s) audited · chunks 39/64 OK (0 failed, 25 pending)
- Findings: **154** (CRITICAL 17 · HIGH 109 · MEDIUM 28 · LOW 0)
- Severe dispositions: **126 kept** · 0 precision false-positive · 0 allowlist false-positive · 0 deferred real risk

### CRITICAL (17)

- **server/agentic/resume-worker.ts:65** — The raw SELECT starting at this line queries agent_runs (and agent_approvals via a LEFT JOIN) without constraining by tenant_id — it performs an un-scoped, cross-tenant read of tenant-scoped tables.
  - _Fix:_ Restrict this query to a single tenant or iterate per-tenant. Add an explicit tenant filter (e.g. WHERE r.tenant_id = ${tenantId}) or rewrite using Drizzle with eq(agentRuns.tenantId, tenantId). Do not run a global cross-tenant SELECT against tenant-scoped tables; ensure every read/write includes the caller's tenant_id.
- **server/business-tools.ts:95** — INSERT into invoice_items omits tenant_id. The application invariant requires every write to tenant-scoped tables include the caller's tenant (tenant_id) explicitly; inserting invoice line items without tenant_id can allow cross-tenant data leakage or orphaned rows.
  - _Fix:_ Include tenant_id in the INSERT for invoice_items (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${newId}, ...)). Also ensure any future reads/updates against invoice_items always filter by tenant_id = ${tid}. Consider adding a unique/foreign-key guard (application-checked) to verify invoice_id belongs to the same tenant before inserting, and add tests enforcing the tenant_id is present on all invoice_items operations.
- **server/doc-collections.ts:166** — addDocument(): the DELETE that precedes chunk re-insert lacks a tenant filter: DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}. doc_chunks is tenant-scoped elsewhere (inserts include tenant_id), so this DELETE can remove another tenant's chunks.
  - _Fix:_ Add tenant scoping to the DELETE, e.g. DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}. Always include tenant_id on any DELETE/UPDATE/SELECT against tenant-scoped tables.
- **server/email.ts:429** — UPDATE on tenant-scoped table `inbox_messages` without tenant_id constraint (UPDATE inbox_messages SET body_text = ..., body_html = ... WHERE id = ${row.id}). Writes to tenant-scoped data are performed without verifying tenant ownership.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or otherwise verify resolved tenant ownership of the row before updating. For backfill flows, determine the tenant for each row and use tenant-scoped updates.
- **server/embeddings.ts:845** — SELECT/UPDATE on tenant-scoped table `memory_entries` without tenant_id constraint (SELECT id, embedding FROM memory_entries ... LIMIT 200; later UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id}). Both the read and the write are executed across all tenants / by id only, allowing modification of rows regardless of tenant ownership.
  - _Fix:_ Perform backfill per-tenant or include tenant_id in both the SELECT and subsequent UPDATE statements (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}). Ensure tenantId is resolved/tracked through the backfill workflow so writes always include tenant scoping.
- **server/embeddings.ts:808** — UPDATE using dynamic table and without tenant constraint: db.execute(sql`UPDATE ${sql.raw(table)} SET ... WHERE id = ${id}`). This code path (when options.tenantId is undefined) updates a tenant-scoped table (memory_entries or agent_knowledge) using only id and a runtime-chosen table string, with no tenant_id predicate.
  - _Fix:_ Require a positive tenantId for any storeEmbeddingVec call that mutates tenant-scoped tables and throw if missing. Avoid constructing the table name via sql.raw; instead branch on the allowed table value and use parameterized, tenant-scoped UPDATEs such as UPDATE memory_entries SET ... WHERE id = ${id} AND tenant_id = ${tenantId}.
- **server/google-drive.ts:1664** — INSERT into project_files omits tenant_id. The INSERT uses INSERT INTO project_files (project_id, file_name, ...) VALUES (...) without explicitly setting tenant_id. Per invariant all tenant-scoped INSERTs must set tenant_id explicitly (columns are NOT NULL with no default).
  - _Fix:_ Include tenant_id (validated) in the INSERT values. Ensure the tenant was validated (assertProjectInTenant or equivalent) before inserting, then INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${tenantId}, ${params.projectId}, ...). Alternatively derive tenant_id via a JOIN on projects where you also enforce p.tenant_id = callerTenant before inserting.
- **server/google-workspace.ts:2307** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} has no tenant_id constraint. This can update another tenant's presenter session by ID.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}), or first SELECT the session and verify its tenant_id equals the caller's tenant before performing the update. Never rely solely on session id without ownership validation.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' WHERE id = ANY(${ids}::int[]) updates proposals without checking tenant_id, risking cross-tenant modification.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. AND tenant_id = ${tenantId}) or perform the update via a query that joins/filters by tenant_id for each id. If running as global admin intentionally, require an explicit admin guard and document it, but avoid unscoped updates.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ANY(${ids}::int[]) performs a destructive change without tenant scoping, potentially disabling tasks for other tenants.
  - _Fix:_ Restrict the UPDATE by tenant_id (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or validate each task's tenant before mutating. For cross-tenant admin operations require an explicit, auditable admin context before running unscoped updates.
- **server/lib/ideabrowser-backfill.ts:108** — INSERT into `project_files` does not set tenant_id. Per project conventions tenant_id is required/no-default, so this write may create cross-tenant or orphaned rows.
  - _Fix:_ Include tenant_id explicitly in INSERTs into tenant-scoped tables (e.g. INSERT ... (project_id, ..., tenant_id) VALUES (..., ${tenantId})). If project_files should be owned via project_id only, enforce and assert ownership server-side before inserting.
- **server/lib/ideabrowser-backfill.ts:124** — INSERT into `project_notes` does not set tenant_id. If project_notes is tenant-scoped this write omits the required tenant_id column.
  - _Fix:_ Ensure every INSERT into tenant-scoped tables sets tenant_id explicitly (e.g. INSERT INTO project_notes (..., tenant_id) VALUES (..., ${tenantId})), or validate and use the owning project's tenant_id when inserting.
- **server/lib/ideabrowser-dev-sync.ts:124** — INSERT into `project_notes` (via SELECT ... WHERE NOT EXISTS) does not set tenant_id — write into a tenant-scoped table without tenant_id.
  - _Fix:_ Add tenant_id to the INSERT payload (INSERT INTO project_notes (project_id, note, author, tenant_id) ... VALUES (..., ${tenantId})). Alternatively, validate ownership of project_id and use that tenant_id to populate the column before inserting.
- **server/lib/ideabrowser-dev-sync.ts:150** — INSERT into `project_files` (via SELECT ... WHERE NOT EXISTS) omits tenant_id — writing to a tenant-scoped table without an explicit tenant_id value.
  - _Fix:_ Include tenant_id in the INSERT (INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})) or validate that project_id is owned by the current tenant and use that tenant value in the INSERT.
- **server/lib/scheduled-post-runner.ts:61** — SELECT/UPDATE uses scheduled_posts without constraining by tenant_id (CTE selects due rows across all tenants). This pulls/locks tenant-scoped rows without an application-level tenant filter.
  - _Fix:_ Limit the CTE to the intended tenant (e.g. add AND tenant_id = ${someTenantId} or pass an explicit tenantId parameter), or if this runner must operate across tenants, make the cross-tenant intent explicit and ensure authorization/audit controls. In any case, avoid selecting tenant-scoped rows without an explicit tenant_id predicate; include tenant_id in the WHERE clause or otherwise enforce and document the admin-scoped privilege-check before touching tenant data.
- **server/plan-executor.ts:703** — UPDATE plans ... WHERE id = ${planId} AND status = 'approved' executes a cross-tenant state transition without constraining by tenant_id. This CAS claim can change the status of a plan belonging to any tenant (no tenant filter), enabling cross-tenant plan execution/side-effects.
  - _Fix:_ Enforce tenant-scoped claiming. Require a validated caller/tenantId (or look up and validate the plan's tenant first) and include AND tenant_id = ${callerTenantId} in the WHERE clause (or perform a prior SELECT that validates tenant ownership and then use that tenant value in the UPDATE WHERE). Do not rely solely on returned tenant_id for later checks — the UPDATE must be constrained atomically to the tenant to avoid TOCTOU/cross-tenant races.
- **server/process-governor.ts:898** — UPDATE agent_desks executed without constraining by tenant_id (WHERE id = ${desk.id} only). This allows the governor to modify agent_desks rows belonging to other tenants if ids collide.
  - _Fix:_ Include tenant scoping in the UPDATE, e.g. UPDATE agent_desks SET ... WHERE id = ${desk.id} AND tenant_id = ${tenantId} (use the same tenantId passed to the function). Always bind tenant_id for any mutating SQL on tenant-scoped tables.

### HIGH (109)

- **server/ab-optimizer.ts:110** — Unscoped SELECT against tenant-scoped table `experiments`. runDueAbExperiments() reads experiments across all tenants (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running') with no tenant_id constraint.
  - _Fix:_ Scope this query to a single tenant or iterate per-tenant. Add a tenantId parameter to runDueAbExperiments() (or otherwise compute the target tenant) and include WHERE tenant_id = ${tenantId} in the SELECT so rows from other tenants are never read by a caller without that tenant context.
- **server/agent-eval.ts:221** — UPDATE on tenant-scoped table `agent_evals` without tenant constraint. The UPDATE uses WHERE id = ${evalId} but does not include tenant_id, allowing a row belonging to another tenant to be modified if evalId is guessed or mis-bound.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}) or otherwise verify ownership before updating. Ensure evalId cannot be used to cross-tenant modify rows.
- **server/agentic/department-budgets.ts:143** — Global SELECT against tenant-scoped table `department_budgets` without a tenant_id constraint. The query `SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active'` reads rows for all tenants, violating the invariant that every read/write on tenant-scoped tables must be constrained to the caller's tenant.
  - _Fix:_ If this is intended to be an admin/global job, make that explicit and restrict invocation to an admin-only context (so the cross-tenant read is a deliberate, auditable admin action). Otherwise, restrict the query to a single tenant by adding `WHERE tenant_id = ${tenantId}` or iterate tenants via a controlled, privileged path that clearly documents and authorizes cross-tenant operations. Concretely either (a) change the function to accept a tenantId and use `WHERE tenant_id = ${tenantId} AND status = 'active'`, or (b) require an explicit admin flag and runtime check (and log/alert) before running the global sweep, and ensure callers cannot invoke this from regular tenant-scoped code paths.
- **server/agentic/self-initiative.ts:76** — Raw/tagged SQL with request-derived tenantId interpolation (db.execute(sql`... ${tenantId} ...`)). This uses string-interpolated SQL rather than Drizzle query builders and mixes request data into raw SQL (injection + isolation risk).
  - _Fix:_ Avoid db.execute with template interpolation for tenant-scoped queries. Use Drizzle query builder (e.g. db.select().from(repair_incidents).where(eq(repair_incidents.tenantId, tenantId))) or parameterized queries, and ensure the tenant filter is always present.
- **server/agentic/self-initiative.ts:81** — Raw/tagged SQL with request-derived values (tenantId, since30d) interpolated into a db.execute(sql`...`) call when querying repair_incidents. Using interpolated SQL for tenant-scoped reads risks injection/isolation mistakes.
  - _Fix:_ Use Drizzle query builder with explicit .where(eq(...)) and parameterized values. e.g. db.select({classification: repair_incidents.classification, n: sql<number>`count(*)::int`}).from(repair_incidents).where(and(eq(repair_incidents.tenantId, tenantId), gte(repair_incidents.createdAt, since30d))).groupBy(...).limit(6).
- **server/agentic/self-initiative.ts:92** — Raw/tagged SQL with tenantId interpolation used to query audit_leads via db.execute(sql`SELECT COUNT(*)... WHERE tenant_id = ${tenantId}`). This is string-interpolated SQL rather than Drizzle builder usage.
  - _Fix:_ Use Drizzle query builder with explicit tenant filter (eq(audit_leads.tenantId, tenantId)) or parameterized queries to avoid SQL construction with request-derived values.
- **server/agentic/self-initiative.ts:100** — db.execute(sql`... task_id IN (SELECT id FROM heartbeat_tasks WHERE tenant_id = ${tenantId})`): raw SQL with tenantId interpolated into a subquery. This is string-interpolated SQL and mixes request-derived data into raw SQL text.
  - _Fix:_ Replace with Drizzle builders and joins/subqueries using eq(heartbeat_tasks.tenantId, tenantId) or use parameterized query helpers so tenant filtering and parameters are handled safely.
- **server/agentic/self-initiative.ts:114** — db.execute(sql`SELECT title FROM self_initiatives WHERE tenant_id = ${tenantId} ...`): raw SQL template interpolating tenantId. Using raw/tagged SQL with request-derived values increases injection and isolation risk.
  - _Fix:_ Use db.select().from(self_initiatives).where(and(eq(self_initiatives.tenantId, tenantId), eq(self_initiatives.status, status))) or equivalent Drizzle API to ensure parameterization and consistent tenant scoping.
- **server/agentic/self-initiative.ts:266** — db.execute(sql`SELECT signature FROM self_initiatives WHERE tenant_id = ${tenantId} AND status IN ('surfaced','approved','acting')`): string-interpolated SQL with tenantId. Also used later with an INSERT that interpolates many values (tenantId, title, telemetry JSON, sig) into raw SQL.
  - _Fix:_ Use Drizzle query builder for both SELECT and INSERT (db.select().from(...).where(...), db.insert(self_initiatives).values({...})). Avoid constructing SQL with interpolated JSON/strings; pass telemetry/strings as parameters or JSONB values through Drizzle to prevent injection and make tenant filter explicit.
- **server/agentic/self-initiative.ts:301** — db.execute with an INSERT that interpolates tenantId, title, telemetry JSON and other values directly into a sql`...` template (INSERT INTO self_initiatives ... VALUES (${tenantId}, ${title}, ... ${JSON.stringify(telemetry)}::jsonb, ...)). This is string-interpolated SQL including untrusted/generated content (LLM output / telemetry).
  - _Fix:_ Use Drizzle's insert builder: db.insert(self_initiatives).values({tenantId, title, rationale, category, evidence: telemetry, ...}).onConflict(...). Do not stringify JSON into the SQL template; let the query builder bind JSON parameters.
- **server/agentic/step-reward.ts:140** — db.execute(sql`INSERT INTO step_rewards (tenant_id, ...) VALUES (${i.tenantId}, ...)`): raw/tagged SQL interpolation with request-derived tenantId and other inputs. This constructs SQL with request data via template interpolation.
  - _Fix:_ Switch to Drizzle query builder (db.insert(step_rewards).values({...})). Ensure tenantId is passed as an explicit column value and let the query builder parameterize inputs to avoid interpolation into SQL text.
- **server/agentic/step-reward.ts:171** — db.execute(sql`SELECT * FROM step_rewards WHERE tenant_id = ${tenantId} AND plan_id = ${planId} ...`): raw SQL with interpolated tenant and plan ids. Using tagged-template SQL here mixes request-derived values into SQL text.
  - _Fix:_ Use Drizzle select builder (db.select().from(step_rewards).where(and(eq(step_rewards.tenantId, tenantId), eq(step_rewards.planId, planId)))) so tenant filter is explicit and parameters are bound safely.
- **server/agentic/task-forces.ts:40** — db.execute(sql`INSERT INTO task_forces (tenant_id, ...) VALUES (${p.tenantId}, ${p.name}, ...)`): raw SQL interpolation with tenantId and other fields. Building INSERT SQL with interpolated values risks injection and makes tenant constraints implicit in raw SQL text.
  - _Fix:_ Use Drizzle's insert API (db.insert(task_forces).values({...}).returning()) to bind parameters safely and keep tenant scoping explicit.
- **server/agentic/task-forces.ts:59** — db.execute(sql`SELECT * FROM task_forces WHERE tenant_id = ${tenantId} ...`): raw SQL with interpolated tenantId (and interpolated status when present). This is string-interpolated SQL rather than Drizzle query builder usage.
  - _Fix:_ Use db.select().from(task_forces).where(eq(task_forces.tenantId, tenantId)) and add optional status filter via .where(eq(task_forces.status, status)) to ensure proper parameterization and tenant isolation.
- **server/agentic/task-forces.ts:87** — db.execute(sql`UPDATE task_forces SET spent_usd = (...) WHERE id = ${id} AND tenant_id = ${tenantId} ...`): raw SQL interpolation with id, tenantId, amountUsd. Constructs update SQL with interpolated values instead of builder API.
  - _Fix:_ Use Drizzle update builder (db.update(task_forces).set({...}).where(and(eq(task_forces.id, id), eq(task_forces.tenantId, tenantId)))) and pass numeric arithmetic via db expressions or parameterized values to avoid building SQL text with request-derived inputs.
- **server/agentic/wake-scheduler.ts:66** — db.execute(sql`INSERT INTO agent_wake_schedules (tenant_id, ...) VALUES (${p.tenantId}, ...)`): raw SQL interpolation with tenantId and several request-derived fields (goal, context, triggerFilter). This interpolates untrusted values into SQL template.
  - _Fix:_ Use Drizzle insert builder: db.insert(agent_wake_schedules).values({tenantId: p.tenantId, personaId: p.personaId, goal: p.goal, context: p.context, ...}).returning() so inputs are bound as parameters and tenant scoping is explicit.
- **server/agentic/wake-scheduler.ts:100** — db.execute(sql`UPDATE agent_wake_schedules SET ... WHERE tenant_id = ${tenantId} AND ... AND (trigger_filter IS NULL OR ${dataJson}::jsonb @> trigger_filter) ...`): raw SQL with tenantId and injected dataJson from event payload. Interpolating event-derived JSON into SQL text is an injection/isolation risk.
  - _Fix:_ Avoid interpolating dataJson into SQL template. Use parameter binding via Drizzle or prepared statements: db.update(agent_wake_schedules).set({...}).where(and(eq(agent_wake_schedules.tenantId, tenantId), sql`(trigger_filter IS NULL OR ${dataJson}::jsonb @> trigger_filter)`)) — or better, use query builder helpers that allow passing a JSON parameter safely.
- **server/agentic/wake-scheduler.ts:142** — db.execute(sql`UPDATE agent_wake_schedules ... WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ORDER BY wake_at ASC LIMIT ${limit} FOR UPDATE SKIP LOCKED )`): raw SQL interpolation of the limit parameter into the query text. Interpolating request-derived 'limit' into raw SQL is unsafe.
  - _Fix:_ Use parameter binding or the query builder that supports LIMIT as a parameter, or validate and coerce limit to an integer and then use the builder (e.g. db.select().from(...).limit(Number(limit))). Avoid injecting numbers into SQL text even when numeric — prefer builder APIs.
- **server/agentic/wake-scheduler.ts:166** — Inside runDueWakes loop: db.execute(sql`UPDATE agent_wake_schedules SET ... WHERE id = ${w.id} AND tenant_id = ${w.tenant_id} AND status = 'firing'`): using returned DB row fields interpolated back into raw SQL templates. While values originate from DB, constructing SQL text via interpolation is brittle and error-prone.
  - _Fix:_ Prefer Drizzle updates with parameterized values (db.update(agent_wake_schedules).set({...}).where(and(eq(agent_wake_schedules.id, w.id), eq(agent_wake_schedules.tenantId, w.tenant_id), eq(agent_wake_schedules.status, 'firing')))). This maintains clarity and avoids ad-hoc SQL construction.
- **server/attention-handlers/feature-announce.ts:100** — db.execute(sql`INSERT INTO notifications (tenant_id, type, title, message, category, metadata) VALUES (${tenantId}, 'digest', ${`📝 Draft announcement: ${title}`.slice(0, 200)}, ${body}, 'owner_digest', ${metadata}::jsonb)`) — raw SQL interpolation of tenantId and LLM-derived title/body/metadata into SQL text. LLM output / untrusted content is interpolated directly.
  - _Fix:_ Use Drizzle insert builder and bind metadata as JSON (db.insert(notifications).values({tenantId, type: 'digest', title, message: body, category: 'owner_digest', metadata: JSON.parse(metadata) })). Never inject LLM output into SQL templates; bind values via the query builder.
- **server/attention-handlers/owner-notify.ts:34** — db.execute(sql`SELECT 1 FROM delivery_logs WHERE id = ${data.deliveryId} AND tenant_id = ${tenantId} LIMIT 1`) — raw/template SQL interpolating a caller-supplied deliveryId and tenantId. Querying referenced rows using interpolated values risks injection and makes verification brittle.
  - _Fix:_ Use Drizzle select builder: db.select().from(delivery_logs).where(and(eq(delivery_logs.id, data.deliveryId), eq(delivery_logs.tenantId, tenantId))).limit(1). This ensures parameters are bound and tenant ownership check is explicit.
- **server/attention-handlers/owner-notify.ts:82** — db.execute(sql`INSERT INTO notifications (tenant_id, type, title, message, category, metadata) VALUES (${tenantId}, 'digest', ${title}, ${message}, 'owner_digest', ${metadata}::jsonb)`) — raw SQL interpolation of tenantId and event-derived payload into SQL text.
  - _Fix:_ Use Drizzle insert builder (db.insert(notifications).values({...})) and pass metadata as parsed JSON or a parameterized object. Ensure tenantId is explicit and all inputs are bound, not interpolated into SQL strings.
- **server/attention-scorer.ts:84** — db.execute(sql`SELECT 1 FROM event_log WHERE tenant_id = ${tenantId} AND event_type = ${eventType} AND created_at > NOW() - INTERVAL '24 hours' LIMIT 1`) — raw/tagged SQL interpolation using tenantId and eventType. This constructs SQL text with request-derived values.
  - _Fix:_ Use Drizzle query builder: db.select().from(event_log).where(and(eq(event_log.tenantId, tenantId), eq(event_log.eventType, eventType), gt(event_log.createdAt, sql`NOW() - INTERVAL '24 hours'`))).limit(1). This keeps tenant filtering explicit and parameters bound.
- **server/audit-fulfillment.ts:79** — db.execute(sql`SELECT icp_hint, notes, tier_interest FROM audit_leads WHERE tenant_id = ${tenantId} AND email = ${customerEmail} ORDER BY id DESC LIMIT 1`) — raw SQL interpolation of tenantId and customerEmail (user input) into query text for intake recovery.
  - _Fix:_ Use Drizzle select builder with explicit parameter binding: db.select({icp_hint: audit_leads.icp_hint, notes: audit_leads.notes, tier_interest: audit_leads.tier_interest}).from(audit_leads).where(and(eq(audit_leads.tenantId, tenantId), eq(audit_leads.email, customerEmail))).orderBy(desc(audit_leads.id)).limit(1). This avoids string-interpolated SQL and ensures tenant ownership is checked.
- **server/auth.ts:1327** — DELETE FROM auth_sessions with no WHERE (DELETE FROM auth_sessions) — unconditional cross-tenant deletion. Although this function is intended for platform admin use, the SQL performs a destructive global mutation without an explicit tenant filter.
  - _Fix:_ Keep the platform-admin authority check (and audit) but harden by (a) making the authorization boundary extremely explicit (requirePlatformAdmin and an additional flag), (b) logging/auditing the calling user/tenant, and (c) consider requiring an explicit mode/confirmation parameter. If per-tenant behavior is desired, iterate tenants and delete with WHERE tenant_id = <tenant>.
- **server/browser-tool.ts:1687** — Caller-supplied parent folder id (_projectDriveFolderId) is forwarded to uploadScreenshotToDrive without verifying the folder belongs to the caller's tenant. This lets a caller cause uploads into arbitrary Drive folders (cross-tenant write/exfiltration risk).
  - _Fix:_ Validate that any supplied parent/project Drive folder id belongs to the requester's tenant before using it. Prefer deriving the destination folder from a server-side project record (after asserting project ownership), or disallow client-provided parentFolderId entirely and only permit server-owned tenant folders.
- **server/build-video-from-brief.ts:427** — userImageDriveFileId from the caller is downloaded (downloadFromDrive) without verifying the file belongs to the caller's tenant or that the caller is authorized to access it. This can allow cross-tenant data access/exfiltration.
  - _Fix:_ Before downloading or using a caller-supplied Drive file id, verify ownership/ACLs that the file is accessible to the caller's tenant (or restrict inputs to files previously uploaded and recorded for that tenant). If that verification is not possible, require the file to be uploaded via the platform's attachment/storage flow which enforces tenant ownership, or perform an explicit server-side permission check via the Drive API.
- **server/ceo-orchestrator.ts:661** — Unfiltered read from personas table (tenant-scoped access missing). The code calls `db.select().from(personasTable)` and loads all personas into `cachedPersonas` without constraining by the current tenantId, which can leak cross-tenant data and allow cross-tenant references.
  - _Fix:_ Restrict the query to the plan's tenant. Use a tenant-aware WHERE clause or Drizzle eq filter (e.g. `await db.select().from(personasTable).where(eq(personasTable.tenantId, plan.tenantId))`) or otherwise pass the tenantId into the import and filter results so only rows for the caller's tenant are returned.
- **server/channel-routing.ts:30** — SQL SELECT joins personas without a tenant constraint. The raw SQL in listChannelRoutes joins `personas p ON p.id = cr.persona_id` and returns persona names without filtering personas by tenant_id. If `personas` is tenant-scoped this exposes cross-tenant rows.
  - _Fix:_ Make channel_routes/personas access tenant-aware. Either add a tenant_id column to channel_routes and include `WHERE cr.tenant_id = ${tenantId} AND p.tenant_id = ${tenantId}`, or, if personas are per-tenant, join/filter `p` by `p.tenant_id = ${tenantId}`. Prefer parameterized insertion of tenantId and use Drizzle query builders (e.g. `.where(eq(personas.tenantId, tenantId))`) to avoid accidental global reads.
- **server/chat-engine.ts:1624** — buildMemorySection declares a default tenantId = 1. If a caller omits tenantId, operations invoked from inside this function (including telemetry/recording functions that accept tenantId) will run under tenant 1, creating an accidental fail-open admin context and risking cross-tenant data access or writes.
  - _Fix:_ Remove the baked-in default tenantId of 1. Make tenantId a required parameter (no default) and enforce a fail-closed runtime check (e.g. throw if tenantId is missing or not a positive integer). Audit all callers to ensure they always pass the caller's tenantId. If a convenience helper is needed, use an explicit guard that refuses to run when tenantId is absent rather than silently defaulting to the admin tenant.
- **server/chat-engine/project-context.ts:23** — Query reads from project_conversations (pc) without constraining that table to the caller's tenant. The WHERE clause filters projects.p.tenant_id but pc.* itself is not explicitly tenant-scoped, so rows could be returned for other tenants if project ids are mis-associated.
  - _Fix:_ Add an explicit tenant filter on project_conversations (e.g. AND pc.tenant_id = ${tenantId} or .where(eq(project_conversations.tenantId, tenantId))). If relying on project/project_id ownership, still include the tenant_id predicate on pc to satisfy the required app-level tenant fence.
- **server/chat-engine/project-context.ts:45** — EXISTS subquery reads from project_conversations without a tenant_id constraint (pc.conversation_id / pc.project_id used but pc.tenant_id is not checked). This allows a tenant isolation gap if project_conversations is a tenant-scoped table.
  - _Fix:_ Constrain the EXISTS subquery to the caller's tenant (e.g. WHERE pc.conversation_id = ${conversationId} AND pc.project_id = p.id AND pc.tenant_id = ${tenantId} or use an equivalent Drizzle eq on pc.tenantId). Always include explicit tenant filters on every tenant-scoped table referenced.
- **server/chat-engine/project-context.ts:159** — SELECT from project_files uses only project_id in the WHERE clause and does not include tenant_id. Per the project invariant, every read against a tenant-scoped table must include an explicit tenant_id constraint.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId} or .where(and(eq(project_files.projectId, projectId), eq(project_files.tenantId, tenantId))). Do not rely solely on prior project ownership checks — include the tenant_id predicate on the table being read.
- **server/chat-engine/project-context.ts:171** — SELECT from project_notes filters only by project_id and omits tenant_id. This is a tenant-isolation gap for the tenant-scoped project_notes table.
  - _Fix:_ Add an explicit tenant filter to the query (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId} or use Drizzle eq(project_notes.tenantId, tenantId)). Ensure every read/write on project-scoped tables includes tenant_id.
- **server/chat-engine/project-context.ts:181** — Query reads project_conversations (pc) without any explicit tenant_id constraint on pc. The WHERE filters c.tenant_id but does not include pc.tenant_id, so project_conversations rows are not individually tenant-fenced.
  - _Fix:_ Include tenant scoping for project_conversations (e.g. add AND pc.tenant_id = ${tenantId} or use .where(eq(project_conversations.tenantId, tenantId))). Ensure every referenced tenant-scoped table has an explicit tenant_id predicate rather than relying on joins or prior validations alone.
- **server/data-protection.ts:359** — SELECT from tenant-scoped table `project_notes` (SELECT title, content, created_at FROM project_notes WHERE project_id = ${proj.id} ...) lacks any tenant_id predicate.
  - _Fix:_ Add an explicit tenant predicate (e.g. AND tenant_id = ${tenantId}) or join via the already-tenant-scoped `projects` row to ensure the project_notes row belongs to the current tenant. Failing that, run the query inside a tenant-scoped transaction (withTenantTx) so the tenant context is enforced.
- **server/data-protection.ts:371** — SELECT from tenant-scoped table `project_files` (SELECT file_name, file_type, file_url, created_at FROM project_files WHERE project_id = ${proj.id} ...) lacks any tenant_id predicate.
  - _Fix:_ Add an explicit tenant predicate (e.g. AND tenant_id = ${tenantId}) or otherwise validate ownership of the referenced project before reading project_files. Alternatively perform these operations inside a tenant-scoped transaction or otherwise bind the tenant to the query to prevent cross-tenant access.
- **server/dfy-intake.ts:52** — getDfyIntakeByToken() queries dfy_intake_forms by token with no tenant_id constraint (SELECT ... WHERE token = ...). This returns a tenant-scoped row without scoping to a caller tenant and can leak/operate across tenants.
  - _Fix:_ Either require and pass an explicit tenantId and include AND tenant_id = ${tenantId} in the WHERE clause, or map the token to its owning tenant server-side and validate ownership before returning the row. Document and defend any intentional global-token design (admin-only, audited) if that is required.
- **server/dormant-deprecation.ts:169** — getTrafficSnapshot(): SELECT COUNT(*) FROM messages WHERE created_at > ... AND role = 'user' is executed without tenant_id. messages is a tenant-scoped table elsewhere; unscoped aggregation reads across tenants.
  - _Fix:_ If the operation must be tenant-scoped, add AND tenant_id = ${tenantId} or run this logic per-tenant. If it is intentionally global/administrative, make the code explicitly require admin/system privileges and/or use a dedicated global analytics table that is safe to query without tenant filters. Document the reason for any global query.
- **server/dormant-deprecation.ts:178** — getTrafficSnapshot(): SELECT COUNT(DISTINCT tool_name) FROM tool_performance WHERE last_success_at > ... is executed without tenant_id, reading tenant-scoped tool_performance across tenants.
  - _Fix:_ Restrict by tenant_id when used for tenant-scoped decisions, or move to an explicit global analytics path requiring admin authorization. Prefer per-tenant aggregation or an audited global metrics table for cross-tenant summaries.
- **server/dormant-deprecation.ts:204** — getEverInvokedTools(): SELECT DISTINCT tool_name FROM tool_performance is run with no tenant constraint, reading a tenant-scoped table globally.
  - _Fix:_ Either add a tenant_id filter to compute 'ever-invoked' per-tenant, or limit this function to admin/system context and use a separate global table designed for cross-tenant aggregation. Make the privilege model explicit if global access is intended.
- **server/dormant-deprecation.ts:214** — getActiveTools14d(): SELECT DISTINCT tool_name FROM tool_performance WHERE last_success_at > ... executes without tenant_id, returning active tools across all tenants.
  - _Fix:_ Scope this query by tenant_id when evaluating per-tenant dormancy. If the auto-deprecation feature is meant to be global, require an explicit system/admin authorization check and/or derive metrics from a non-tenant-scoped analytics table instead of querying tenant-scoped tables without filters.
- **server/email.ts:370** — SELECT on tenant-scoped table `inbox_messages` without tenant_id filter (SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1). This reads tenant-scoped rows with no tenant constraint and may return/inspect rows belonging to other tenants.
  - _Fix:_ Include tenant context in the query (e.g. WHERE message_id = ${msgId} AND tenant_id = ${tenantId}) or resolve the tenant for the message before querying. If the code intentionally needs a global uniqueness check, do it via a globally-unique index and an explicitly audited code path that documents the cross-tenant intent.
- **server/email.ts:415** — SELECT on tenant-scoped table `inbox_messages` without tenant_id filter (SELECT id, message_id FROM inbox_messages WHERE ...). Global scan/backfill reads tenant rows without constraining by tenant_id.
  - _Fix:_ Run backfill per-tenant (loop tenants and include tenant_id in WHERE) or add an explicit tenant_id predicate to the SELECT. Avoid scanning tenant-scoped tables across all tenants without explicit justification and auditing.
- **server/embeddings.ts:814** — SELECT on tenant-scoped table `agent_knowledge` without tenant_id filter (SELECT id, tenant_id, title, content FROM agent_knowledge WHERE ... LIMIT 50). This backfill reads rows across tenants.
  - _Fix:_ Scope backfill queries by tenant (iterate tenants or accept a tenant argument) so each query includes AND tenant_id = ${tenantId}. Avoid cross-tenant SELECTs on tenant-scoped tables.
- **server/felix-brain.ts:23** — SELECT from tenant-scoped table `agent_runs` is executed without constraining by tenant_id (no WHERE tenant_id = ...). This reads in-flight runs for all tenants into process memory, violating the requirement that every read against tenant-scoped tables must be tenant-scoped at the application level.
  - _Fix:_ Restrict the query to the caller's tenant (or iterate per-authorized tenant) by adding a tenant filter, e.g. WHERE tenant_id = ${tenantId} (parameterized). If the intent is to rehydrate only a specific tenant, require the tenantId as an input and validate it. If a multi-tenant bootstrap is intended, perform the operation in an explicit, audited admin context and ensure only authorized admin processes can run it; otherwise avoid scanning all tenants in a single unscoped query.
- **server/firecrawl.ts:231** — UPDATE on tenant-scoped table 'scrapedPages' omits tenant constraint. The code updates by id only: .where(eq(scrapedPages.id, existing[0].id)) — this can modify a row belonging to another tenant if IDs are guessable/shared.
  - _Fix:_ Include the tenant filter in the WHERE clause (or re-check ownership) when updating: .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Alternatively re-read the row with tenant-scoped WHERE and use that row's id for the update, or use a single UPDATE ... WHERE id = $id AND tenant_id = $tenantId pattern.
- **server/firecrawl.ts:363** — UPDATE on tenant-scoped table 'scrapedPages' inside crawl loop omits tenant constraint. The update uses .where(eq(scrapedPages.id, existing[0].id)) without also constraining tenant_id.
  - _Fix:_ Same fix as above: ensure all UPDATEs constrain to caller's tenant, e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Validate ownership before any write.
- **server/google-workspace.ts:2287** — INSERT into presenter_slide_images does not include tenant_id (VALUES (...) ON CONFLICT ...). If presenter_slide_images is tenant-scoped this allows inserting rows with no tenant ownership metadata.
  - _Fix:_ Make presenter_slide_images tenant-aware: include tenant_id in the INSERT values (e.g. VALUES (${tenantId}, ...)) or resolve+validate the session_id→tenant ownership first and only insert when it matches the current tenant. Prefer explicit tenant_id column rather than inferring from session_id.
- **server/health-audit.ts:314** — SELECT FROM code_proposals (stale proposals query) lacks any tenant_id filter. Reading tenant-scoped proposal rows globally can leak tenant data to this scanner.
  - _Fix:_ Run the stale-proposals scan per-tenant (loop tenants) or add an explicit WHERE tenant_id = ${tenantId} filter. If this routine must be global admin-only, make that explicit and ensure only an admin context runs it; otherwise scope queries to the current tenant.
- **server/health-audit.ts:346** — SELECT FROM heartbeat_tasks (stuck heartbeat query) has no tenant_id filter. heartbeat_tasks appear to be tenant-scoped, so this can surface tasks across tenants.
  - _Fix:_ Scope the SELECT to a tenant (WHERE tenant_id = ${tenantId}) or run the scanner per-tenant. If heartbeat_tasks are global, document that explicitly; otherwise always include tenant_id in read queries.
- **server/heartbeat-schedules.ts:60** — SELECT * FROM research_schedules has no tenant_id constraint (SELECT * FROM research_schedules WHERE is_enabled = true AND next_run_at <= NOW()). This reads full schedule rows across all tenants without scoping.
  - _Fix:_ If this is intentionally cross-tenant scheduler work, move the query into a well-documented scheduler-only boundary and restrict the selected columns to minimal metadata (e.g., id and tenant_id) or a dedicated system table. Otherwise add an explicit WHERE tenant_id = ${tenantId}. Ensure any subsequent mutations use tenant-scoped WHERE clauses (e.g., include tenant_id) and that only trusted code executes cross-tenant reads.
- **server/heartbeat-startup.ts:81** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — missing tenant_id in WHERE clause. Updating a tenant-scoped table without constraining tenant_id risks modifying another tenant's row if ids collide/are mis-used.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or perform the update via storage layer functions that assert the tenant context. Ensure all UPDATE/DELETE statements against tenant-scoped tables include an explicit tenant_id condition.
- **server/heartbeat-startup.ts:85** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — missing tenant_id in WHERE clause (second normalization update).
  - _Fix:_ Add AND tenant_id = ${t.tenantId} to the WHERE clause or use the storage API that enforces tenant context. Audit similar ad-hoc DB updates in startup flow to ensure tenant scoping is present.
- **server/heartbeat-task-scheduler-storage.ts:43** — UPDATE heartbeat_tasks SET last_run_at = ${now}, next_run_at = ${nextRun} WHERE id = ${id} ... — UPDATE omits tenant_id constraint. This mutates a tenant-scoped table without verifying tenant ownership.
  - _Fix:_ Add an explicit tenant_id check in the WHERE clause (e.g. AND tenant_id = ${expectedTenantId}) or pass tenant context into this function so the update can include and assert the tenant_id. Prefer using a storage API that enforces tenant scoping rather than raw UPDATEs on tenant-scoped tables.
- **server/heartbeat.ts:260** — SELECT * FROM event_log WHERE status = 'pending' ORDER BY created_at ASC LIMIT 10 — selects event_log rows across tenants with no tenant_id constraint. event_log rows include tenant_id and this read is not tenant-scoped.
  - _Fix:_ Either select only the minimal metadata (id and tenant_id) and then operate per-tenant, or add tenant_id = ${tenantId} if this should be tenant-bound. If this is a scheduler cross-tenant consumer, centralize that behavior in a single audited module and explicitly document/limit the columns read. Ensure any follow-up mutations always include tenant_id in their WHERE clauses (they do later here, but the initial read should still be minimized/explicit).
- **server/heartbeat.ts:2682** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id in WHERE clause after creating a delegated task. This can change approval_status on a task without asserting tenant ownership.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or perform the update through the storage API that enforces the task's tenant. Ensure all direct SQL UPDATE/DELETE calls against tenant-scoped tables include tenant_id checks.
- **server/heartbeat.ts:3154** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id in WHERE clause (delegateTaskFromChat path).
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause or use the storage layer method that asserts the tenant context when modifying heartbeat_tasks. Audit other places that update approval_status (or other columns) to ensure tenant scoping is always enforced.
- **server/job-queue.ts:159** — Atomic claim/update of agent_jobs performs UPDATE ... WHERE id IN (SELECT id FROM agent_jobs ...) without scoping by tenant_id. This mutates tenant-scoped rows (status/attempts/lease_until) across all tenants with no per-tenant constraint.
  - _Fix:_ Make the claim operation tenant-aware: include tenant_id in the selection/UPDATE (e.g. only claim rows for a known tenant), or if the worker must operate globally, ensure each UPDATE that mutates a job also includes an explicit tenant_id = ${job.tenantId} check (or validate the job's tenant before mutating). Add explicit authorization/audit for cross-tenant background operations so job state can't be clobbered across tenants.
- **server/job-queue.ts:195** — reclaimExpiredLeases step-1: UPDATE agent_jobs SET status = 'failed_terminal' ... WHERE status = 'running' AND lease_until < NOW() AND attempts >= max_attempts — no tenant_id filter. This marks running jobs terminal across all tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) or perform the terminalization per-tenant. If reclaiming must be global, add an explicit tenant check derived from the row (or have the reclaim loop iterate tenants) and log/audit tenant impacts.
- **server/job-queue.ts:209** — reclaimExpiredLeases step-2: UPDATE agent_jobs SET status = 'pending' ... WHERE status = 'running' AND lease_until < NOW() — no tenant_id constraint. This requeues jobs across tenants without tenant-scoped guards.
  - _Fix:_ Scope the requeue UPDATE by tenant_id (or iterate per-tenant). When writing back to pending, include AND tenant_id = ${tenantId} (or validate tenant ownership using the job row) to prevent cross-tenant side effects.
- **server/job-queue.ts:238** — completeJob: UPDATE agent_jobs SET status='succeeded' ... WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} — no tenant_id check. A stale or malicious caller could complete a job belonging to another tenant by id only.
  - _Fix:_ Add tenant fencing to the UPDATE (e.g. AND tenant_id = ${expectedTenantId}). When completing by id, require the caller to provide/verify the persisted tenant_id (or read it in a prior tenant-checked SELECT) and include it in the WHERE clause to guarantee the mutation cannot affect other tenants' rows.
- **server/job-queue.ts:298** — failJob terminal-path UPDATE agent_jobs SET status='failed_terminal' ... WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} — no tenant_id constraint. This can mark another tenant's job terminal by id alone.
  - _Fix:_ Include AND tenant_id = ${expectedTenantId} in the WHERE clause (or perform a prior tenant-validated SELECT) before marking a job terminal. Ensure failure transitions are fenced by tenant to avoid accidental or malicious cross-tenant mutations.
- **server/job-queue.ts:318** — failJob retry-path UPDATE agent_jobs SET ... next_run_at = ${nextRunAt} ... WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} — no tenant_id constraint. This re-schedules jobs across tenants without tenant scoping.
  - _Fix:_ Add tenant_id = ${expectedTenantId} to the WHERE clause (or perform a tenant-validated read before updating). Ensure backoff/retry transitions are applied only to jobs that belong to the expected tenant.
- **server/job-queue.ts:335** — cancelJob: UPDATE agent_jobs SET status = 'cancelled' ... WHERE id = ${id} AND status IN (...) — no tenant_id check. Cancelling by id without tenant scoping can cancel jobs belonging to other tenants.
  - _Fix:_ Require tenantId parameter for cancellation, and include AND tenant_id = ${tenantId} in the WHERE clause. If this is an admin-only operator path, enforce and audit admin privileges explicitly before mutating rows unscoped by tenant.
- **server/lib/agent-trace.ts:125** — UPDATE against tenant-scoped table `agent_trace_spans` omits tenant_id in WHERE clause (persistSpanClose). The UPDATE uses only WHERE span_id = ${spanId}, allowing a request handling one tenant to modify/close spans belonging to another tenant if the span_id is known.
  - _Fix:_ Constrain the UPDATE to the owning tenant. Include tenant_id in the WHERE clause (for example WHERE span_id = ${spanId} AND tenant_id = ${tenantId}) or resolve the current tenant (currentTenantId()) and use it as a parameter. Propagate the tenant id into persistSpanClose (or have it read currentTenantId() inside the function) so every UPDATE to agent_trace_spans is scoped to the caller's tenant. Ensure the change is applied at every callsite that closes spans.
- **server/lib/chronicle-precision.ts:98** — Query joins the tenant-scoped table `skills` without constraining it to the caller tenant. The LEFT JOIN `skills s ON s.id = ps.promoted_skill_id` can surface rows from other tenants (s.id present) because there is no `s.tenant_id = ${tenantId}` predicate.
  - _Fix:_ Restrict the join to the caller's tenant. For example, change the join to `LEFT JOIN skills s ON s.id = ps.promoted_skill_id AND s.tenant_id = ${tenantId}` (or add an explicit `AND s.tenant_id = ${tenantId}` in the JOIN condition). Ensure every access to tenant-scoped tables (including in JOINs) includes an explicit tenant_id equality check or equivalent Drizzle `.where(eq(table.tenantId, tenantId))` so cross-tenant reads cannot occur.
- **server/lib/ideabrowser-backfill.ts:49** — SELECT against tenant-scoped table `projects` aggregates across all tenants with no caller tenant filter (no WHERE tenant_id = <tenant>).
  - _Fix:_ Restrict the query to the intended tenant (e.g. add WHERE tenant_id = ${tenantId} or accept an explicit admin/owner tenant param). If this code must compute counts across tenants, make the cross-tenant intent explicit (audit/owner-only path) and ensure callers are authorized to perform cross-tenant reads.
- **server/lib/ideabrowser-backfill.ts:104** — SELECT from `project_files` uses only project_id (no tenant_id check). If `project_files` is tenant-scoped this read lacks an explicit tenant filter/ownership verification.
  - _Fix:_ Either include tenant_id in the WHERE clause (WHERE tenant_id = ${tenantId}) or join/verify ownership via the projects table (e.g. WHERE project_id = ${id} AND tenant_id = ${tenantId}) before querying project_files.
- **server/lib/ideabrowser-backfill.ts:120** — SELECT from `project_notes` uses only project_id (no tenant_id check). If `project_notes` is tenant-scoped this read lacks an explicit tenant filter/ownership verification.
  - _Fix:_ Add an explicit tenant_id predicate or verify project ownership (join to projects WHERE projects.id = project_notes.project_id AND projects.tenant_id = ${tenantId}) before reading project_notes.
- **server/lib/restraint-trust.ts:88** — Use of sql.raw() with a column name derived from caller input (outcome) in a dynamic UPDATE. The code interpolates a runtime value into SQL via sql.raw(column) when updating trust_scores: UPDATE trust_scores SET ${sql.raw(column)} = ${sql.raw(column)} + ${delta} ... — even though the column name is mapped/validated, this is raw SQL interpolation of a request-derived value (injection + isolation risk).
  - _Fix:_ Avoid injecting any runtime/request-derived value into SQL text. Replace the raw-interpolation with an explicit, server-side mapping to fixed parameterized updates (e.g. switch/case on outcome and run an UPDATE that sets a known column identifier), or use Drizzle's typed update API to set columns by property (no sql.raw). If a dynamic column approach is unavoidable, strictly map outcomes to constant column identifiers at compile-time and do not pass any user-derived string into sql.raw; prefer per-outcome prepared statements so no part of the SQL text is built from request data.
- **server/lib/stuck-task-sweep.ts:56** — SELECT from agent_runs scans rows with tenant_id present in results but the WHERE clause does not filter by tenant_id — a read against a tenant-scoped table without an application-level tenant constraint.
  - _Fix:_ If the sweep should be tenant-scoped, add WHERE tenant_id = ${tenantId} (and accept tenantId as input). If it must be cross-tenant for admin auditing, gate the API to authorized admin callers and log/audit access. Never issue cross-tenant SELECTs in code paths intended for tenant-scoped callers; prefer explicit admin-only tooling or per-tenant queries.
- **server/lib/stuck-task-sweep.ts:84** — SELECT from mind_tickets queries tenant-scoped rows but does not include a tenant_id predicate; it reads tenant data across all tenants without an application-level filter.
  - _Fix:_ Make the query tenant-scoped by adding AND tenant_id = ${tenantId} (and pass tenantId into the function) or restrict this function to an explicit admin/audit-only path with thorough authorization and logging. Ensure all reads from tenant-scoped tables include explicit tenant_id filtering unless intentionally and safely operating in an audited admin context.
- **server/lib/system-probes.ts:163** — SELECT from tenant-scoped table `messages` without a tenant_id constraint (no WHERE tenant_id = ...). This performs a tenant-scoped read with no application-level tenant filter.
  - _Fix:_ Constrain the query to an explicit tenant context (e.g. add a .where(eq(messagesTable.tenantId, tenantId)) or call a storage helper that accepts ALL_TENANTS only from an admin context). If the intent is an all-tenants aggregate, move this into an admin-only helper with an explicit, well-documented guard and/or use an admin-only storage API rather than issuing an unscoped select directly.
- **server/martech-bundle.ts:314** — UPDATE against tenant-scoped table `tenantVoiceProfiles` uses only the row id in the WHERE clause (.where(eq(tenantVoiceProfiles.id, existing.id))) and does not include tenant_id ownership constraint. This can allow cross-tenant updates if ids are misused.
  - _Fix:_ Include the tenant ownership check in the WHERE clause. For example: .where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, ${opts.tenantId}))). Alternatively, always use a storage-layer helper that enforces tenant scoping for updates to tenant-scoped tables.
- **server/memory-graph.ts:130** — UPDATE on memory_categories lacks a tenant_id predicate — this UPDATE (memory_count = memory_count + 1 WHERE id = ${catId}) can modify a category row from a different tenant.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${catId} AND tenant_id = ${tenantId}) or perform the update inside a tenant-scoped transaction helper so the tenant_id is enforced. Validate that persona scoping (if needed) is also applied.
- **server/memory-graph.ts:709** — UPDATE on memory_entries lacks a tenant_id predicate — this UPDATE (SET category_id = ${categoryId} WHERE id = ${mem.id}) can modify a memory row belonging to another tenant.
  - _Fix:_ Constrain the UPDATE to the caller's tenant (e.g. WHERE id = ${mem.id} AND tenant_id = ${tenantId}) or use the existing tenant-aware storage/transaction helpers to ensure the write is applied only to the intended tenant's row.
- **server/mpeg-engine.ts:1058** — INSERT into tenant-scoped table `project_files` does not set tenant_id. The code inserts project file metadata without including tenant_id in the VALUES list even though tenant scoping is enforced only at the application layer (no RLS).
  - _Fix:_ Include the tenant id explicitly in the INSERT and bind it from the validated tenant context (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)). Keep the assertProjectInTenant check, but still set tenant_id on every INSERT and validate options.tenantId is a positive integer before writing.
- **server/mpeg-engine.ts:1446** — INSERT into tenant-scoped table `project_files` does not set tenant_id. In the parallel/concatenate path the code inserts project file metadata without providing tenant_id in the VALUES list despite relying on assertProjectInTenant for ownership.
  - _Fix:_ Same fix as above: add tenant_id to the inserted columns and bind options.tenantId into VALUES (e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${options.tenantId}, ${options.projectId}, ...)). Ensure assertProjectInTenant is performed and that options.tenantId is validated as a positive integer prior to the insert.
- **server/oauth-subscriptions.ts:906** — Query over oauth_subscriptions scans all tenants (SELECT ... FROM oauth_subscriptions WHERE is_active = TRUE) with no tenant_id constraint — returns cross-tenant rows including sensitive tokens.
  - _Fix:_ Restrict this query to a single tenant (e.g. WHERE tenant_id = ${tenantId} AND is_active = TRUE) or otherwise iterate only over an explicit, authorized tenant list. Avoid broad, tenant-agnostic queries against tenant-scoped tables unless run from a carefully-audited administrative routine that enforces explicit authorization.
- **server/oauth-subscriptions.ts:915** — Query SELECT tenant_id, is_active FROM oauth_subscriptions WHERE provider = 'google-workspace' has no tenant_id filter — exposes tenant-scoped rows across all tenants.
  - _Fix:_ Add an explicit tenant filter (WHERE provider = 'google-workspace' AND tenant_id = ${tenantId}) or require an explicit list of tenant IDs that the admin context is authorized to act on. Do not perform unscoped queries against oauth_subscriptions.
- **server/oauth-subscriptions.ts:1258** — Proactive refresh iterate query SELECT provider, tenant_id, expires_at, is_active, consecutive_failures FROM oauth_subscriptions WHERE is_active = TRUE lacks tenant_id constraint and reads rows for all tenants.
  - _Fix:_ Limit the query to a specific tenant (WHERE tenant_id = ${tenantId}) or pass/derive an explicit tenant list that the caller is authorized to operate on. Any read of a tenant-scoped table must include a tenant_id filter.
- **server/oauth-subscriptions.ts:1328** — forceRefreshAllSubscriptions uses SELECT provider, tenant_id FROM oauth_subscriptions WHERE is_active = TRUE without a tenant_id constraint, returning rows for all tenants.
  - _Fix:_ Avoid unscoped reads. Require callers to provide an explicit tenantId or authorized tenant list and add WHERE tenant_id = ${tenantId} (or iterate per-tenant) before performing refresh operations.
- **server/orchestrator-ledger.ts:451** — Use of sql.raw(lockKey) inside SQL (SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)). lockKey is derived from ledgerKey (caller-provided) and tenantId — using sql.raw with a value derived from caller input risks SQL injection and bypassing parameterization.
  - _Fix:_ Do not inject caller-derived values with sql.raw. Pass the advisory lock key as a bound parameter instead (e.g. SELECT pg_advisory_xact_lock(${BigInt(lockKey)}::bigint)) or compute a numeric value server-side and supply it as a parameter. Validate and sanitize ledgerKey before use and avoid sql.raw for untrusted input.
- **server/pace-control.ts:85** — Query against heartbeat_logs (COUNT(*) ... FROM heartbeat_logs WHERE created_at > ... AND ${laneFilter}) lacks a tenant_id constraint — returns activity across all tenants.
  - _Fix:_ Heartbeat logs are tenant-scoped. Add tenant_id = ${tenantId} to the WHERE clause and change the API to accept an explicit tenantId (or otherwise scope the query to an authenticated tenant). Ensure all queries against tenant-scoped tables include the caller's tenant_id.
- **server/pace-control.ts:140** — Dashboard snapshot query SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ... (and the following personaRows query) do not filter by tenant_id — exposing cross-tenant activity.
  - _Fix:_ Scope snapshot queries to a specific tenant by adding WHERE tenant_id = ${tenantId} (and pass tenantId into getPaceSnapshot or otherwise constrain aggregation to a permitted tenant). Avoid global queries on tenant-scoped tables.
- **server/persona-export.ts:33** — SELECT * FROM personas WHERE id = ${personaId} reads a persona row without constraining by tenant_id — allows exporting a persona belonging to another tenant.
  - _Fix:_ Require and use the tenantId to scope the query: SELECT * FROM personas WHERE id = ${personaId} AND tenant_id = ${tenantId}. Additionally, verify ownership/access control before exporting persona data.
- **server/plan-executor.ts:1138** — SELECT id, tenant_id, status, plan_json, execution_log FROM plans WHERE status = 'executing' AND updated_at < ... has no tenant_id filter. This boot-time recovery query reads across tenants and can surface or iterate over other tenants' plans without an explicit admin tenant guard.
  - _Fix:_ Restrict recovery scans to an explicit tenant context or run the recovery per-tenant. If this is intended to be a global admin operation, require and check an admin identity (e.g. ADMIN_TENANT_ID or an explicit admin-only code path) before querying, and/or add a configurable tenant filter so the query becomes WHERE status='executing' AND tenant_id = ${adminTenantId} AND ... or iterate tenants explicitly.
- **server/plan-executor.ts:1204** — SELECT id, tenant_id, status, ceo_decision, ... FROM plans WHERE status = 'failed' AND ceo_decision = 'approved' ... lacks a tenant_id constraint. This selects failed plans across tenants for transient-recovery logic, potentially exposing or operating across tenants without an explicit admin boundary.
  - _Fix:_ Require an explicit tenant context or admin authorization for transient recovery. Either add AND tenant_id = ${tenantId} to the WHERE clause, or perform per-tenant recovery loops after enumerating tenants with explicit admin privileges. If intended as a global admin operation, gate it behind an admin-only check and document it clearly.
- **server/plan-executor.ts:1263** — SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 returns approved plan ids across all tenants (no tenant_id filter) and then fire off executePlan(r.id) for each. Because executePlan's claim UPDATE also lacks a tenant constraint, this can cause cross-tenant plan execution.
  - _Fix:_ Enumerate approved plans per-tenant or add a tenant constraint to the SELECT. Prefer iterating tenants (with admin authorization) and invoking executePlan for each tenant-scoped result, or change the SELECT to include AND tenant_id = ${tenantId} so work is processed strictly under the intended tenant context. Ensure executePlan's CAS claim (see finding at line 703) is also fixed to include tenant_id in the WHERE.
- **server/process-governor.ts:1235** — UPDATE governance_rules SET last_triggered_at... WHERE id = ${rule.id} — missing tenant_id constraint. This modifies governance_rules across tenants when rule.id is not validated to belong to the caller's tenant.
  - _Fix:_ Add tenant scoping to the UPDATE: UPDATE governance_rules SET ... WHERE id = ${rule.id} AND tenant_id = ${tenantId}. Ensure any UPDATE/DELETE that touches tenant-scoped tables includes tenant_id.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} is executed with no tenant filter. updateProjectBrain accepts only projectId and will read project data across tenants, breaking tenant isolation.
  - _Fix:_ Require and pass tenantId into updateProjectBrain and scope the query, e.g. SELECT ... FROM projects WHERE id = ${projectId} AND tenant_id = ${tenantId}. Validate ownership before reading/writing per-tenant resources.
- **server/project-brain.ts:332** — Backfill query SELECT DISTINCT p.id, p.name FROM projects JOIN conversations ... has no tenant scoping and will enumerate projects across all tenants. This leaks/operates on cross-tenant data during backfill.
  - _Fix:_ Scope the query by tenant (e.g. WHERE p.tenant_id = ${tenantId}) or perform per-tenant backfill runs. Ensure backfill functions accept a tenantId or explicitly restrict to allowed tenants.
- **server/project-brain.ts:350** — SELECT c.id, c.title FROM conversations WHERE c.project_id = ${proj.id} lacks tenant filtering. Conversations are tenant-scoped and must be constrained to the caller's tenant before being read.
  - _Fix:_ Include tenant filter on conversations queries (AND tenant_id = ${tenantId}) or join via projects that are already validated for tenant ownership. Ensure conversation rows are validated to belong to the expected tenant.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} is executed without tenant constraint. Messages are tenant-scoped and this can expose messages from other tenants.
  - _Fix:_ Constrain message queries by tenant (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}) or validate the conversation ownership before selecting messages. Require tenant context in backfill/update functions.
- **server/recurring-messages.ts:265** — SELECT from tenant-scoped table `agent_knowledge` without tenant_id filter
  - _Fix:_ The cron runner issues a global SELECT across agent_knowledge (tenant-scoped). Either restrict the query to a specific tenant (WHERE tenant_id = ${tenantId}) or explicitly document+gate a global/system scan mode and ensure strict per-row tenant checks before any use. Prefer iterating tenants and running tenant-scoped queries or add an explicit tenant_id parameter to the query to maintain tenant isolation.
- **server/recurring-messages.ts:307** — UPDATE agent_knowledge performed using only id (WHERE id = ${r.id}) with no tenant_id constraint
  - _Fix:_ Include tenant scoping in the UPDATE to prevent cross-tenant writes, e.g. ADD `AND tenant_id = ${r.tenant_id}` to the WHERE clause. Alternatively, retrieve and validate the tenant before updating and use parameterized tenant_id in the UPDATE to ensure the row belongs to the expected tenant.
- **server/reference-learner.ts:296** — UPDATE memory_entries uses WHERE id = ${storedId} without tenant_id constraint (tenant-scoped table)
  - _Fix:_ Constrain the UPDATE by tenant to prevent accidental or malicious cross-tenant mutation. Change the WHERE to include `AND tenant_id = ${input.tenantId}` (or otherwise validate ownership before the UPDATE). Ensure all writes to tenant-scoped tables always include the tenant_id filter.
- **server/research-engine.ts:171** — cleanupZombieSessions executes an UPDATE on research_sessions without scoping by tenant_id. This will mark running sessions as completed across all tenants (except the explicit NOT IN list), violating tenant isolation and allowing cross-tenant state modification.
  - _Fix:_ Restrict the UPDATE to a tenant scope. Either accept/compute a specific tenantId and add AND tenant_id = ${tenantId} to the WHERE clause, or iterate per-tenant and run the UPDATE per-tenant. Ensure the tenant id is a validated integer and always parameterize it (e.g. WHERE tenant_id = ${tenantId} AND status = 'running' ...).
- **server/research-engine.ts:460** — INSERT into project_notes omits tenant_id (INSERT INTO project_notes (project_id, note, author, created_at) ...). Tenant-scoped tables must receive tenant_id explicitly; without it this can insert records with no/incorrect tenant attribution or fail if tenant_id is NOT NULL.
  - _Fix:_ Include tenant_id in the INSERT columns and values and ensure ownership first. For example: INSERT INTO project_notes (project_id, tenant_id, note, author, created_at) VALUES (${projectId}, ${session.tenantId}, ${note}, ${'Research Engine'}, NOW()). Alternatively, resolve/verify the project's tenant via assertProjectInTenant(projectId, session.tenantId) and use that tenant id in the INSERT within a transaction.
- **server/research-engine.ts:468** — Loop inserting findings into project_notes omits tenant_id (INSERT INTO project_notes (project_id, note, author, created_at) ...). Same tenant-attribution/isolation risk applies for each inserted finding.
  - _Fix:_ Include tenant_id in the INSERT and verify project ownership before inserting. E.g. INSERT INTO project_notes (project_id, tenant_id, note, author, created_at) VALUES (${projectId}, ${session.tenantId}, ${noteContent}, ${'Research Engine'}, NOW()). Ensure the code calls assertProjectInTenant(projectId, session.tenantId) (or equivalent) before performing these INSERTs.
- **server/routes.ts:188** — SELECT from tenant-scoped table `file_storage` without tenant_id constraint (startup restoreUploadsFromDb queries all rows matching mimeType/isNull without scoping to a tenant).
  - _Fix:_ Scope the query to a specific tenant before reading rows (e.g. add where(eq(fileStorage.tenantId, tenantId)) or iterate per-tenant). If this must run across tenants, make the operation explicitly platform-admin and document/limit its scope; avoid unscoped reads that can access other tenants' data.
- **server/routes.ts:4653** — INSERT into project_notes omits tenant_id. The code inserts (project_id, note, author) without setting tenant_id, but tenant-scoped tables must receive tenant_id explicitly.
  - _Fix:_ Include the tenant id in the INSERT (e.g. INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'})) or call a storage-layer helper that requires/sets tenantId. Ensure the tenantId is the same one verified earlier (from the conversation lookup) to prevent cross-tenant writes.
- **server/routes/admin-zombie-detector.ts:38** — Query against agent_trace_spans has no tenant filter — SELECT from agent_trace_spans omits WHERE tenant_id = ... and thus can return cross-tenant data.
  - _Fix:_ Constrain the query to the caller's tenant (e.g. add AND tenant_id = ${tenantId}) or, if a platform-wide cross-tenant view is required, make that explicit and safe: require the canonical platform-admin gate and use a dedicated admin aggregation routine that explicitly accepts/validates an intended tenant selector or iterates per-tenant. Never leave tenant-scoped tables unfiltered implicitly.
- **server/routes/admin.ts:881** — SELECT DISTINCT tool_name FROM tool_performance is executed with no tenant constraint — tool_performance appears to be tenant-scoped, so this leaks data across tenants.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE tenant_id = ${tenantId}) or move this into an explicit admin-only aggregation routine that validates intent and tenant scope. Ensure the route resolves a tenantId and applies it in the WHERE clause instead of returning rows across all tenants.
- **server/routes/admin.ts:886** — Query against tool_performance (failingRows) lacks tenant_id filtering — reads recent failures across all tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause, or implement an explicit, documented platform-admin aggregation that safely enumerates per-tenant results. Given there is no DB RLS, the app must enforce tenant scoping in SQL.
- **server/routes/admin.ts:899** — Query against ai_insights (stuckInsights) is missing a tenant filter — reads tenant-scoped ai_insights rows across all tenants.
  - _Fix:_ Constrain the SELECT with WHERE tenant_id = ${tenantId} when the intent is per-tenant data. If cross-tenant inspection is intended for platform admins, make that explicit, require the canonical platform-admin guard, and use a controlled aggregation approach rather than an unscoped query.
- **server/routes/admin.ts:910** — Query against heartbeat_tasks (disabledTasks) is executed with no tenant filter — heartbeat_tasks is tenant-scoped elsewhere and this returns rows for all tenants.
  - _Fix:_ Add WHERE tenant_id = ${tenantId} to restrict results to the caller's tenant, or implement a deliberate platform-admin aggregation that validates and documents cross-tenant access. Never rely on implicit admin tenancy to justify unscoped reads.
- **server/routes/admin.ts:921** — Query against ai_insights (stuckPending) has no tenant constraint — reads applied/high-priority incidents across all tenants without tenant_id filtering.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause for per-tenant results. If global admin visibility is required, route this through a dedicated, well-audited admin routine that explicitly accepts a tenant selector and documents the cross-tenant nature, rather than performing an unscoped read.

### MEDIUM (28)

- **server/agentic/approvals.ts:135** — Bulk UPDATE on tenant-scoped table `agent_approvals` performed without a tenant_id constraint. expireStaleApprovals() updates all pending approvals whose expiry passed across all tenants in one statement.
  - _Fix:_ If this is intended as a multi-tenant sweep, make the multi-tenant intent explicit and ensure callers are authorized to run it, or (preferably) iterate per-tenant and include tenant_id = ${tenantId} in the WHERE clause. At minimum validate/limit scope or document and audit this privileged sweep path.
- **server/agentic-engines.ts:500** — Unscoped SELECT from tenant-scoped table `ai_insights`. retryPendingMinervaRouting() queries ai_insights for rows with action_taken == PENDING_PLAN_MARKER without a tenant filter, returning rows across all tenants.
  - _Fix:_ Scope the query to a tenant (add tenantId parameter / iterate tenants safely) or otherwise ensure the code treats each returned row's tenant_id strictly and that the operation is intended and authorized as a global sweep. Prefer per-tenant queries with explicit WHERE tenant_id = ${tid} to avoid accidental cross-tenant reads/updates.
- **server/agentic-features.ts:861** — SELECT against tenant-scoped table `outreach_enrollments` lacks tenant_id filter. enrollInSequence() checks the sequence belongs to the tenant earlier, but the subsequent query SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' has no tenant constraint.
  - _Fix:_ Add tenant scoping to the existing-enrollment check (e.g. AND tenant_id = ${params.tenantId}), or verify ownership explicitly before reading/writing outreach_enrollments. Always include tenant_id in WHERE for tenant-scoped tables to avoid cross-tenant leakage.
- **server/auth.ts:1136** — DELETE from tenant-scoped table password_reset_tokens without any tenant_id constraint (DELETE FROM password_reset_tokens WHERE expires_at < ...). This performs a cross-tenant mutation from a background task.
  - _Fix:_ Run expired-token cleanup in a platform-admin context explicitly documented/guarded, or iterate per-tenant and include WHERE tenant_id = <tenant> (or require a platform-only maintenance path). Prefer explicit per-tenant deletes or an authorized admin-only maintenance endpoint so tenant isolation reasoning is explicit.
- **server/auth.ts:1242** — DELETE from tenant-scoped table auth_sessions without tenant_id filter (DELETE FROM auth_sessions WHERE expires_at < ...). This clears sessions across all tenants from a background job.
  - _Fix:_ Either scope the cleanup to a specific tenant (DELETE ... WHERE tenant_id = <tenant> AND expires_at < ...) invoked per-tenant, or ensure the caller is platform-admin and clearly document/guard this background operation. Prefer per-tenant maintenance to keep isolation enforced by SQL.
- **server/auth.ts:1339** — DELETE FROM auth_sessions WHERE token NOT LIKE 'v2:%' executed without a tenant_id constraint — this will remove legacy sessions across all tenants.
  - _Fix:_ If the intent is a global migration step, ensure it runs under an audited, platform-admin-only maintenance path. Otherwise iterate tenants and run DELETE ... WHERE token NOT LIKE 'v2:%' AND tenant_id = <tenant> to keep deletions scoped.
- **server/auto-asset-capture.ts:184** — Background SELECT enumerating conversations across tenants (SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ...) with no tenant filter. The backfill reads many tenants' data in one operation.
  - _Fix:_ Run the backfill per-tenant (accept a tenantId and include WHERE c.tenant_id = ${tenantId}), or explicitly gate this maintenance to a platform-admin context and document it. Prefer per-tenant iteration to keep tenant-scoped reads constrained by SQL.
- **server/auto-consolidation.ts:365** — SELECT DISTINCT tenant_id FROM memory_entries WHERE status = 'active' ... — enumerates tenant_ids by scanning a tenant-scoped table with no caller tenant context. This is a cross-tenant read used to schedule work.
  - _Fix:_ Either perform this as a documented platform-level maintenance operation under strict admin control, or maintain a tenant registry to iterate and invoke consolidation per-tenant. Prefer an explicit per-tenant invocation (or an audited platform job) rather than scanning tenant-scoped tables without a tenant filter.
- **server/auto-transcript.ts:128** — Backfill query selects conversations across all tenants (SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations ...) without scoping by tenant_id — reads and then processes data from multiple tenants in one run.
  - _Fix:_ Run backfill per-tenant (add WHERE c.tenant_id = ${tenantId}) or require that the worker is running as a platform-admin maintenance job with explicit authorization and audit logging. Prefer per-tenant scoping to enforce isolation via SQL filters.
- **server/auto-transcript.ts:153** — SELECT DISTINCT across project_conversations / conversations / projects to find linked conversations (SELECT DISTINCT pc.conversation_id, pc.project_id, c.tenant_id FROM project_conversations pc JOIN conversations c ... ) is executed without a tenant constraint, performing cross-tenant reads.
  - _Fix:_ Scope this query to a single tenant (WHERE c.tenant_id = ${tenantId}) or run it from a platform-admin maintenance routine that is explicitly authorized/audited. Prefer per-tenant iteration to keep tenant-scoped reads constrained by SQL.
- **server/auto-transcript.ts:171** — Missing-Links query reads conversations/projects across all tenants (SELECT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL AND EXISTS (SELECT 1 FROM projects p WHERE p.id = c.project_id AND p.tenant_id = c.tenant_id) AND NOT EXISTS (...)) without a tenant_id filter.
  - _Fix:_ Limit the query to a tenant (WHERE c.tenant_id = ${tenantId}) and run per-tenant, or make this operation an explicit, audited platform-level maintenance task. Prefer per-tenant scoping so the SQL enforces tenant isolation.
- **server/chat-daily-log.ts:12** — Potential INSERT/UPSERT without explicit tenant_id. updateDailyLog calls `storage.upsertDailyNote({ date, content, personaId, tenantId } as any)` where tenantId is an optional parameter to updateDailyLog. If caller omits tenantId this may perform an upsert without an explicit tenant_id value for a tenant-scoped daily_notes table.
  - _Fix:_ Ensure updateDailyLog is always called with an explicit, validated tenantId. Validate `tenantId` at the entrypoint and fail or derive it explicitly before calling storage.upsertDailyNote so the storage layer never receives undefined/null tenantId. If storage.upsertDailyNote requires tenantId, change the function signature to require it and propagate checks accordingly.
- **server/data-protection.ts:150** — SELECT from tenant-scoped table `conversations` (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}) has no tenant_id constraint.
  - _Fix:_ Restrict the query to a single tenant when running in a tenant context (e.g. WHERE tenant_id = ${someTenantId}) or, if this is an admin/global background job, explicitly document and isolate the admin privilege. Prefer iterating per-tenant or using withTenantTx(tenantId, ...) so application-level tenant predicates (or RLS context) are always present.
- **server/data-protection.ts:403** — SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} is missing a tenant_id constraint.
  - _Fix:_ Scope the count to the conversation's tenant (e.g. WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id} or AND tenant_id = ${tenantId}). Prefer explicit tenant predicates on every read against tenant-scoped tables to avoid accidental cross-tenant information leakage.
- **server/embeddings.ts:256** — Use of sql.raw() with an interpolated vector literal built from a request-derived embedding: embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}. Using sql.raw with interpolated values derived from request/embedding data risks SQL injection if the interpolated content is not strictly validated.
  - _Fix:_ Avoid sql.raw interpolation of untrusted-derived strings. Prefer parameterized bindings or a safe pgvector binding mechanism; if interpolation is necessary, validate/assert the embedding shape strictly (numbers only, fixed length) before interpolation (and centralize that validation).
- **server/google-drive.ts:1473** — SELECT from tenant-scoped table 'projects' reads the project row by id without constraining to the caller's tenant. The code does: .where(eq(projects.id, params.projectId)) and only conditionally compares proj.tenantId to params.tenantId later (or relies on downstream ensureProjectFolder). This performs a tenant-scoped read without applying the caller's tenant filter at query time.
  - _Fix:_ Constrain the SELECT to the caller's tenant (fail-closed) e.g. .where(and(eq(projects.id, params.projectId), eq(projects.tenantId, params.tenantId))) or, if params.tenantId may be absent, require and derive the caller tenant first (or validate ownership immediately after the select and refuse if the caller is not the tenant). Always avoid reading tenant-scoped rows by id alone without verifying tenant ownership in the same query.
- **server/heartbeat-schedules.ts:26** — SELECT from tenant-scoped table `messages` without tenant_id constraint (SELECT DISTINCT tenant_id FROM messages ...). This is a cross-tenant read with no application-level tenant filter.
  - _Fix:_ Avoid doing ad-hoc cross-tenant SELECTs on tenant-scoped tables. Either use a dedicated, global tenant registry table (or system metadata table) to enumerate tenants, or restrict the query to a specific tenant_id. If this cross-tenant enumeration is intentional, centralize it in a clearly-audited scheduler boundary and limit the selected columns to only the tenant identifier (and document the justification). Consider Postgres RLS or an explicit admin-only code path to make the cross-tenant intent explicit.
- **server/heartbeat-task-scheduler-storage.ts:11** — SELECT * FROM heartbeat_tasks ... WHERE enabled = true AND next_run_at <= NOW() ... — this selects heartbeat_tasks across all tenants with no tenant_id filter. Even though this module is named scheduler-cross-tenant, the query reads tenant-scoped rows without tenant constraints.
  - _Fix:_ If cross-tenant enumeration is intended, mark this module as the single audited cross-tenant boundary and restrict selected columns to minimal metadata (id, tenant_id, cron_expression, etc.). Otherwise add tenant_id = ${tenantId} or expose a separate API to fetch per-tenant due tasks. Consider documenting/centralizing cross-tenant privileges and/or using Postgres RLS to limit accidental cross-tenant reads.
- **server/heartbeat.ts:1582** — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' AND status = 'success' ORDER BY id DESC LIMIT 1 — query reads heartbeat_logs without tenant constraint. heartbeat_logs appears tenant-scoped.
  - _Fix:_ Scope the query to a tenant (WHERE tenant_id = ${tenantId}) or, if the intention is to observe the latest successful cross-tenant run, move this logic into an explicit admin/system-level tracking table. At minimum restrict the selected columns and document the cross-tenant intent.
- **server/job-queue.ts:281** — failJob reads max_attempts with SELECT max_attempts FROM agent_jobs WHERE id = ${id} with no tenant_id constraint. This read returns tenant-scoped data without validating the caller's tenant.
  - _Fix:_ When reading job metadata, include tenant_id scoping (e.g. WHERE id = ${id} AND tenant_id = ${expectedTenantId}) or otherwise verify the job belongs to the expected tenant before using the value to decide retry logic. For admin-only paths, ensure explicit admin authorization is enforced and audited.
- **server/job-queue.ts:377** — getJobTenantForAdmin: SELECT tenant_id FROM agent_jobs WHERE id = ${id} executes without tenant constraint. Although used to learn a job's tenant, it's a blind read of a tenant-scoped table without scoping/authorization.
  - _Fix:_ If this helper is intended for admin use, ensure the caller is properly authorized and that the function name/contract documents it's admin-only. Otherwise, accept an expected tenantId and SELECT ... WHERE id = ${id} AND tenant_id = ${tenantId} (or include a protective audit log) to avoid exposing or relying on unscoped reads.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor issues SELECT tenant_id, persona_id, COUNT(*) FROM agent_knowledge GROUP BY tenant_id, persona_id HAVING COUNT(*) >= ${MIN_ENTRIES} — this scans agent_knowledge across all tenants with no per-request tenant scoping.
  - _Fix:_ If this background diagnostic intentionally iterates all tenants, restrict execution to an authorized/maintenance context and ensure strong audit/logging. Otherwise, if tenant-scoped invocation is expected, add a WHERE tenant_id = ${tenantId} filter or iterate tenants explicitly and perform per-tenant queries (and include tenant checks on subsequent mutations like notifications/snapshots).
- **server/lib/mcp-api-keys.ts:166** — SELECT from tenant-scoped table `mcp_api_keys` by key_prefix has no tenant_id constraint in the WHERE clause (reads a tenant-scoped table without constraining to a tenant).
  - _Fix:_ If possible, resolve and pass an explicit tenantId so the query can include `WHERE tenant_id = <tenantId> AND key_prefix = …`. If that’s not possible because this function must derive tenant from the presented key, harden the lookup: add `AND revoked_at IS NULL` to the SELECT, ensure strict rate-limiting and audit logging to prevent prefix enumeration, and consider maintaining a separate global, minimal lookup index/table (prefix → tenant_id) designed for authentication lookups so application reads remain explicit and constrained. Also ensure any subsequent writes/updates use the resolved tenant_id in their WHERE clauses (they do for last_used_at) and review for possible enumeration or timing-attack mitigations.
- **server/lib/outcome-promotion.ts:158** — SELECT from tenant-scoped table `deliverable_grade_events` with no tenant_id constraint — this query aggregates rows across all tenants (tenant_id omitted from WHERE).
  - _Fix:_ If this scan must be tenant-scoped, add an explicit WHERE tenant_id = ${tenantId} (or run the scan per-tenant). If a cross-tenant aggregation is intentional, make the admin intent explicit (e.g. run only in a trusted admin job context) and document/limit returned data. Prefer an explicit per-tenant loop or an authorized admin-only path rather than an unconstrained SELECT on a tenant-scoped table.
- **server/routes.ts:486** — DELETE from tenant-scoped table `order_lookup_codes` without tenant_id constraint (pruneOrderLookupCodes uses DELETE WHERE expires_at < NOW()).
  - _Fix:_ Perform prune per-tenant (DELETE ... WHERE tenant_id = <tid> AND expires_at < NOW()) or require an explicit admin-scoped operation that enumerates tenants and runs scoped deletes. Never run blanket DELETEs on tenant-scoped tables without an explicit tenant filter.
- **server/routes.ts:627** — SELECT across `projects` (tenant-scoped) with no tenant_id constraint in backfillProjectDriveFolders (SELECT p.id, p.name, p.tenant_id ... WHERE p.drive_folder_id IS NULL).
  - _Fix:_ If this backfill is intended to run per-tenant, constrain the SELECT to a tenant_id or iterate tenants and run a scoped query per tenant. Otherwise make the admin intent explicit and gate the operation, but avoid implicit cross-tenant SELECTs without explicit tenant scoping.
- **server/routes.ts:1396** — Multiple SELECTs against tenant-scoped tables (conversations, messages, heartbeat_tasks, memory_entries, heartbeat_logs) with no tenant_id filter in /api/public/stats.
  - _Fix:_ Either add tenant_id filters to these queries when producing tenant-scoped metrics, or if global cross-tenant counts are required restrict this endpoint to platform-admins AND make the tenant-aggregation explicit (e.g. aggregate per-tenant with explicit admin intent). Avoid ad-hoc unscoped selects against tenant-scoped tables.
- **server/routes.ts:1426** — Multiple raw SELECT COUNT(...) FROM tenant-scoped tables (conversations, messages, personas, projects, skills, memory_entries, heartbeat_tasks, heartbeat_logs, inbox_messages, governance_rules, research_sessions) executed without tenant_id constraints in /api/public/architecture.
  - _Fix:_ If this endpoint needs global architecture statistics, ensure it is strictly platform-admin only and make that explicit in code/comments; otherwise, add tenant_id filters or perform controlled per-tenant aggregation. Do not leave unscoped selects against tenant-scoped tables that could expose cross-tenant data.
