# Tenant-Isolation Audit Report

_Generated 2026-09-19T18:52:02.746Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

> ⚠️ **DEGRADED RUN — INCOMPLETE COVERAGE.** 497 file(s) were NOT audited this run (chunk cap in effect). Findings below are PARTIAL — do not read "few findings" as "all clear."

- Coverage: **192/689** file(s) audited · chunks 20/64 OK (0 failed, 44 pending)
- Findings: **66** (CRITICAL 20 · HIGH 40 · MEDIUM 6 · LOW 0)
- Severe dispositions: **60 kept** · 0 precision false-positive · 0 allowlist false-positive · 0 deferred real risk

### CRITICAL (20)

- **server/agent-eval.ts:221** — UPDATE without tenant constraint: agent_evals row is updated using WHERE id = ${evalId} but no tenant_id check. An attacker who guesses or reuses an evalId could modify another tenant's eval row.
  - _Fix:_ Include tenant scoping on the UPDATE, e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}, or use a transaction/SELECT-to-verify ownership before updating.
- **server/agentic-engines.ts:687** — Unscoped UPDATE across tenants: autoApplyEligibleInsights performs UPDATE ai_insights ... WHERE status = 'new' AND category IN (...) ${tenantScope} — when tenantId is undefined tenantScope is empty and the UPDATE touches rows across tenants.
  - _Fix:_ Never run bulk UPDATEs across tenants. Require a tenantId parameter and include AND tenant_id = ${tenantId}. If a global sweep is intended, make the caller iterate tenants and call the tenant-scoped update per-tenant.
- **server/agentic/approvals.ts:135** — Bulk expiry UPDATE missing tenant constraint: expireStaleApprovals updates agentApprovals rows to status='expired' using WHERE status = 'pending' AND expiresAt < now() without filtering by tenant_id, so approvals for all tenants may be expired in one operation.
  - _Fix:_ Scope the expiry to a tenant or require the caller to supply a tenantId and include AND tenant_id = ${tenantId} in the WHERE. If a global sweep is needed, iterate tenants and run the tenant-scoped update per-tenant to preserve tenant isolation.
- **server/agentic/resume-worker.ts:65** — Unrestricted SELECT on tenant-scoped tables (agent_runs / agent_approvals) — the query at db.execute(...) returns rows across all tenants because there is no WHERE tenant_id = <caller tenant> filter.
  - _Fix:_ Restrict the query to the caller's tenant (or otherwise enforce admin-only semantics). For example, add AND r.tenant_id = ${tenantId} to the WHERE clause (and ensure the approval join also matches that tenant) or accept an explicit tenantId parameter and bind it. If the intent is to perform a global multi-tenant sweep, do it only from a dedicated, audited admin path and avoid returning rows into tenant-scoped logic; log and document the admin intent. Always use parameterized Drizzle filters (e.g. .where(eq(agentRuns.tenantId, tenantId))) or sql`` interpolations with an explicit tenant variable to enforce tenant scoping.
- **server/agentic/wake-scheduler.ts:142** — UPDATE on tenant-scoped table agent_wake_schedules performed without constraining tenant_id. The statement `UPDATE agent_wake_schedules SET status = 'firing', attempts = attempts + 1, updated_at = now() WHERE id IN (SELECT id FROM agent_wake_schedules WHERE status = 'pending' AND wake_at <= now() ORDER BY wake_at ASC LIMIT ${limit} FOR UPDATE SKIP LOCKED) RETURNING *` can modify/claim rows across all tenants.
  - _Fix:_ Scope this claim to a specific tenant (or iterate per-tenant). Either (a) accept a tenantId parameter and add `AND tenant_id = ${tenantId}` to the inner SELECT and outer UPDATE so only that tenant's rows are affected, or (b) first query the set of tenant ids with pending wakes and process each tenant separately, always including `WHERE tenant_id = ${tenantId}` on reads and writes. Avoid global UPDATE/SELECT that touch tenant-scoped rows without an explicit tenant_id filter.
- **server/business-tools.ts:96** — INSERT into invoice_items does not set tenant_id. Tenant isolation invariant requires every INSERT into tenant-scoped tables to include the caller's tenant_id; invoice_items is likely tenant-scoped and here is written only with invoice_id (no tenant_id). Without an explicit tenant_id (and no RLS/fk enforcement), items could be orphaned or associated with another tenant's invoice or fail schema NOT NULL constraints.
  - _Fix:_ Include the tenant id on the invoice_items insert (and on any other invoice_items writes/reads). For example, add tenant_id to the INSERT columns and pass the tid value: INSERT INTO invoice_items (tenant_id, invoice_id, description, ...) VALUES (${tid}, ${newId}, ...). Also review all reads/updates/deletes against invoice_items to ensure WHERE tenant_id = ${tid} is enforced, and consider a schema-level NOT NULL tenant_id (or DB constraint) to prevent future omissions.
- **server/commitment-drafter.ts:234** — UPDATE on tenant-scoped table `commitments` without tenant_id constraint: `UPDATE commitments SET draft_status = 'open', drafted_at = NULL WHERE id = ${row.id}`. This can modify a row belonging to any tenant if id is global and row.tenantId is not validated.
  - _Fix:_ Always include the tenant predicate when mutating tenant-scoped rows. Use the known tenantId value and make the WHERE clause include tenant_id = ${tenantId} (and any status guards). For example: `UPDATE commitments SET draft_status='open', drafted_at=NULL WHERE id = ${row.id} AND tenant_id = ${tenantId}`. Prefer using the row.tenantId previously validated (or skip the mutation if tenant is invalid).
- **server/deep-interview.ts:338** — INSERT into tenant-scoped table `project_notes` does not set tenant_id: INSERT INTO project_notes (project_id, content, created_at) SELECT p.id, ${briefContent}, NOW() ... Even though the SELECT enforces p.tenant_id, the INSERT omits tenant_id and will violate the codebase invariant that tenant_id must be explicitly passed on INSERT (columns are NOT NULL with no default).
  - _Fix:_ Include tenant_id explicitly in the INSERT. For example: INSERT INTO project_notes (project_id, content, created_at, tenant_id) SELECT p.id, ${briefContent}, NOW(), p.tenant_id FROM ... AND p.tenant_id = ${state.tenantId} LIMIT 1; or supply ${state.tenantId} directly. Ensure tenant_id column is populated on all inserts to tenant-scoped tables (or use withTenantTx and an INSERT that still sets tenant_id explicitly if schema requires it).
- **server/email.ts:370** — SELECT from tenant-scoped table `inbox_messages` without constraining by tenant_id (SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1). This allows cross-tenant reads and can cause wrong tenant association.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND tenant_id = ${tenantId}) or otherwise compute tenantId first and use it when looking up inbox_messages. Avoid global lookups by message_id alone.
- **server/email.ts:415** — SELECT from tenant-scoped table `inbox_messages` without tenant constraint (SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...) ORDER BY id). This reads rows across all tenants.
  - _Fix:_ Restrict the query to a specific tenant (e.g. WHERE tenant_id = ${tenantId} AND ...). If this backfill must run for all tenants, iterate per-tenant and include tenant_id in each query to preserve isolation.
- **server/email.ts:428** — UPDATE against tenant-scoped table `inbox_messages` without tenant constraint (UPDATE inbox_messages SET ... WHERE id = ${row.id}). The UPDATE can modify rows from any tenant.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}), or perform the update inside a per-tenant loop so the tenant_id is enforced.
- **server/embeddings.ts:845** — SELECT from tenant-scoped table `memory_entries` without tenant constraint in backfillEmbeddingVecs (SELECT id, embedding FROM memory_entries WHERE ... LIMIT 200). This reads memory rows across tenants.
  - _Fix:_ Backfills must be tenant-aware. Either run per-tenant and include AND tenant_id = ${tenantId} in the WHERE clause or otherwise restrict execution to an explicit administrative-safe context that documents the cross-tenant intent and enforces additional protections.
- **server/embeddings.ts:855** — UPDATE of tenant-scoped table `memory_entries` without tenant constraint (UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id}). This can overwrite embedding_vec for rows belonging to other tenants.
  - _Fix:_ Add tenant filtering to the UPDATE (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) and perform the update in a per-tenant backfill loop to preserve isolation.
- **server/embeddings.ts:861** — SELECT from tenant-scoped table `agent_knowledge` without tenant constraint in backfillEmbeddingVecs (SELECT id, embedding FROM agent_knowledge WHERE ... LIMIT 200). This reads knowledge rows across tenants.
  - _Fix:_ As with memories, run the knowledge backfill per-tenant and include AND tenant_id = ${tenantId} in the WHERE clause, or otherwise constrain the query to an explicit admin tenancy with documented controls.
- **server/embeddings.ts:872** — UPDATE of tenant-scoped table `agent_knowledge` without tenant constraint (UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id}). This can modify rows for any tenant.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (and derive tenantId from the row or iterate per-tenant) to ensure writes are confined to the correct tenant.
- **server/embeddings.ts:809** — Fallback branch in storeEmbeddingVec executes UPDATE ${sql.raw(table)} ... WHERE id = ${id} with no tenant filter when options.tenantId is undefined. For tenant-scoped tables (memory_entries, agent_knowledge) this writes without enforcing tenant ownership.
  - _Fix:_ Do not perform tenant-scoped updates without an explicit tenantId. Require options.tenantId for tenant-scoped tables and enforce WHERE tenant_id = ${tenantId}. If a global admin path is needed, make that explicit, documented, and gated with additional checks.
- **server/google-drive.ts:1664** — INSERT into tenant-scoped table 'project_files' does not set tenant_id (or include a tenant-scoped value). Per invariant INSERTs must explicitly set tenantId (columns are NOT NULL with no default).
  - _Fix:_ Include the tenant ID in the INSERT values (or derive it from a validated project->tenant mapping). E.g. INSERT INTO project_files (..., tenant_id, ...) VALUES (..., ${resolvedTenantId}, ...). If caller-supplied projectId is used, validate ownership first and use the trusted tenantId in the INSERT.
- **server/job-queue.ts:160** — claimDueJobs performs an UPDATE on agent_jobs (setting status='running', incrementing attempts, etc.) using an inner SELECT that does not constrain rows by tenant_id. This atomically claims jobs across all tenants instead of scoping to a specific tenant.
  - _Fix:_ Limit claims to an explicit tenant scope (e.g. accept a tenantId argument or run under an explicit ALL_TENANTS/admin path). Add `AND tenant_id = ${tenantId}` to the inner SELECT and include tenant_id in the outer UPDATE WHERE clause (or otherwise require and verify a passed tenantId) so workers cannot claim rows of other tenants.
- **server/job-queue.ts:195** — reclaimExpiredLeases first UPDATE moves expired 'running' rows to failed_terminal without any tenant_id filter, touching jobs from all tenants.
  - _Fix:_ Scope reclaimed-to-terminal updates to a tenant (or require an explicit admin/all-tenants code path). Add `AND tenant_id = ${tenantId}` (or iterate per-tenant) so lease-reclaim terminal transitions cannot affect other tenants' jobs. If global reclaim is intended for operators, require an explicit ALL_TENANTS constant and admin authorization and make that explicit in code/comments.
- **server/job-queue.ts:209** — reclaimExpiredLeases secondly UPDATE re-queues expired 'running' rows back to pending (next_run_at = cooldown) and does so without any tenant_id constraint, affecting jobs across tenants.
  - _Fix:_ As above, restrict the update to a tenant or require explicit admin authorization. Ensure the WHERE includes `AND tenant_id = ${tenantId}` or run the logic per-tenant so jobs are never re-queued across tenant boundaries inadvertently.

### HIGH (40)

- **server/ab-optimizer.ts:111** — Cross-tenant SELECT: runDueAbExperiments queries the experiments table without scoping by tenant_id (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100). This reads rows from all tenants.
  - _Fix:_ Restrict the query to a single tenant (WHERE tenant_id = ${tenantId}) or iterate tenants and run a tenant-scoped query per-tenant. Never select tenant-scoped rows across all tenants without explicit per-tenant scoping.
- **server/agentic-engines.ts:193** — Unscoped SELECT across tenants: suppressPendingRejectedPlanDuplicates performs SELECT FROM plans JOIN ai_insights with an optional tenantScope that may be empty, allowing cross-tenant rows to be returned when tenantId is undefined.
  - _Fix:_ Require an explicit tenant context or iterate tenants from a safe, authoritative list and run the query per-tenant. If the function must support global sweeps, make the caller explicitly pass the tenantId or otherwise make the cross-tenant intent explicit and audited.
- **server/agentic-engines.ts:500** — Unscoped SELECT across tenants: retryPendingMinervaRouting selects ai_insights WHERE status='applied' AND priority='high' AND action_taken = PENDING_PLAN_MARKER without a tenant_id filter, then processes rows for different tenants.
  - _Fix:_ Scope the SELECT by tenant (WHERE tenant_id = ...) or iterate per-tenant and perform tenant-scoped queries. Avoid returning tenant-scoped rows for multiple tenants in a single unscoped query.
- **server/agentic-features.ts:862** — Ownership check missing: enrollInSequence queries outreach_enrollments to see if a contact is already enrolled using WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active' but does NOT filter by tenant_id. This can detect or affect enrollments from other tenants.
  - _Fix:_ Add tenant scoping to the existence check, e.g. WHERE sequence_id = ${params.sequenceId} AND tenant_id = ${params.tenantId} AND contact_email = ${params.contactEmail} AND status = 'active'. Ensure all reads that influence control flow are tenant-scoped.
- **server/agentic/department-budgets.ts:143** — SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' scans the department_budgets table across all tenants with no tenant_id filter — a tenant-scoped table is being read without constraining to a caller tenant.
  - _Fix:_ If this code must operate per-tenant, add an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId} AND status = 'active') and iterate tenants from a trusted admin context. If a global sweep is intended, make that explicit and restrict the operation to an ADMIN-only path (use ADMIN_TENANT_ID or other strong guard), document the intent, and ensure only privileged, well-audited processes can run it. Avoid unguarded cross-tenant reads on tenant-scoped tables.
- **server/auto-asset-capture.ts:207** — SELECT from tenant-scoped table `messages` is executed without constraining by tenant_id (WHERE tenant_id = ...). This violates the tenant-isolation invariant — a poisoned/foreign conversation_id could surface cross-tenant rows.
  - _Fix:_ Constrain the query by the conversation's tenant (e.g. add AND tenant_id = ${c.tenant_id}) or join to conversations with an explicit tenant_id filter so the messages read is guaranteed to belong to the expected tenant before using the rows.
- **server/auto-project.ts:135** — Query selects messages (tenant-scoped table) by conversation_id only: `WHERE conversation_id = ${conversationId}` without an AND tenant_id = ${tenantId}. Even though the conversation row was validated earlier, every read against tenant-scoped tables must include an explicit tenant_id constraint.
  - _Fix:_ Add tenant constraint to the messages query (e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}) or join messages to conversations with the tenant_id equality to ensure reads are scoped to the caller's tenant.
- **server/auto-transcript.ts:46** — Selecting from tenant-scoped `messages` table using only conversation_id: `WHERE conversation_id = ${conversationId}` (no tenant_id filter). This violates the requirement that every read against tenant-scoped tables include an explicit tenant constraint.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (or join messages to conversations/projects that are already verified to belong to the tenant) so the messages read are explicitly scoped to the caller's tenant.
- **server/ceo-orchestrator.ts:661** — Selecting from the tenant-scoped personas table without constraining by tenant_id. The code does `const cachedPersonas = await db.select().from(personasTable);` which returns personas across all tenants, breaking tenant isolation.
  - _Fix:_ Filter persona reads to the plan owner tenant. Add an explicit WHERE on tenant_id (or use Drizzle's .where(eq(personasTable.tenantId, plan.tenantId))). Example: `const cachedPersonas = await db.select().from(personasTable).where(eq(personasTable.tenantId, plan.tenantId));` or cache per-tenant results. Ensure any code that depends on cachedPersonas receives only rows for the caller's tenant.
- **server/channel-routing.ts:30** — Query joins the global channel_routes table to the tenant-scoped personas table without a tenant filter: `SELECT cr.*, p.name as persona_name FROM channel_routes cr JOIN personas p ON p.id = cr.persona_id WHERE cr.enabled = true`. This reads persona rows from all tenants.
  - _Fix:_ Make persona lookup tenant-aware. Either (a) make channel_routes tenant-scoped (add tenant_id and filter it), or (b) accept a tenantId parameter and include `AND p.tenant_id = ${tenantId}` in the JOIN/WHERE so persona names are only resolved for the caller's tenant. Example: `... JOIN personas p ON p.id = cr.persona_id AND p.tenant_id = ${tenantId} WHERE cr.enabled = true`. Ensure all code paths that call listChannelRoutes pass the caller's tenantId.
- **server/chat-engine/project-context.ts:23** — SELECT joins project_conversations (pc) but does not constrain pc.tenant_id — tenant-scoped table rows are only partially filtered via projects.p.tenant_id. This can allow cross-tenant project_conversations rows to be matched (IDOR / tenant isolation bypass).
  - _Fix:_ Include an explicit tenant filter on project_conversations (pc) in the WHERE clause. E.g. add AND pc.tenant_id = ${tenantId} (or use Drizzle .where(eq(project_conversations.tenantId, tenantId))). Ensure all joins/filters on pc are scoped to the caller's tenant.
- **server/chat-engine/project-context.ts:38** — Subquery/EXISTS uses project_conversations (pc) without constraining pc.tenant_id while enumerating projects p.tenant_id = ${tenantId}. The EXISTS check may match cross-tenant linkage rows and leak or associate projects incorrectly across tenants.
  - _Fix:_ Constrain the EXISTS to the tenant as well, e.g. WHERE pc.conversation_id = ${conversationId} AND pc.project_id = p.id AND pc.tenant_id = ${tenantId} (or use Drizzle .where(eq(project_conversations.tenantId, tenantId))). Ensure every reference to project_conversations includes tenant scoping.
- **server/chat-engine/project-context.ts:181** — Query selecting project conversations joins project_conversations (pc) but does not include a tenant_id constraint on pc. Even though pc.project_id = ${projectId} was previously verified, every tenant-scoped table access must include an explicit tenant filter to enforce isolation.
  - _Fix:_ Add an explicit tenant filter on project_conversations (pc), e.g. WHERE pc.project_id = ${projectId} AND pc.tenant_id = ${tenantId} AND c.id != ${conversationId} AND c.tenant_id = ${tenantId}, or use Drizzle eq(pc.tenantId, tenantId). Ensure all tenant-scoped tables are always filtered by tenant_id.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` without tenant_id constraint (SELECT id, tenant_id, description, ... FROM commitments WHERE status = 'active' AND due_at IS NOT NULL ...). This reads tenant-scoped rows across all tenants.
  - _Fix:_ Restrict reads to a tenant context or require an explicit admin authorization check. If this routine must run as a global admin scanner, make that explicit and minimize returned sensitive columns (avoid selecting free-text fields like description). Otherwise add `WHERE tenant_id = ${tenantId}` or otherwise scope the query to the caller's tenant. If the intent is platform admin visibility, ensure the code path runs only under a vetted admin principal and document the exception.
- **server/data-protection.ts:150** — SELECT from tenant-scoped table `conversations` without a tenant_id predicate (SELECT id, tenant_id, title FROM conversations WHERE deleted_at IS NOT NULL AND deleted_at < ${cutoff}). This scans rows across all tenants in a codebase that relies on app-layer WHERE filters for isolation.
  - _Fix:_ Avoid scanning tenant-scoped tables without an explicit tenant predicate. If this job must run per-tenant, iterate tenants and include WHERE tenant_id = ${tenantId} (or run inside withTenantTx/administrative per-tenant transaction). If it is an admin/global maintenance task, ensure only an explicit, audited admin context can call it and document the intent; otherwise add a tenant_id filter or use per-tenant processing to prevent accidental cross-tenant exposure.
- **server/doc-collections.ts:166** — DELETE from tenant-scoped table doc_chunks lacks tenant_id constraint (DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId}).
  - _Fix:_ Restrict the DELETE to the caller's tenant. Add AND tenant_id = ${tenantId} (or join to doc_collections with a tenant filter) so the statement becomes: DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} AND tenant_id = ${tenantId}. Ensure every DELETE/UPDATE on doc_chunks includes tenant_id in the WHERE.
- **server/doc-collections.ts:295** — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} does not include tenant_id in its WHERE clause (writes to tenant-scoped table without tenant scoping).
  - _Fix:_ Include tenant scoping in the UPDATE to prevent cross-tenant writes. Use WHERE id = ${row.id} AND tenant_id = ${tenantId} (or otherwise verify the row's tenant before updating). Ensure all updates to doc_chunks include eq(doc_chunks.tenant_id, tenantId) in the WHERE.
- **server/embeddings.ts:259** — Use of sql.raw() with an interpolated vector literal (embedding) derived from the request/query (embedding built from user query) in vectorSearchMemory: 1 - (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}). This constructs SQL fragments from dynamic data.
  - _Fix:_ Avoid string-interpolating user/request-derived values into sql.raw(). If a literal must be injected, validate and parameterize robustly (as the code already validates numbers), or use parameter binding provided by the query builder rather than embedding a raw string. Ensure tenant scoping is preserved and that the interpolation cannot be manipulated.
- **server/embeddings.ts:585** — Use of sql.raw() with an interpolated vector literal (embedding) derived from the request/query in vectorSearchKnowledge (embedding_vec <=> ${sql.raw(`'${vec}'::vector`)}). This interpolates dynamic data into SQL via sql.raw().
  - _Fix:_ Avoid direct string interpolation into sql.raw() for request-derived embeddings. Prefer parameterized approaches or ensure the literal is strictly validated and documented; ideally run per-tenant and keep tenant filter present (which this query does), and centralize/abstract any raw SQL literal building to a single vetted helper.
- **server/felix-brain.ts:23** — rehydrateTaskStateOnBoot executes a SELECT against the tenant-scoped table `agent_runs` without constraining by tenant_id (query at lines 23-28). This reads rows for all tenants and can leak cross-tenant data at boot.
  - _Fix:_ Restrict the query to the intended tenant(s). Either run the rehydration per-tenant and add a WHERE tenant_id = ${tenantId} clause, or explicitly require/validate an admin-scoped tenant filter before querying. If global multi-tenant enumeration is required, perform it under a controlled, audited admin path (not implicitly at boot) and document/limit its privileges. Always ensure caller-supplied or runtime-derived tenant identifiers are passed as parameters (e.g. WHERE tenant_id = ${tenantId}) instead of scanning all rows.
- **server/firecrawl.ts:239** — UPDATE on tenant-scoped table 'scrapedPages' uses WHERE eq(scrapedPages.id, existing[0].id) without constraining by tenant_id. Per invariant every write to a tenant-scoped table must include the caller's tenant filter.
  - _Fix:_ Include the tenant constraint in the WHERE clause, e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Alternatively use Drizzle helpers that combine id+tenant checks in a single query before updating.
- **server/firecrawl.ts:372** — UPDATE on tenant-scoped table 'scrapedPages' uses WHERE eq(scrapedPages.id, existing[0].id) without constraining by tenant_id. This allows a write without an explicit tenant filter.
  - _Fix:_ Add an explicit tenant filter to the UPDATE, e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Prefer always including tenantId in the WHERE for tenant-scoped tables.
- **server/google-drive.ts:879** — UPDATE on tenant-scoped table 'projects' uses .where(eq(projects.id, projectId)) but does not include the tenant_id constraint even though ownership was just checked. Every write must include the tenant filter.
  - _Fix:_ Add the tenant constraint to the UPDATE, e.g. .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). This ensures the update is scoped to the caller's tenant and prevents cross-tenant writes.
- **server/glasses-gateway.ts:173** — UPDATE on tenant-scoped table 'apiKeys' sets lastUsedAt using .where(eq(apiKeys.id, key.id)) without a tenant_id constraint. Even though the key was looked up earlier, writes must still include the tenant filter.
  - _Fix:_ Constrain the UPDATE by tenant as well, for example .where(and(eq(apiKeys.id, key.id), eq(apiKeys.tenantId, (req as any).tenantId))). Ensure all writes to tenant-scoped tables include the caller's tenant_id in the WHERE clause.
- **server/health-audit.ts:314** — SELECT from tenant-scoped table `code_proposals` is not tenant-scoped — query lacks any tenant_id constraint.
  - _Fix:_ Scope this query to a tenant (e.g. add WHERE tenant_id = ${tenantId}) or explicitly iterate per-tenant as an admin operation. Never read tenant-scoped rows without filtering by the caller's tenant context.
- **server/health-audit.ts:333** — UPDATE on tenant-scoped table `code_proposals` modifies rows without constraining by tenant_id (WHERE clause uses only id array).
  - _Fix:_ Include tenant scoping in the UPDATE (for example: WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or otherwise verify ownership before updating. Avoid updating tenant data across tenants.
- **server/health-audit.ts:346** — SELECT from tenant-scoped table `heartbeat_tasks` is not tenant-scoped — query lacks any tenant_id constraint.
  - _Fix:_ Restrict the SELECT to the calling tenant (e.g. add WHERE tenant_id = ${tenantId}) or explicitly design this routine to iterate safely per-tenant with explicit authorization. Do not read tenant-scoped tables globally.
- **server/health-audit.ts:368** — UPDATE on tenant-scoped table `heartbeat_tasks` clears/enables tasks without constraining by tenant_id (WHERE id = ANY(...)).
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or verify tasks belong to the intended tenant before mutating them. Avoid blind global updates on tenant-scoped tables.
- **server/google-workspace.ts:2287** — INSERT into presenter_slide_images does not include tenant_id. If presenter_slide_images is tenant-scoped, this inserts rows without an explicit tenant assignment.
  - _Fix:_ Include the tenant_id column/value in the INSERT (VALUES (..., ${tenantId}, ...)) or otherwise resolve & validate the session→tenant ownership and only insert when tenant is known. Ensure all INSERTs into tenant-scoped tables set tenant_id explicitly (columns are NOT NULL/no default).
- **server/google-workspace.ts:2293** — INSERT into presenter_slide_images for thumbnail rows does not include tenant_id. This writes tenant-scoped data without a tenant constraint.
  - _Fix:_ Pass tenant_id in the INSERT for thumb rows as well (e.g. add tenant_id column and VALUES ${tenantId}) or validate session ownership and derive tenant_id server-side before inserting. Never rely on implicit or foreign-key-only enforcement.
- **server/google-workspace.ts:2307** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} — no tenant constraint. This updates a session row without verifying it belongs to the caller's tenant.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or first SELECT the session by id and verify its tenant_id matches caller's tenant before updating. Always include tenant filters on mutations to tenant-scoped tables.
- **server/heartbeat-startup.ts:81** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — UPDATE on tenant-scoped table `heartbeat_tasks` without constraining tenant_id.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or route this write through storage/updateHeartbeatTask which asserts the tenant context. Ensure the code asserts the caller/admin tenant before modifying rows.
- **server/heartbeat-startup.ts:85** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — second occurrence of an UPDATE on tenant-scoped table without tenant_id predicate.
  - _Fix:_ Same fix: constrain by tenant_id (WHERE id = ${t.id} AND tenant_id = ${t.tenantId}) or call into storage/updateHeartbeatTask so tenant assertion is enforced before the UPDATE.
- **server/heartbeat-task-scheduler-storage.ts:12** — getDueHeartbeatTasksAcrossTenants() performs SELECT * FROM heartbeat_tasks ... with no tenant_id filter — reads tenant-scoped data across all tenants.
  - _Fix:_ If this function is intentionally a scheduler cross-tenant boundary, make that explicit in code/comments and restrict returned columns to only the minimal metadata needed (e.g. id, tenant_id, cron fields) OR ensure callers are privileged scheduler code. Otherwise add a tenant_id filter or provide a separate storage method that enforces tenant context for request-scoped callers.
- **server/heartbeat-task-scheduler-storage.ts:44** — claimDueHeartbeatTasksAcrossTenants() runs UPDATE heartbeat_tasks SET ... WHERE id = ${id} ... without tenant_id constraint — updates tenant-scoped rows without asserting tenant ownership.
  - _Fix:_ Include tenant scoping in the WHERE clause (AND tenant_id = ${tenantId}) or require the caller to pass/verify tenantId before performing the UPDATE. Alternatively, perform claiming through a scheduler-specific trusted API that atomically asserts/records the tenant context.
- **server/heartbeat.ts:260** — Unscoped SELECT from tenant-scoped table `event_log` (SELECT * FROM event_log WHERE status = 'pending' ...) with no tenant_id predicate.
  - _Fix:_ If the heartbeat is intended to process events cross-tenant, narrow the projection to tenant identifiers only (SELECT id, tenant_id, ...) or ensure this code is an explicit admin/scheduler boundary. If this query should be tenant-scoped, add WHERE tenant_id = ${tenantId} or use a storage function that asserts tenant context before reading events.
- **server/heartbeat.ts:2682** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — UPDATE on tenant-scoped `heartbeat_tasks` with no tenant_id filter (created inside processDelegations).
  - _Fix:_ Constrain the UPDATE by tenant_id (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or perform the approval_status change via storage.updateHeartbeatTask which validates tenant ownership. Always assert the tenant before mutating tenant-scoped rows.
- **server/heartbeat.ts:3155** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — UPDATE on tenant-scoped `heartbeat_tasks` without tenant_id (delegateTaskFromChat path).
  - _Fix:_ Same remediation: include tenant_id in the WHERE clause (AND tenant_id = ${tenantId}) or use the storage layer that enforces tenant context for updates. Ensure approval workflow cannot modify another tenant's task row by id alone.
- **server/job-queue.ts:238** — completeJob updates agent_jobs -> status='succeeded' using WHERE id = ${id} AND status = 'running' AND attempts = ${attempts} but does not include tenant_id, allowing a caller to complete a job from a different tenant by id.
  - _Fix:_ Require the caller to provide/verify tenant scope and include `AND tenant_id = ${tenantId}` in the WHERE clause (or read and assert the persisted tenant before updating). Use lease/attempt fencing plus tenant checks to prevent cross-tenant completes.
- **server/job-queue.ts:281** — failJob reads max_attempts via SELECT max_attempts FROM agent_jobs WHERE id = ${id} without scoping by tenant_id, then later updates agent_jobs (terminal or requeue) also without tenant_id. These allow failures/completions to be recorded for jobs of other tenants.
  - _Fix:_ When looking up and mutating job rows, require and validate tenant scope. Either SELECT ... WHERE id = ${id} AND tenant_id = ${tenantId} (or require an explicit admin path), and include tenant_id in subsequent UPDATE WHERE clauses to ensure only the owning tenant's job row is mutated. Do not rely solely on id+attempts for fencing without tenant checks.

### MEDIUM (6)

- **server/commitment-drafter.ts:146** — Cross-tenant SELECT against tenant-scoped table `commitments` with no tenant_id constraint (SELECT DISTINCT tenant_id ... FROM commitments). This enumerates tenant rows across all tenants.
  - _Fix:_ Avoid scanning the entire tenant-scoped table without an explicit admin/authorization guard. Either restrict this operation to an authenticated admin tenant (and include an explicit WHERE tenant_id = ${adminTenant} or similar), or enumerate tenant ids from a trusted system table that is intended for cross-tenant listing (e.g. tenants) under an admin-only code path. If the job must iterate tenants, ensure the SELECT is performed in an explicitly-authorized admin context and minimize the returned columns (only tenant_id) or implement a safe admin-only query path documented and audited.
- **server/data-protection.ts:404** — SELECT COUNT(*) FROM messages WHERE conversation_id = ${conv.id} is executed without scoping by tenant_id. Although `conv.id` was retrieved from a tenant-scoped query, this standalone SELECT against `messages` lacks an explicit tenant predicate and can act as a cross-tenant existence/oracle.
  - _Fix:_ Include the tenant predicate in the message count query, e.g. SELECT COUNT(*)::int as cnt FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${tenantId}. Prefer binding tenantId from the surrounding function argument so every read against tenant-scoped tables is explicitly constrained.
- **server/heartbeat-schedules.ts:60** — Unscoped SELECT from tenant-scoped table `research_schedules` (SELECT * FROM research_schedules ... ) with no tenant_id filter.
  - _Fix:_ If this is intended to be a cross-tenant scheduler enumeration, limit the selected columns to tenant-identifying metadata (e.g. SELECT id, tenant_id) and clearly document/contain this as an admin/scheduler-only boundary. Otherwise add an explicit tenant_id predicate (WHERE tenant_id = ${tenantId}) or use the storage API that asserts tenant context before reading tenant-scoped rows.
- **server/job-queue.ts:335** — cancelJob updates agent_jobs SET status='cancelled' WHERE id = ${id} AND status IN (...) but does not restrict by tenant_id. An unauthenticated or mis-scoped caller could cancel another tenant's job by id.
  - _Fix:_ Require tenantId (or admin authorization) and include `AND tenant_id = ${tenantId}` in the WHERE clause. If this API is intentionally admin-facing, make the admin-only nature explicit and gate it with an admin check and explicit ALL_TENANTS handling.
- **server/job-queue.ts:375** — getJobTenantForAdmin SELECT tenant_id FROM agent_jobs WHERE id = ${id} executes without a tenant filter. Although intended for admin lookup, an unguarded read against the tenant-scoped table is performed without an explicit admin scope comment or authorization here.
  - _Fix:_ Mark this function clearly as admin-only and require an authorization check at call sites, or accept an explicit admin-only code path. If callers are tenant-scoped, pass the tenantId and add `AND tenant_id = ${tenantId}`. Prefer explicit APIs that return job tenant only to properly-authorized operator roles.
- **server/job-queue.ts:406** — listJobs builds a WHERE clause that may be empty when tenantId is not provided. The resulting SELECT FROM agent_jobs may run unscoped and return rows across tenants if called without tenantId (no enforcement in the function itself).
  - _Fix:_ Require callers to pass an explicit tenantId for tenant-scoped listing, or require that callers intentionally pass an ALL_TENANTS constant and be admin-authorized. If tenantId is optional, make the function enforce caller scope (throw if tenantId is missing in tenant-facing contexts) and ensure the query always includes `AND tenant_id = ${tenantId}` for tenant-scoped requests.
